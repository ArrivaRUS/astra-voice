# Дизайн-спецификация — Astra Voice

> **ЧЕРНОВИК.** Окончательно утверждает Юрка (гейт **G3**). Пока документ не принят,
> разработка не стартует.
> Направление **A «Панель»** · окно **900 × 620** · принято на ⛔ G2 (2026-09-08).
> Автор: `design-engineer` · дата: 2026-09-08 · версия 0.1 (черновик, раунд 2).

## Как читать этот документ

- **Все числа — из `design/tokens.json` или из замеров DOM.** Ничего «на глаз».
  Токены пишутся как `token(radius.card)` — значение брать из JSON, а не переписывать.
- **Цвета — только ссылками на токены.** `color.light.*` / `color.dark.*` — тема приложения;
  `color.fixed.*` — то, что рисуется поверх чужого рабочего стола и с темой не меняется.
- **Источник значения** указан у каждого блока: `файл:строка` генератора макета,
  `brand-basics.md §…`, `flows.md:…`, `decisions/log.md` или **«замер DOM»** —
  `chromium --headless=new --dump-dom` + `getBoundingClientRect()` по странице,
  собранной теми же функциями `_base`/`_shell`.
- **Референс** — PNG в `design/refs/`; у каждого экрана есть светлая и тёмная версия
  (`…-dark.png`), исходник — одноимённый `.html` в `design/mockups/final/`.
- **`⚠ уточнить`** — место, где в источниках значения нет и я его не выдумывал.
  Все такие места собраны в §14.

## Сквозные правила

1. **Состояния — по единому принципу** (`state.*`, решение Юрки):
   hover = поверхность на ступень, pressed = ещё ступень,
   focus = кольцо **2 px** акцентом снаружи с зазором **2 px**, радиус кольца 8.
2. **`disabled` — только цветом `fg-disabled`, `opacity: 1`.** Прозрачность не используется
   нигде: она гасит текст ниже порога контраста.
3. **Состояние никогда не передаётся только цветом** (PRD §8): рядом всегда подпись,
   иконка или изменение формы.
4. **Переходы:** появление `cubic-bezier(0.2,0,0,1)` / 160 мс, исчезновение
   `cubic-bezier(0.4,0,1,1)` / 120 мс, hover / pressed / focus — 120 мс.
   Уровень звука в пилюле — `linear`, по кадрам.
5. **Цифры набираются PT Mono** (`brand §3`): комбинации клавиш, размеры и ОЗУ моделей,
   версия, проценты, скорости, пути.
6. **Контраст текста ≥ 4,5 : 1 в обеих темах** (PRD §8). Все пары посчитаны в `tokens.md §10`.

---

# 1. Окно и сайдбар

**Референс:** `design/refs/01-general-base.png` · `01-general-base-dark.png`
**Источник:** `_shell.py:29,53-71,193-198` · `_base.py:50-58` · замер DOM.

## 1.1 Окно целиком

| Параметр | Значение | Токен / источник |
|---|---|---|
| Размер | **900 × 620** | `size.window-w/h` · `_shell.py:29` |
| Минимальный размер | **900 × 620** — окно не уменьшается | решение В3 |
| Радиус | 8 | `radius.window` |
| Тень | `0 14px 44px` цвета `color.<тема>.shadow` | `shadow.window` |
| Фон | `bg-app` | `_base.py:50` |
| Раскладка | вертикально: заголовок 32 → тело 552 → строка-статус 36 | `_shell.py:31` |

Тело окна — горизонтально: **сайдбар 184** + **контент 716** (из них колонка содержимого
**672** после отступов 22 слева и справа).

## 1.2 Заголовок окна (title bar)

Рисует оконный менеджер KWin; в макете он воспроизведён, чтобы показать пропорции.

| Параметр | Значение |
|---|---|
| Высота | **32** (`size.titlebar-h`) |
| Паддинг | `0 10`, зазор между элементами 8 |
| Фон / граница снизу | `bg-titlebar` / 1 px `border` |
| Знак слева | 16 px по ширине (`_shell.py:191`) |
| Текст | «Astra Voice — Настройки», `font.role.titlebar` = 12.5 / 500, цвет `fg-secondary` |
| Кнопки окна | 14 × 14, радиус 3; «закрыть» — `#D96B62` (светлая) / `#8C3B36` (тёмная) |

**Реализация:** кнопки и фон заголовка ставит KWin по теме системы — свои не рисуем.
Наше — только имя окна и иконка приложения.

## 1.3 Сайдбар

| Параметр | Значение | Источник |
|---|---|---|
| Ширина | **184**, не тянется | `_shell.py:53` |
| Фон / граница справа | `bg-surface-2` / 1 px `border` | `_shell.py:53` |
| Внутренний отступ | `12 10` | `_shell.py:54` |
| Блок логотипа | паддинг `4 6 12`, отступ снизу 6, граница снизу 1 px `border` | `_shell.py:55` |
| Знак в логотипе | 22 px по ширине, имя 15 px / вес 500 / трекинг +0.03em | `_shell.py:184` |
| Отступ до навигации | 8 | `_shell.py:57` |
| Зазор между пунктами | **2** | `_shell.py:57` |

### Пункт навигации

| Параметр | Значение |
|---|---|
| Высота | **34,3** (замер DOM) |
| Паддинг | `7 9`, радиус `radius.nav-item` = 7 |
| Иконка | 17 px, зазор до текста 9 |
| Текст | `font.role.nav-item` = 13.5 / 500, цвет `fg-secondary` |
| Счётчик справа | `font.role.nav-counter` = 11.5, цвет `fg-muted` |

| Состояние | Оформление |
|---|---|
| покой | прозрачный фон, текст `fg-secondary` |
| **hover** | фон `state.<тема>.hover-on-surface-2` (`#E7ECF4` / `#141924`), 120 мс |
| **pressed** | фон `state.<тема>.pressed-on-surface-2` (`#DDE3EE` / `#1E232D`) |
| **активный** | фон `primary`, текст `primary-fg`; счётчик — `rgba(255,255,255,.75)` в светлой, `rgba(11,18,32,.85)` в тёмной |
| **focus** | кольцо 2 px `state.<тема>.focus-ring`, зазор 2, радиус 8 |
| disabled | не бывает: все разделы всегда доступны |

### Состав навигации

Шесть разделов в фиксированном порядке: **Общие** · **Модели** (со счётчиком) · **Вывод** ·
**Сеть и обновления** · **Продвинутые** · **О программе**.
Внизу, прижатая к низу через распорку, — **«Отладка (Ctrl+Shift+D)»**: вес 400, размер 13,
цвет `fg-muted`; появляется только после комбинации (`_shell.py:175-185`).

## 1.4 Область контента

| Параметр | Значение |
|---|---|
| Шапка раздела | паддинг `12 22 8` |
| Заголовок H2 | `font.role.h2-section` = 20 / 1.25 / 700, цвет `fg` |
| Подзаголовок | `font.role.small` = 13 / 1.45, цвет `fg-muted`, отступ сверху 2 |
| Тело | паддинг `0 22 16`, прокрутка вертикальная |
| Ширина колонки | **672** |

### Полоса прокрутки

Ширина **5**, отступ справа **4**, радиус 3, цвет `fg-faint` при непрозрачности **0.45**
(`component.scrollbar`). Появляется только когда содержимое не помещается.
**⚠ уточнить (У3):** политика показа полосы в QML (`ScrollBar.AsNeeded` против всегда видимой)
и поведение при прокрутке колесом — в макете статичная полоса, поведения нет.

## 1.5 Поведение при resize

Окно фиксированное **900 × 620** и по решению В3 не уменьшается.
**⚠ уточнить (У4):** что делать при **увеличении** окна — тянуть колонку контента
(тогда карточки моделей станут шире) или держать её на 672 и центрировать.
В макете этого случая нет: все страницы нарисованы ровно на 900 px.

---

# 2. Строка-статус (низ окна)

**Референс:** `design/refs/04-footer-states.png` · `04-footer-states-dark.png`
**Источник:** `_base.py:139-143` · `_shell.py:212-283` · `flows.md §3.2`.

| Параметр | Значение |
|---|---|
| Высота | **36** (`size.statusbar-h`) |
| Паддинг / зазор | `0 14` / 10 |
| Фон / граница сверху | `bg-app` / 1 px `border` |
| Базовый текст | `font.role.statusbar` = 12.5, цвет `fg-muted` |
| Версия справа | `font.role.statusbar-num` = **PT Mono 12**, перед ней точка-разделитель 4 px с полями 8 |
| Иконка трея слева | 15 px, цвет `fg-muted`, зазор до текста 7 |

**Композиция:** слева — иконка состояния и активная модель; распорка; справа — состояние
обновления; точка; версия моно.

## 2.1 Левая часть — активная модель (5 состояний)

| Состояние | Текст |
|---|---|
| `loading` | «Загружаю GigaAM v3 RNN-T…» |
| `active` | «GigaAM v3 RNN-T» |
| `switching` | «Переключаю на GigaAM v3 CTC…» |
| `error` | «Модель не загрузилась — открыть Модели» |
| `none` | «Модель не выбрана — установить» |

Источник — `flows.md:218-220`.

## 2.2 Правая часть — обновления (17 состояний)

**Главное правило:** `available` и `admin-track` — **единственный цветовой акцент внизу окна**
(`primary`, вес 500). Всё остальное серое `fg-muted` либо красное `danger-ink` для ошибок.

| # | Состояние | Текст | Вид |
|---|---|---|---|
| 1 | `disabled` | Проверка обновлений отключена | `fg-muted`, не кликается |
| 2 | `policy-locked` | Проверка обновлений отключена (задано администратором) | `fg-muted` + иконка `lock` 12 px |
| 3 | `checking` | Проверяю обновления… | `fg-muted` |
| 4 | `uptodate` | Установлена последняя версия | `fg-muted` + иконка `check` 12 px, держится **3000 мс**, только после ручной проверки |
| 5 | `available` | **Доступна версия 0.2.1 · Установить** | `primary`, вес 500 |
| 6 | `admin-track` | **Доступна 0.2.1 · Скачать для администратора** | `primary` + иконка `shield` 13 px |
| 7 | `downloading` | Загрузка… 42 % | прогресс **120 × 6** + число моно, зазор 8 |
| 8 | `verifying` | Проверяю подпись… | прогресс 120 × 6 на 100 % |
| 9 | `polkit` | Жду подтверждения администратора… | `fg-secondary` |
| 10 | `installing` | Установка… | прогресс 120 × 6 |
| 11 | `restart` | **Установлено 0.2.1 · Перезапустить** | `primary`, вес 500 |
| 12 | `error-net` | Не удалось скачать обновление · Повторить | `danger-ink` + иконка `alert` 12 |
| 13 | `error-sig` | Пакет не прошёл проверку подписи · Подробнее | `danger-ink` + `alert` |
| 14 | `error-polkit` | Установка отменена · Открыть папку | `danger-ink` + `alert` |
| 15 | `unavailable` | Источник обновлений недоступен · Обновить из файла… | `fg-muted` — **не молчать** |
| 16 | `model-update` | Обновление модели: GigaAM v3 RNN-T | `fg-muted`, **не акцент** |
| 17 | `skipped` | Версия 0.2.1 пропущена · Показать | `fg-muted` |

Источник — `_shell.py:212-283`, тексты сверены с `flows.md §3.2`.

## 2.3 Поведение

- Кликабельные состояния: 5, 6, 11, 12, 13, 14, 15, 17. Остальные — просто текст.
- Кликабельный фрагмент получает **focus-кольцо** по общему правилу.
- Смена состояния — переход 160 мс на появление, 120 мс на уход.
- При resize строка растягивается по ширине окна; левая и правая части прижаты к краям
  распоркой, версия всегда крайняя справа.

---

# 3. Строка настройки и карточка-группа

**Референс:** `design/refs/01-general-base.png` · `05-advanced.png`
**Источник:** `_base.py:71-81` · `_shell.py:25-27,70-71`.

## 3.1 Группа

```
CAPS-заголовок группы      ← font.role.group-caps: 11 / 500 / +0.08em / uppercase, fg-muted
   ↓ 5 px
┌───────────────────────┐  ← карточка: radius 10, border 1 px, фон bg-surface
│ строка настройки      │
├───────────────────────┤  ← разделитель 1 px border-soft, ТОЛЬКО между строками
│ строка настройки      │
└───────────────────────┘
   ↓ 8 px до следующей группы
```

| Параметр | Значение |
|---|---|
| Отступ заголовка группы | `0 0 5 2` (`space.group-caption-gap`) |
| Отступ между группами | **8** (`space.group-gap`) |
| Радиус карточки | 10, `overflow: hidden` — чтобы разделители не торчали за скругление |
| Граница | 1 px `border` |

## 3.2 Строка настройки

| Параметр | Значение | Источник |
|---|---|---|
| Паддинг | **`7 14`** | `_shell.py:70` (канон, К11) |
| Минимальная высота | **38** | `_shell.py:70` |
| Зазор между блоками | 10 | `_base.py:73` |
| Подпись | `font.role.setting-label` = 14 / 1.5 / 400, цвет `fg` |
| Пояснение под подписью | `font.role.setting-sub` = 12 / 1.4, цвет `fg-muted`, отступ сверху 2 |
| Левый блок | `flex: 1`, `min-width: 0` — при длинном тексте переносится, контрол не сжимается |
| Значок «?» | 15 × 15, круг, граница 1 px `fg-faint`, текст 10 / 500 `fg-muted` |
| Бейдж «Задано администратором» | паддинг `2 6`, радиус 5, фон `bg-surface-2`, текст 11.5 `fg-muted`, иконка `lock` 11 px |

**Порядок элементов слева направо:** подпись и пояснение → «?» → бейдж замка → контрол.

| Состояние | Оформление |
|---|---|
| покой | как выше |
| **hover** | фон строки `state.<тема>.hover-on-surface`, 120 мс — **⚠ уточнить (У5):** кликабельна ли строка целиком или только контрол |
| **disabled** | подпись и пояснение — `fg-disabled`, контрол в своём disabled-виде, **`opacity: 1`** |
| **заблокировано политикой** | как disabled + бейдж с замком; тумблер — фон `border` |
| **focus** | кольцо на контроле, не на строке |

## 3.3 Баннер внутри карточки (`note`)

Используется для ошибок и предупреждений, привязанных к конкретной настройке.

| Параметр | Значение |
|---|---|
| Паддинг / радиус / зазор | `10 12` / 9 / 9 |
| Текст | 12.5 / 1.45; заголовок — блок, вес 700, отступ снизу 2 |
| Иконка | 15 px, цвет по типу |

| Тип | Фон | Текст |
|---|---|---|
| `info` | `bg-surface-2` | `fg-secondary` |
| `warning` | `warning-bg` | `warning-ink` |
| `error` | `danger-bg` | `danger-ink` |
| `success` | `success-bg` | `success-ink` |

---

# 4. Контролы

**Референс:** `design/refs/01-general-base.png` · `03-output.png` · `05-advanced.png`
**Источник:** `_base.py:84-117` · `_p1.py:13-24` · замеры DOM.

## 4.1 Кнопка

| Параметр | Обычная | Маленькая (`sm`) | Ghost |
|---|---|---|---|
| Высота (замер DOM) | **33,5** | **28,1** | 33,5 |
| Паддинг | `6 12` | `4 9` | `6 8` |
| Размер / вес текста | 13 / 500 | 12.5 / 500 | 13 / 500 |
| Радиус / граница | 7 / 1 px | 7 / 1 px | 7 / без границы |
| Иконка + зазор | 13 px + 6 | 13 px + 6 | 13 px + 6 |

| Вариант | Фон | Текст | Граница |
|---|---|---|---|
| `secondary` (по умолчанию) | `bg-surface` | `fg` | `border` |
| `primary` | `primary` | `primary-fg` | `primary` |
| `ghost` | прозрачный | `primary` | нет |
| `danger` | **не существует** — решение В5 | | |

| Состояние | secondary / ghost | primary |
|---|---|---|
| **hover** | фон `state.*.hover-on-surface` | фон `state.*.primary-hover` (`#19356A` / `#789CEA`) |
| **pressed** | фон `state.*.pressed-on-surface` | фон `state.*.primary-pressed` (`#173263` / `#8AACF0`) |
| **disabled** | фон `bg-surface-2`, текст `fg-disabled`, граница `border`, **opacity 1** | то же |
| **focus** | кольцо 2 px `state.*.focus-ring`, зазор 2, радиус 8 | то же |
| **loading** | **⚠ уточнить (У6):** в макете кнопки с индикатором нет; прогресс всегда рядом, а не внутри кнопки |

**Разрушающие действия** («Удалить модель») оформлены обычной `secondary`; подтверждение
и красный акцент живут в диалоге (см. §11).

## 4.2 Тумблер

| Параметр | Значение |
|---|---|
| Габарит | **38 × 21**, радиус 11 |
| Ручка | 17 × 17, круг, фон `#FFFFFF`, отступ 2 от края |
| Смещение «вкл» | ручка на `left: 19` |
| Выключен | фон `fg-faint` |
| Включён | фон `primary` |
| **Заблокирован политикой** | фон `border` + **бейдж с замком рядом** — цвет не единственный носитель |
| **disabled** | фон `border`, подпись строки `fg-disabled` |
| **hover / pressed** | подсветка строки, сам тумблер не меняется |
| **focus** | кольцо вокруг тумблера |
| Анимация | ручка едет 120 мс кривой `motion.easing.hover` |

## 4.3 Сегментированный переключатель

| Параметр | Значение |
|---|---|
| Высота (замер) | **30,8**; сегмент внутри **28,8** |
| Паддинг сегмента | `5 11`, радиус контейнера 7, `overflow: hidden` |
| Граница | 1 px `border`, фон `bg-surface` |
| Текст | 12.5, цвет `fg-secondary`; активный — вес 500 |
| Активный сегмент | фон `primary`, текст `primary-fg` |
| hover неактивного | фон `state.*.hover-on-surface` |
| focus | кольцо вокруг **всего** переключателя, стрелки ← → переключают |

Применяется для «Удерживать / Нажать-нажать» (`_p1.py:20`).

## 4.4 Выпадающий список

**Закрытый (`select`)**

| Параметр | Значение |
|---|---|
| Высота (замер) | **33,5**, паддинг `6 10`, радиус 7, граница 1 px |
| Текст | 13, цвет `fg`; в disabled — `fg-disabled` |
| Шеврон `chevd` | 13 px, цвет `fg-muted`, прижат вправо распоркой |
| Типовая ширина | **236** в строке настройки, **268** в разделе «Сеть» |

**Раскрытый (`popover`)**

| Параметр | Значение |
|---|---|
| Минимальная ширина | **236**, паддинг 5, радиус 8, граница 1 px |
| Тень | `shadow.popover` = `0 12px 34px` |
| z-index | **5** (`zindex.popover`) |
| Пункт | паддинг `7 9`, радиус 6, зазор 8, текст 13 |
| Выбранный пункт | фон `selection-bg`, текст `selection-fg`, вес 500 |
| Пункт с описанием | описание 11.5, `fg-muted`, отступ сверху 1 |
| Недоступный пункт | `fg-disabled` |
| hover пункта | `state.*.hover-on-surface` |

**⚠ уточнить (У7):** поведение при нехватке места снизу (раскрывать вверх?) и
максимальная высота списка с прокруткой — в макете список всегда помещается.

## 4.5 Поле ввода и поле захвата

| Параметр | Значение |
|---|---|
| Паддинг / радиус / граница | `7 11` / 7 / 1 px `border` |
| Текст | 13; placeholder — `fg-muted`, введённый — `fg` |
| **Режим захвата** | граница `primary` + внутреннее кольцо `0 0 0 2px primary-bg`, текст `fg` |

Полный набор состояний поля захвата комбинации — §7.

## 4.6 Чип-фильтр

Паддинг `4 11`, радиус **14**, граница 1 px, текст 12.5 `fg-secondary`, зазор с иконкой 6.
Активный — фон и граница `primary`, текст `primary-fg`, вес 500.

## 4.7 Чип-хоткей («клавиша»)

| Параметр | Значение |
|---|---|
| Высота (замер) | **26,5** |
| Паддинг | `3 8 2` — снизу на 1 px меньше, под нижнюю грань |
| Радиус | 6, фон `bg-surface-2`, текст `fg` |
| **Нижняя граница** | **2 px** `border` — читается как физическая клавиша |
| Шрифт | **PT Mono 13**, трекинг +0.02em |

## 4.8 Бейдж

Паддинг `2 7`, радиус 5, высота **21,3** (замер), текст 11.5 / вес 500, зазор с иконкой 4.

| Бейдж | Фон | Текст |
|---|---|---|
| **Активна** | `accent-bg` | `accent-ink` (`#0A776A`) |
| **Рекомендуем** | `primary-bg` | `primary` |
| **Обновление доступно** | `warning-bg` | `warning-ink` |
| **Новое** | `bg-surface-2` | `fg-muted` |
| **Ошибка** | `danger-bg` | `danger-ink` |

На одной карточке — **не более двух**; приоритет
Активна > Обновление > Рекомендуем > Новое (`_p1.py:317`).

---

# 5. Карточка модели — 20 состояний

**Референс:** `design/refs/02-models-card-states.png` · `02-models-card-states-dark.png` ·
`02-models-catalog.png`
**Источник:** `_base.py:187-200` · `_shell.py:74-78` · `_p1.py:300-505`.

## 5.1 Геометрия

| Параметр | Значение |
|---|---|
| Паддинг | **`9 13`** (`_shell.py:75` — плотнее базового, чтобы влезть в колонку 672) |
| Отступ снизу | 8 |
| Радиус / граница | 10 / 1 px `border` |
| Верхний блок | текст слева (`flex: 1`) + метрики справа, зазор **14** |
| Разделитель | 1 px `border-soft`, поля `8 0 7` |
| Нижний блок | зазор 9, текст 12 `fg-muted`, перенос разрешён |

## 5.2 Типографика карточки

| Элемент | Значение |
|---|---|
| Имя модели | 15 / 500, цвет `fg` |
| Вендор (в той же строке, после «·») | 12.5 / 400, цвет `fg-muted` |
| Назначение | 12.5, цвет `fg-muted`, отступ сверху 2 |
| Бейджи | ряд с зазором 5, отступ сверху 4, перенос разрешён |
| Метрики | подпись 11.5 `fg-muted`, число внутри значения — вес 700 `fg` |
| Нижняя строка тегов | 12 `fg-muted`, разделители — точки 4 px |
| Числа (МБ, ОЗУ, %, скорость) | **PT Mono** |

## 5.3 Полоски метрик — честность цифр

| Параметр | Значение |
|---|---|
| Подпись слева | ширина **54**, выключка вправо |
| Дорожка | **78 × 6**, радиус 3, фон `bg-surface-2` |
| Заливка «**замерено на этом компьютере**» | **`primary`** |
| Заливка «**оценка / бенчмарк автора**» | **`fg-faint`** |
| Зазор между элементами | 8 |
| Метрик на карточку | 2: «Качество» и «Скорость», вертикальный зазор 5 |

**Правило:** замер и чужой бенчмарк отличаются **и цветом заливки, и словом в подписи** —
не только цветом. Коэффициент пересчёта появляется после первой диктовки (`flows.md:359`).

## 5.4 Двадцать состояний

| # | Состояние | Что показывает карточка | Действия |
|---|---|---|---|
| 1 | **активная модель** | граница `accent`, фон `accent-bg`, бейджи «Активна» + «Рекомендуем» | «Удалить» — **disabled** |
| 2 | **установлена** | обычная карточка | «Выбрать» (primary) · «Удалить» |
| 3 | **скачивается** | разделитель + прогресс на всю ширину + строка «Загрузка 43 % · 5,2 МБ/с · осталось ~25 с» моно + источник | «Отмена» (sm) |
| 4 | **в очереди** | «В очереди — начнём, когда докачается …» | «Отмена» |
| 5 | **проверка контрольной суммы** | прогресс на 100 % + «Проверяю контрольную сумму… 4 файла» | «Отмена недоступна» (текст, не кнопка) |
| 6 | **переключение активной** | граница `accent`, подпись «Переключаю…» | — |
| 7 | **доступна для скачивания** | обычная карточка, метрики серые (оценка) | «Скачать» (primary) · «Из файла…» |
| 8 | **новая в каталоге (≤ 30 дней)** | бейдж «Новое» | «Скачать» · «Из файла…» |
| 9 | **доступно обновление ревизии** | бейдж «Обновление доступно» (`warning`) | «Обновить» · «Подробнее» |
| 10 | **проверяю новую ревизию** | прогресс 66 % + «скачано → sha256 → пробное распознавание» | «Отмена» |
| 11 | **новая ревизия не прошла проверку** | баннер `error`, старая ревизия остаётся рабочей | «Повторить» |
| 12 | **предупреждение по ОЗУ** | иконка `alert` + текст `warning-ink` — **предупреждение, не запрет** | «Скачать» (secondary, не primary) |
| 13 | **не рекомендуется для русского** | иконка `info` + пояснение | «Скачать» |
| 14 | **нет сети** | пояснение `fg-muted` | «Скачать» **disabled** · «Установить из файла…» |
| 15 | **офлайн-режим включён вами** | пояснение со ссылкой на настройку | «Скачать» disabled · «Из файла…» |
| 16 | **запрещено администратором** | иконка `lock` + «Задано администратором» | «Скачать» disabled |
| 17 | **ошибка: sha256 не совпал** | текст `danger-ink` + иконка `alert` | «Повторить» · «Удалить загрузку» |
| 18 | **ошибка: кончилось место на диске** | текст `danger-ink` + сколько не хватает | «Повторить» · «Открыть папку» |
| 19 | **модель повреждена** | текст `danger-ink` | «Переустановить» |
| 20 | **снята с каталога** | текст `fg-muted`, карточка в виде «недоступна» | «Удалить» |

Источник списка — `_p1.py:458-501` (20 ячеек галереи состояний).

## 5.5 Оформление по типу состояния

| Тип | Граница | Фон | Текст |
|---|---|---|---|
| активная / переключение | `accent` | `accent-bg` | обычный |
| рекомендованная | `primary` | `bg-surface` | обычный |
| обычная | `border` | `bg-surface` | обычный |
| **недоступная** (14–16, 20) | `border` | `bg-surface` | **`fg-disabled`, `opacity: 1`** — раунд 2, Р5 |
| ошибка (11, 17–19) | `border` | `bg-surface` | строка ошибки — `danger-ink` |

| Интерактивное состояние | Оформление |
|---|---|
| **hover** | фон `state.*.hover-on-surface` |
| **pressed** | фон `state.*.pressed-on-surface` |
| **focus** | кольцо вокруг карточки, радиус 8 |
| **loading** (3, 5, 10) | прогресс `6` высотой, радиус 3, заливка `primary`, отступ снизу 7 |
| **empty** (каталог пуст) | см. `02-models-empty-offline.png` — блок по центру колонки с иконкой, текстом и кнопкой «Установить из файла…» |

## 5.6 Каталог

Показывается **целиком, с прокруткой** — решение G2, никакого «показать ещё».
12 карточек в порядке `CATALOG_ORDER` (`_shell.py:313-314`), секции «Установленные» и
«Доступные» разделены CAPS-заголовком группы.

## 5.7 Resize

Карточка тянется по ширине колонки; метрики справа не сжимаются (`flex: none`),
левый блок ужимается и переносит текст. Вендор не переносится (`white-space: nowrap`,
`_shell.py:74`). При ширине колонки < 672 карточка рвётся — поэтому окно не уменьшается.

---

# 6. Панель обновления и строка внизу

**Референс:** `design/refs/04-update-panel.png` · `04-update-admin.png` · `04-footer-states.png`
**Источник:** `_shell.py:130-134,212-283` · `_p2.py:235+`.

## 6.1 Панель «Что нового»

Открывается кликом по `available` в строке-статусе.

| Параметр | Значение |
|---|---|
| Радиус / паддинг | 10 / `14 16` |
| Граница | 1 px `primary` — когда есть что ставить; 1 px `border` — нейтральные состояния |
| Фон | `bg-surface` |
| Список изменений | `ul`, отступ слева 17, текст 12.5 / 1.6, цвет `fg-secondary` |
| Заголовок | `font.role.h3-subsection` = 16 / 500 |

Панель показывает: номер версии, размер пакета, «Что нового» из тела релиза и действия
по треку (сам ставлю / скачать для администратора). У неё **14 состояний**
(`_p2.py`, раздел «Панель обновления — 14 состояний») — они зеркалят состояния 3–17
строки-статуса из §2.2 и оформляются теми же токенами.

## 6.2 Строка внизу

Полностью описана в §2. Здесь только связка: клик по акцентному состоянию открывает панель
§6.1; ошибки 12–14 открывают её же с раскрытыми подробностями.

---

# 7. Поле захвата комбинации — 7 состояний

**Референс:** `design/refs/01-general-hotkey.png` · `01-general-hotkey-dark.png`
**Источник:** `_p1.py:27-64` · `flows.md §3.4`.

| # | Состояние | Вид | Действия |
|---|---|---|---|
| 1 | `idle` | чип-клавиша «Ctrl + Space» + кнопка «Изменить» (sm), зазор 8 | — |
| 2 | `capturing` | поле в режиме захвата: граница `primary`, кольцо `0 0 0 2px primary-bg`; текст **моно 14** «Нажмите комбинацию…»; справа 12 px «Esc — отмена» | Esc |
| 3 | `captured` | то же поле, текст моно 14 «Alt + F2», справа «Отпустите клавиши» | — |
| 4 | `success` | баннер `note.o` (`success-bg` / `success-ink`), иконка `check` 15, отступ сверху 8 | — |
| 5 | `conflict` | баннер `note.w`, заголовок «Комбинация занята в KDE», объяснение, комбинация — моно | «Оставить Alt + F2» · «Выбрать другую» (primary) |
| 6 | `duplicate` | баннер `note.w`, «Эта комбинация уже назначена» | «Выбрать другую» (primary) · «Открыть „нажать-нажать“» (ghost) |
| 7 | `not-grabbed` | баннер `note.e` (`danger-bg` / `danger-ink`), «Горячая клавиша не захвачена» | «Выбрать другую» (primary) · «Повторить попытку» |

**Геометрия баннеров:** паддинг `10 12`, радиус 9, зазор 9, иконка 15 px,
заголовок — вес 700 с отступом снизу 2, кнопки — ряд с зазором 8 и отступом сверху 9.

**Важно по смыслу:** `conflict` — предупреждение, а не запрет: пользователь вправе оставить
комбинацию. `not-grabbed` — ошибка: диктовка не работает, пока комбинация не назначена.

---

# 8. Пилюля-оверлей — 12 состояний

**Референс:** `design/refs/09-pill.png` · `09-pill-dark.png`
**Источник:** `_base.py:156-164,344-390` · `_shell.py:327-339` · `flows.md:180-196`.

## 8.1 Окно

Пилюля — **отдельное окно ОС**, а не элемент внутри главного:
`Qt.Tool | Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint | Qt.WindowDoesNotAcceptFocus`.
**Не активируется, не появляется в Alt+Tab**, после показа делает повторную проверку
перекрытия (урок Astra Cowork, `flows.md:196`).

## 8.2 Геометрия

| Параметр | Значение |
|---|---|
| Высота | **36** |
| Радиус | **18** (= половина высоты) |
| Ширина | **резиновая под текст: min 172 → max 320**, ellipsis **запрещён** |
| Проверка | «Слушаю» в рендере = **187,6** — внутри диапазона (замер DOM) |
| Паддинг / зазор | `0 10` / 9 |
| Фон | `color.fixed.pill-bg` = `rgba(14,23,41,.94)` |
| Текст | `color.fixed.pill-fg` = `#F2F5FA`, 12.5; ключевое слово — вес 700 |
| Тень | `0 6px 18px rgba(0,0,0,.4)` |
| Кнопка «×» | 22 × 22, круг, фон `rgba(255,255,255,.12)`, знак `#C4CDDC` |

**Палитра пилюли не переключается с темой приложения** — она рисуется поверх чужих обоев.
Контраст текста на подложке: 16,4 : 1 на тёмных обоях, 14,4 : 1 на светлых (худший случай).

## 8.3 Индикатор уровня

| Параметр | Значение |
|---|---|
| Столбиков | **9** |
| Ширина столбика / зазор / радиус | **6** / **3** / 3 |
| Ширина блока | **78** = 9 × 6 + 8 × 3 (замер DOM) |
| Поле по высоте | **20** |
| Формула высоты | `h = max(3, round(level × 20))` |
| Плоские («молчит») | 4 px, цвет `#8C97AC` |
| Цвет живых | `#12B3A0` |
| Анимация | `linear`, по кадрам, без сглаживания |

## 8.4 Двенадцать состояний

| # | Состояние | Форма (не только цвет) | Текст | Длительность |
|---|---|---|---|---|
| 1 | `hidden` | окна нет | — | по умолчанию |
| 2 | `loading-model` | иконка `refresh` 16 px `#C4CDDC` | Загружаю модель… | до готовности, ≤ **5000 мс** |
| 3 | `listening` | 9 живых столбиков `#12B3A0` + «×» справа | **Слушаю** | пока зажат хоткей |
| 4 | `listening-silent` | плоские столбики + иконка `alert` 15 px `#F2B559` + «×» | Микрофон молчит | через **2000 мс** тишины |
| 5 | `limit` | столбики + иконка | Достигнут лимит записи | **2000 мс**, затем распознавание |
| 6 | `processing` | **3 точки шириной 5, высоты 5/9/5, `#E8A33A`, ПУЛЬСИРУЮТ** | Распознаю… | до результата |
| 7 | `done` | иконка `check` 16 px `#4FBF88` | **Готово** | **500 мс** |
| 8 | `clipboard-only` | иконка `file` 15 px `#C4CDDC` | Скопировано в буфер | **1200 мс** |
| 9 | `empty` | знак «—» 15 px `#8C97AC` | Ничего не распознано | **1000 мс** |
| 10 | `cancelled` | иконка `x` 15 px `#8C97AC` | Отменено | **800 мс** |
| 11 | `error` | иконка `alert` 16 px `#F0645F`, текст `#F0645F`, «›» справа | <короткая причина> | **3000 мс**, клик — подробности |
| 12 | `disabled` | окна нет вовсе (индикатор выключен в настройках) | — | трей остаётся обязателен |

**Появление** — не позже **100 мс** после нажатия. Нажатие короче порога — пилюля живёт
**150 мс** и уходит молча, без вставки и без ошибки.

**⚠ уточнить (У8):** для `limit` во `flows.md:184` указана «иконка часов», но глифа `clock`
в наборе `_base._IC` нет, и макет рисует только столбики. Нужен либо глиф, либо правка флоу.
**⚠ уточнить (У9):** `loading-model` описан как «круговой индикатор», но в макете это
статичная иконка `refresh`. Параметры вращения (период, направление) не заданы.
**⚠ уточнить (У10):** положение пилюли на экране — «снизу экрана», но отступ от края панели
и поведение на нескольких мониторах в источниках не заданы.

---

# 9. Трей и меню

**Референс:** `design/refs/09-tray.png` · `09-tray-dark.png`
**Источник:** `_base.py:171-180,237-249` · `brand-basics §4.3-4.6` · `flows.md:198-214`.

## 9.1 Иконка

Два **самостоятельных** рисунка: **22 px** (мастер-геометрия) и **16 px** (собственная
пиксельная сетка: точки 2 / 3 / 4 px, промежутки 2 и 1, строка 14 × 4).
Уменьшать 22-й файл нельзя — производные радиусы дают 1,9 px.

Геометрия 22: точки `r 1.2185 / 1.6815 / 2.3151` на `cy 8.4849`, строка `2,12 18×4 rx 2`.

| Состояние | Цвет **третьей точки** | Подсказка |
|---|---|---|
| `idle` | `currentColor` темы панели | Astra Voice — готов · Ctrl+Space |
| `listening` | `#12B3A0` | Слушаю… |
| `processing` | `#E8A33A` | Распознаю… |
| `done` (**800 мс**) | `#2FA36B` | Готово |
| `error` | `#D64545` | Микрофон недоступен — открыть |
| `hotkey-not-grabbed` | `#D64545` | Горячая клавиша не захвачена — выбрать другую |

**Правила, которые нельзя нарушать:** цвет несёт **только третья точка**; строка и точки 1–2
всегда `currentColor`; форма не меняется никогда; **трей-иконка статична** — пульсация
только у пилюли (решение В4). Контракт с Plasma:
`<style id="current-color-scheme">` + `class="ColorScheme-Text"` + `fill="currentColor"`.

**Принятый риск Р4:** на светлой панели KDE цвета состояний дают 1,8–3,7 : 1.
Смысл держится на форме знака и подсказке — решение принято осознанно (В6).

## 9.2 Меню трея

| Параметр | Значение |
|---|---|
| Ширина | **262** |
| Паддинг / радиус | 5 / 8 |
| Граница / тень | 1 px `border` / `0 12px 34px` |
| Пункт | паддинг `6 9`, радиус 6, зазор 9, текст 13 `fg` |
| Комбинация справа | **PT Mono 11.5**, цвет `fg-muted` |
| Заголовок-статус | 12, `fg-muted`, паддинги сверху и снизу по 3 |
| Разделитель | 1 px `border-soft`, поля `4 6` |
| **hover** | фон `state.*.hover-on-surface-2` |
| **выделенный** | фон `primary`, текст `primary-fg`, комбинация `rgba(255,255,255,.7)` |
| **недоступный** | `fg-disabled` |

**Состав и условия неактивности** (`flows.md:209-214`):
«Готов / Слушаю…» — всегда неактивен, это статус · «Отмена» — только во время записи или
распознавания · «Модель ▸» — галочка на активной, при одной модели пункт без подменю ·
«Скопировать последний текст» — неактивен, если текста нет · «Настройки…» `Ctrl+,` ·
«Проверить обновления» — **неактивен**, если проверки выключены или офлайн ·
«О программе» · «Выход» `Ctrl+Q`.

---

# 10. Онбординг — 5 шагов

**Референс:** `design/refs/08-onboarding-1-network.png` … `08-onboarding-5-done.png` (+ `-dark`)
**Источник:** `_shell.py:136-143` · `_p3.py:24-60`.

| Параметр | Значение |
|---|---|
| Окно | то же 900 × 620, заголовок «Astra Voice — первый запуск» |
| Шапка шага | паддинг `14 22 0`, текст «Шаг N из 5» — `font.role.caption` = 12 |
| Точка шага | 7 × 7, круг, фон `border` |
| Пройденный шаг | фон `fg-faint` |
| **Текущий шаг** | **20 × 7**, радиус 4, фон `primary` |
| Зазор между точками | 7 |
| Тело | паддинг `24 40 0`, содержимое по центру по горизонтали |
| Нижняя панель | высота **60**, паддинг `0 22`, зазор 10, граница сверху 1 px, фон `bg-app` |

**Шаги:** 1 «Сеть» → 2 «Модель» → 3 «Горячая клавиша» → 4 «Микрофон» → 5 «Готово».
Шаги 2, 3, 4 переиспользуют компоненты из §5, §7 и §4 — своей вёрстки у них нет,
меняется только обрамление.

**Состояния внутри шагов** (`_p3.py`): шаг «Модель» — выбор / скачивание / ошибка;
шаг «Микрофон» — уровень живой / тишина / устройство занято.

**⚠ уточнить (У11):** можно ли пропустить онбординг и что происходит при закрытии окна
на середине — в макете кнопки «Пропустить» нет, во флоу поведение не описано.

---

# 11. Диалоги и уведомления

**Референс:** `design/refs/10-dialogs.png` · `10-dialogs-dark.png` ·
`02-models-file-dialogs.png`
**Источник:** `_shell.py:111-129` · `_p3.py:17-18`.

## 11.1 Диалог подтверждения

| Параметр | Значение |
|---|---|
| Ширина | **420** |
| Радиус / граница | 9 / 1 px `border` |
| Фон | `bg-app` |
| Тень | `0 16px 44px` |
| Шапка | высота **30**, фон `bg-titlebar`, граница снизу 1 px, паддинг `0 10`, текст 12.5 `fg-secondary`, зазор 7 |
| Тело | паддинг `16 18 6`, зазор между иконкой и текстом **13** |
| Низ | паддинг `12 18 14`, зазор 8, кнопки прижаты вправо распоркой |

**Порядок кнопок:** слева распорка, затем «Отмена» (`secondary`), затем подтверждающая
(`primary`). Разрушающее действие текстом называет предмет: «Удалить GigaAM v3 CTC».

**Диалоги в продукте:** подтверждение удаления модели · удаление активной модели
(**недоступно**, кнопка disabled с пояснением) · «Установить из файла…» (выбор папки,
проверка прошла, контрольная сумма не совпала, модели нет в манифесте, не хватает файлов) ·
сброс настроек · сбор диагностики.

**⚠ уточнить (У12):** модальность (блокирует ли диалог главное окно) и позиционирование
(центр родителя или центр экрана) в источниках не заданы.

## 11.2 Уведомление KDE

Рисует система; наше — текст, иконка и кнопки действий. В макете воспроизведено, чтобы
показать длину строк.

| Параметр | Значение |
|---|---|
| Ширина | **340**, радиус 8, паддинг `11 12` |
| Фон / граница | `#1B2331` / `rgba(255,255,255,.12)` |
| Тень | `0 14px 36px rgba(0,0,0,.5)` |
| Шапка | 11.5, цвет `#8C97AC`, зазор 8, отступ снизу 6 |
| Заголовок | 13.5, блок, отступ снизу 3, цвет `#F2F5FA` |
| Тело | 12.5 |
| Кнопки | ряд с зазором 7, отступ сверху 10; паддинг `5 10`, радиус 6, фон `rgba(255,255,255,.10)` |
| Основная кнопка | фон `#6C93E8`, текст `#0B1220`, вес 500 |

Цвета здесь фиксированные (`color.fixed.notify-*`): уведомление живёт в оболочке KDE,
а не в нашей теме.

## 11.3 Подсказка (tooltip)

Фон `#0E1729`, текст `#F2F5FA` 12, паддинг `5 9`, радиус 6, тень `0 6px 18px rgba(0,0,0,.35)`.

---

# 12. Темы

**Референс:** любой экран в паре `…​.png` / `…-dark.png`.
**Источник:** `_base.py:13-34` · `_shell.py:44-50` · `decisions/log.md`.

## 12.1 Как переключается

Тема берётся из **`~/.config/kdeglobals`**, а не из палитры Qt: в процессе оверлея
Qt-палитра врёт (проверенный урок Astra Cowork). Один булев флаг `Theme.dark` разводит
все цветовые токены.

## 12.2 Что меняется и что нет

| Меняется с темой | Не меняется никогда |
|---|---|
| `color.light.*` ↔ `color.dark.*` | `color.fixed.*` — пилюля, уведомление KDE, подсказка, иконка приложения |
| `state.light.*` ↔ `state.dark.*` | вся геометрия: размеры, отступы, радиусы, шрифты |
| Цвет закрывающей кнопки заголовка | форма знака и правило «цвет несёт третья точка» |
| Направление ступени hover: вниз по светлоте в светлой, **вверх** в тёмной | длительности и кривые движения |

## 12.3 Отличия тёмной темы по существу

1. **Подложки состояний заданы прозрачностью** (`.12` / `.13`) поверх поверхности,
   а не отдельными hex — контраст считается по композиту.
2. **`fg-disabled` отдельный** (`#7E889B`), не равен `fg-muted` — иначе «выключено»
   и «подпись» неразличимы (К3).
3. **Ступень hover идёт вверх по светлоте** (`rgba(255,255,255,.06)` и `.10`):
   буквальный `bg-surface-2` в тёмной теме темнее фона окна и читался бы как «утоплено».
4. **Кольцо фокуса** — чистый `accent #2FD9C4` (10,6 : 1). В светлой теме взят текстовый
   вариант акцента — см. ⚠ У1.

## 12.4 Проверка

Каждый экран собран в двух вариантах; PNG лежат парами в `design/refs/`.
При правке любого цветового токена пересчитывать контраст **обеих** тем —
таблицы в `tokens.md §10`.

---

# 13. Соответствие экранов и референсов

| Экран | Разделы спеки | Референс |
|---|---|---|
| 01 Общие (база, хоткей, индикатор, микрофон) | §1, §3, §4, §7 | `01-general-*.png` |
| 02 Модели (каталог, состояния, пусто/офлайн, диалоги файла) | §5, §11 | `02-models-*.png` |
| 03 Вывод | §3, §4 | `03-output*.png` |
| 04 Сеть и обновления, панель, строка внизу | §2, §6 | `04-*.png` |
| 05 Продвинутые | §3, §4 | `05-advanced*.png` |
| 06 О программе | §3 | `06-about*.png` |
| 07 Отладка | §1, §3 | `07-debug*.png` |
| 08 Онбординг (5 шагов) | §10 | `08-onboarding-*.png` |
| 09 Пилюля и трей | §8, §9 | `09-pill*.png`, `09-tray*.png` |
| 10 Диалоги и системные окна | §11 | `10-dialogs*.png` |

---

# 14. Места «⚠ уточнить»

Всё, где в источниках значения нет и я его не придумывал.

| # | Раздел | Что уточнить |
|---|---|---|
| **У1** | §12.3, `state.light.focus-ring` | Решено «кольцо **accent**», но `#12B3A0` в светлой теме даёт **2,2–2,6 : 1** — как индикатор фокуса не виден и не проходит порог 3 : 1. Поставил `accent-ink #0A776A` (5,16 : 1). Буквальное значение сохранено как `focus-ring-literal`. **Нужно подтверждение.** |
| **У2** | §4.1, `state.dark.pressed-on-surface` | Подпись `fg-muted` на тёмном pressed даёт **4,24 : 1** — чуть ниже порога. Состояние держится доли секунды. Оставить или уменьшить ступень до 7 %? |
| **У3** | §1.4 | Политика полосы прокрутки в QML (`AsNeeded` / всегда видимая), поведение при прокрутке колесом. |
| **У4** | §1.5 | Поведение при **увеличении** окна: тянуть колонку контента или держать 672 и центрировать. |
| **У5** | §3.2 | Кликабельна ли строка настройки целиком или только контрол внутри. |
| **У6** | §4.1 | Состояние `loading` у кнопки: в макете его нет, прогресс всегда рядом с кнопкой. |
| **У7** | §4.4 | Раскрытие списка вверх при нехватке места снизу; максимальная высота списка с прокруткой. |
| **У8** | §8.4 | `limit`: во флоу «иконка часов», глифа `clock` в наборе нет, макет рисует только столбики. |
| **У9** | §8.4 | `loading-model`: «круговой индикатор» во флоу, статичная иконка `refresh` в макете. Период и направление вращения не заданы. |
| **У10** | §8.1 | Положение пилюли: отступ от нижнего края и от панели KDE; поведение на нескольких мониторах. |
| **У11** | §10 | Можно ли пропустить онбординг; что происходит при закрытии окна на середине. |
| **У12** | §11.1 | Модальность диалогов и позиционирование (центр родителя или экрана). |
| **У13** | §1.2 | Порядок обхода по Tab между сайдбаром, контентом и строкой-статусом — в источниках не задан, а PRD §8 требует полной доступности с клавиатуры. |
| **У14** | §3.2 | Звук начала и конца записи: тумблер есть, сам звук (файл, длительность, громкость) нигде не определён. |

---

# 15. Что нужно, чтобы принять спеку (G3)

1. Ответы по **У1** и **У2** — они меняют цветовые токены.
2. Ответы по **У4**, **У5**, **У13** — они меняют раскладку и доступность.
3. Остальные ⚠ можно закрыть по ходу разработки, зафиксировав решение в `decisions/log.md`.
4. После принятия — генерировать `Theme.qml` **скриптом из `tokens.json`**
   (маппинг токенов на QtQuick Controls 2 — `tokens.md §9`), а не переписывать значения руками.
