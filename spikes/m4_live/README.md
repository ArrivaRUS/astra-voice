# M4 — живой прогон Ц2 у заказчика (KDE, 2026-09-14, 0.1.0~m4.3)

- Стенд: машина заказчика, KDE, Handy остановлен, `virtual_mic.sh up` (вход по умолчанию → `av_test_src`), Kate (новый документ,
  фокус через xdotool), `dictate50.sh --session kde --yes --wav data/test/test-ru-6s.wav`, `xdotool keydown --delay 100 ctrl+space`.
- Попытка 1 (19:28) прервана на диктовке 1: `xdotool keydown ctrl+space` без задержки не срабатывает (12 мс между Control и
  пробелом), `virtual_mic.sh up` не переключал вход — оба дефекта e2e закрыты в скриптах (`--delay 100`, `set-default-source`).
- Попытка 2 (19:36–20:01): **50 из 50 распознаны, пусто 0, отменено 0, ошибок микрофона 0.**
  `t_ms` («отпустил → текст», включая вставку ~160 мс): min 251 · p50 269 · p90 522 · **p95 523** · max 777; `paste_ms` p50 164.
  **Вердикт скрипта: не уложились (порог 500).**
- Распределение бимодальное: 34 диктовки в 251–270 мс, 16 — ровно на ~250 мс дольше (502–523), две — на ~500 (768, 777);
  между 270 и 500 — ни одной. Это квантование по тику ~250 мс (в воркере `POLL_INTERVAL = 0.25`), а не шум машины.
  Разбор → debugger (следующая сессия), после фикса — повторный прогон (e2e теперь работает сквозняком, ~25 мин).
- Файлы: `dictate50-kde-2026-09-14.log` (полный вывод), `events-2026-09-14.json` (50 событий статистики: `t_ms`,
  `paste_ms`, `audio_ms`, `result`, `cold`; текста фраз в статистике нет по конструкции), `…attempt1-aborted.log`.
- Машина после прогона: виртуальный микрофон выгружен, вход по умолчанию — встроенный, Handy остановлен (вернуть:
  `systemctl --user start app-Handy@autostart.service`), окно Kate с 50 строками открыто у заказчика (не сохранять).
