# Связка релизных ключей

`release.gpg` — связка ключей, по которой `astra_voice.security.verify` проверяет
подпись `SHA256SUMS.asc` у релизов и `.deb` «из файла». Файл попадает в пакет как
`/usr/share/astra-voice/data/keys/release.gpg`, root-owned, и отпечатки в коде
(`PINNED_FINGERPRINTS`) едут в том же пакете — подменить одно без другого нельзя
(`docs/threat-model.md` §4.2 «б»).

## Текущее содержимое

uid: `Astra Voice Release <alekovalev@astralinux.ru>`.

| Ключ | Отпечаток | Статус |
|---|---|---|
| Мастер M1 | `5F1F 7718 559F 8F57 FFE1  1780 55BB 1162 F17A 5CB0` | `cert`, без срока; создан 26.09.2026 на рабочей машине заказчика, закрытая часть затем перенесена на носитель; риск принят для v0.1, преемник M2 офлайн до v1.0; отпечаток пинится в коде |
| `S1` | `7602 A029 F0E3 4CD2 344A  9CDA 657E D046 89FF 4D79` | `sign`, до 26.09.2027; рабочий, закрытая часть в GitHub Environment `release` |
| `S2` | `9053 EE0C 0085 683C E6C7  042E DC2E 5932 25D8 0428` | `sign`, до 26.09.2027; резерв, закрытая часть офлайн |

Пин по отпечатку **мастера** позволяет плановую ротацию `S1 → S2` без правки кода.

## Как менялось

До 26.09.2026 здесь лежал тестовый ключ спайка S2. Он заменён на публичную связку
мастера и подключей одним коммитом по порядку ниже.

Порядок замены мастера (одним коммитом, иначе гейт `verify` разъедется):

1. заказчик генерирует новый мастер и подключи по `docs/SECURITY.md` §4;
2. `gpg --export <master-fpr> > data/keys/release.gpg`;
3. в `src/astra_voice/security/verify.py` заменить `PINNED_FINGERPRINTS` на отпечаток
   нового мастера;
4. отпечаток нового мастера — в `README.md` и `docs/INSTALL-ADMIN.md` (раздел 2).
