Ты архитектор. Проект Astra Voice — утилита голосового ввода для Astra Linux SE 1.8.5 (X11, KDE Plasma 5.27/Fly, glibc 2.36, Python 3.11, PyQt5 5.15.9 + QtQuick Controls 2 из apt; CPU Core Ultra 7 255U, без GPU).

Прочитай файлы в /home/astra/Документы/astra-voice/ (ты в этом каталоге):
- PRD.md (версия 0.3: требования F1–F16, S1–S16, §7 каталог моделей и манифест catalog.json с полями engine/layout, §8, §9, §11)
- decisions/log.md (рамки, НЕ пересматривать)
- design/spec.md и design/tokens.md §9 (Theme.qml генерируется из design/tokens.json)
- design/flows.md
- design/mockups/final/index.html (список кастомных QML-компонентов)
- research/summary.md
- research/tech-platform-claude.md
- research/tech-models-claude.md §A4–A5 и §B–C
- research/catalog-numbers.md
- research/legal.md §3–4

Принятые рамки из decisions/log.md (не пересматривай, строй архитектуру внутри них):
Python + PyQt5 из apt под GPL-3; пилюля-оверлей по умолчанию; сетевые проверки выключены по умолчанию; модель держится в памяти постоянно; Ctrl+Space push-to-talk + toggle; автозапуск XDG; два трека обновлений — без ЗПС: GitHub Releases → .deb → один polkit-шаг → apt-get install ./deb, с ЗПС/без прав: «скачать пакет для администратора»; ГОСТ-подпись в v1.1; каталог 12 моделей с профилем «только отечественные»; замер ОЗУ/RTFx на реальной машине.

Факты среды из соседнего проекта Handy (/home/astra/Документы/handy-gigaam/docs/TROUBLESHOOTING.md, прочитай если доступен):
- onnxruntime-колёса требуют шим __isoc23_* на glibc 2.36;
- гонка WirePlumber после загрузки даёт симптом «микрофон молчит»;
- тест STT без микрофона — виртуальный источник PipeWire + paplay.

Верни план (до 500 строк, максимум таблиц, минимум воды):

1) КОМПОНЕНТЫ — таблица: компонент · ответственность · зависит от · как тестировать. Обязательно покрой:
   - процессная модель (GUI-процесс vs воркер распознавания, IPC, крах воркера не роняет GUI);
   - глобальный хоткей X11 (удержание/toggle, конфликты с KDE);
   - захват звука (VAD, лимит длительности, детект тишины + перезапуск WirePlumber);
   - ВЫБОР РАНТАЙМА v1: onnx-asr vs sherpa-onnx — обоснуй по критериям: GigaAM v3 e2e_rnnt + все 12 моделей каталога, python-колёса manylinux под glibc 2.36, число собственных .so для будущей ЗПС-подписи, ОЗУ/скорость, раскладки репозиториев из research/catalog-numbers.md;
   - вставка текста (буфер обмена + Ctrl+V, восстановление буфера, терминалы, Klipper);
   - пилюля (окно без кражи фокуса поверх всех окон на X11/KWin, рабочая область монитора);
   - трей SNI + QtQuick;
   - окно настроек;
   - загрузчик моделей (Range/докачка, sha256, атомарная замена, ревизии, источники HF → GitHub Releases → корпоративный URL, кеш/backoff, лимиты);
   - проверка ревизий модели через HF API;
   - обновлятор утилиты (Releases API → .deb → проверка подписи minisign/GPG и sha256 → одно polkit-действие + root-helper → apt-get install --no-remove → перезапуск; трек для администратора; детект DIGSIG_ELF_MODE);
   - автозапуск; settings.json + policy.conf; тема из kdeglobals; i18n; логи/статистика.

2) УПАКОВКА: один .deb (зависимости apt + вендоренные колёса — какие именно, сколько .so), сборка одной командой, SBOM, релиз с подписью, структура репозитория, CI GitHub Actions (Debian 12 как прокси среды; что проверяемо ТОЛЬКО на реальной машине).

3) MILESTONES по зависимостям под релизы v0.1 (30.09) / v0.2 (15.10) / v1.0 (31.10): done-критерии, команды валидации, спайки в начале (пилюля без кражи фокуса, polkit из GUI, onnxruntime на glibc 2.36, замер ОЗУ).

4) РИСКИ с альтернативами и обратимостью; уровни тестов (unit / integration с виртуальным микрофоном / e2e на реальной машине); инварианты безопасности.

Файлы не меняй — только читай и отвечай текстом.
