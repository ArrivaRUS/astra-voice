# S2 — спайк polkit-помощника (обновлятор, трек A)

> Фаза 4, M0.S2 (`docs/plans.md` § M0). Исполнитель: developer. Дата: **2026-09-09**.
> Машина заказчика: ALSE 1.8.5, KDE Plasma 5.27 / X11, агент авторизации
> `polkit-kde-authentication-agent-1` (pid 3737).
> Установку трёх системных файлов заказчик выполнил сам одной командой (`install-spike.sh`,
> один `sudo`); пароль в диалоге polkit вводил **заказчик** — исполнитель пароль не вводил,
> не читал и не сохранял. Подробности и дословные логи — `spikes/s2_polkit_helper/README.md`
> § «Живой прогон (этап 2)», артефакты — `spikes/s2_polkit_helper/out/live_s2*`.

## 0. Сводка вердиктов

| # | Критерий `docs/plans.md` § M0.S2 | Факт | Сессия | Вердикт |
|---|---|---|---|---|
| 1 | Тестовый пакет 0.0.1 → 0.0.2, помощник-скелет (`python3 -IS`, stdlib, `os.environ.clear()`, argv `install <абс. путь>`, staging 0700), `.policy` `auth_admin` без `_keep` | пакеты собраны и подписаны (`packages/`, `SHA256SUMS.asc`); помощник 23 510 Б установлен по `/usr/libexec/astra-voice/update-helper` (0755 root:root); `.policy` 1 197 Б; keyring 754 Б | KDE (живой) | ✅ **зелёный** |
| 2 | Заказчик вводит `sudo` **один раз** по подготовленной команде; команда отката записана рядом | выполнено заказчиком (`install-spike.sh`), откат — `uninstall-spike.sh` (не запускался) | KDE (живой) | ✅ **зелёный** |
| 3 | Из `QProcess` GUI: `pkexec … install <deb>` → **ровно одно** окно пароля | опрос окон класса `polkit` раз в секунду весь прогон: одновременно **максимум 1**, уникальных за прогон **1** (id 37748748) | KDE (живой) | ✅ **зелёный** |
| 4 | Коды 0 / 126 / 127 различимы | **0** подтверждён живьём (`exit=0`, `result=ok`); **126/127** живьём не вызывались — каждый потребовал бы ещё одного диалога у заказчика; разобраны в `gui_probe.py` и покрыты юнит-тестами | KDE (живой) / код | ⚠ **зелёный по 0**, 126/127 — не проверено живьём |
| 5 | `dpkg-query -W` = 0.0.2 | `astra-voice-spike 0.0.2` (было 0.0.1) | KDE (живой) | ✅ **зелёный** |
| 6 | GUI не блокируется | `QProcess` асинхронный, тикер «GUI жив: N» крутился, кнопка снова активна после `finished` | KDE (живой) | ✅ **зелёный** (инструментального снимка нет) |
| 7 | `apt-get install -y ./x.deb` **без** `--allow-unauthenticated` (T1 §5) | в установленном помощнике `run_apt` строит фиксированный argv `[apt-get, install, -y, --no-remove, ./<staged>]`; установка прошла | KDE (живой) + чтение кода | ✅ **зелёный** |
| 8 | Удержанный `lock-frontend` → `apt-locked` (код 72), apt не убит (T-18) | сымитировать без root нельзя; есть юнит-тест (`tests/test_helper.py`) | — | ⛔ **не проверено живьём** |
| 9 | `-IS`: stdlib грузится без `site` | помощник отработал под `pkexec` с очищенным окружением | KDE (живой) | ✅ **зелёный** |
| 10 | `--no-download` при offline | ветка включается по `/etc/astra-voice/policy.conf`; файла нет → `policy-absent`, offline=false | — | ⛔ **не проверено живьём** |
| 11 | Провал → `pkcon install-local` / трек B | основной путь прошёл | — | ✅ **не активируется** |

**Итог: принят.** Ключевой критерий «один диалог пароля из GUI и установка локального
подписанного пакета» закрыт фактом на машине заказчика; три пункта (126/127, `apt-locked`,
`--no-download`) остаются за юнит-тестами и требуют либо отдельного прогона, либо согласия
заказчика на второй диалог.

---

## 1. Что именно произошло

```
15:52:19  нажата кнопка зонда (QProcess → pkexec /usr/libexec/astra-voice/update-helper install …)
          появилось ОДНО окно polkit-kde-authentication-agent-1
15:52:24  помощник вернул JSON, exit=0        # 5 с на всё, включая ввод пароля заказчиком
```

```json
{"result": "ok", "exit": 0, "message": "установлено", "policy": "policy-absent",
 "fingerprint": "CB951AD794407972A0B1A5BBAFA87398C4953A71",
 "sha256": "738d6f2c1b5522a3f5edf92d39c9f7c704d287beb0a5cfe1a3d553edf89743af",
 "package": "astra-voice-spike", "version": "0.0.2", "installed": "0.0.1"}
```

Проверка подписи прошла по своему keyring (`/usr/share/astra-voice/keys/test-release.gpg`)
с пином отпечатка `CB951AD7…3A71`; sha256 сверен с `SHA256SUMS`; версия проверена на
неоткат (`dpkg --compare-versions 0.0.2 gt 0.0.1`).

Журнал помощника `/var/lib/astra-voice/updates.log` (0644 root) содержит одну строку с тем же
результатом — годится как источник для «истории обновлений» в GUI (M8).

## 2. Находки

| # | Находка | Что делать |
|---|---|---|
| S2-R1 | JSON помощника виден только в окне зонда; в артефакты не попадал | в прогоне добавлен дубль stdout в консоль (`gui_probe.py`); в M8 — писать ответ помощника в лог приложения |
| S2-R2 | `126`/`127` требуют отдельных диалогов у заказчика (отмена/несуществующее действие) | закрыть на стенде/ВМ, а не в рабочей сессии заказчика |
| S2-R3 | `apt-locked` и `--no-download` без root не воспроизводятся | оставить за юнит-тестами; в M8 сделать e2e на ВМ |
| S2-R4 | **`pytest` не установлен** ни в системе, ни в scratch-venv — 35 юнит-тестов спайка сейчас прогнать нечем | вопрос человеку: поставить `python3-pytest` (apt) или завести отдельный venv для тестов |
| S2-R5 | `staging` (0700 root) непривилегированному пользователю не читается — проверить, что после сбоя там не копятся `.deb`, можно только с root | добавить в помощник самоочистку staging и её проверку в M8 |

## 3. Состояние системы

Изменено (заказчиком, по подготовленной команде; откат — `spikes/s2_polkit_helper/uninstall-spike.sh`,
**не запускался**):

```
/usr/libexec/astra-voice/update-helper
/usr/share/polkit-1/actions/io.github.arrivarus.astra_voice.update.policy
/usr/share/astra-voice/keys/test-release.gpg
/var/lib/astra-voice/{staging,update.lock,updates.log}
пакет astra-voice-spike: 0.0.1 → 0.0.2   (тестовый, ставился намеренно)
```

Ничего из перечисленного не удалялось. Зонд закрыт, процессов не осталось.
