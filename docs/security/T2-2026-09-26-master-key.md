# Astra Voice — ИБ-ревью T2: смена якоря доверия подписи + релизный конвейер (2026-09-26)

> security-analyst (Fable 5.1) · триггер **T2** по `security-gate` (крипто, секреты, CI/деплой с `contents: write`, разбор входа) ·
> ветка `wip/master-key` — worktree `~/.cache/astra-voice-dev/wt-keys`, коммит `78b6809` над `3f46e37` (main сейчас `f24b1e1`,
> разница — только журнал) · ветка `wip/r1-release` — worktree `~/.cache/astra-voice-dev/wt-release`, коммиты `5917063`+`699fb96`
> над `f24b1e1` · метод: `git diff main...HEAD`, чтение кода и доков, `gpg --show-keys`/`--list-packets` по связке, `gpgv` по
> встроенному каталогу, **практический эксперимент** с gpg 2.2.40 во временном `GNUPGHOME` (`--faked-system-time`), read-only
> запросы `gh api` к репозиторию, `tools/secrets-check.sh` и поиск по истории git. Код и git не трогал. Папку с закрытыми ключами
> не открывал (на момент проверки, ~20:35 MSK, её по указанному пути уже не было); файл истории буфера обмена не открывал —
> только `stat`. Юнит-тесты не запускал (в системном python нет pytest; их гоняет CI). Второе мнение GPT-6 Astra (xhigh,
> read-only) — §7; Astra дополнительно проверила связку на привитый подключ (см. там).

## 0. Вердикты

| Ветка | Вердикт | Условия |
|---|---|---|
| `wip/master-key` `78b6809` | **PASS с условиями** | Сам диф верный: связка — только публичные пакеты, мастер `c`-only без срока, S1/S2 `s` до 2027-09-26, пин = мастер, каталог переподписан S1 и даёт `VALIDSIG … 5F1F…5CB0`, старый отпечаток вычищен, `check_keyring.py` берёт пины из кода. Условия: **до мержа** — правки формулировок в `SECURITY.md`/`README.md` (K-6: честно про происхождение ключа, строка `trust.json`, срок S2/календарь); **сегодня** — заказчик выполняет §5 (гигиена); **до тега v0.1** — решение заказчика по K-1: перегенерировать мастер офлайн (рекомендую; Astra — настойчиво) или принять риск письменно в `decisions/log.md` с преемником M2 до v1.0. Если перегенерируют — ветка переделывается с новыми отпечатками, перепроверка Юрки по §10. |
| `wip/r1-release` `699fb96` | **PASS с условиями** | Конвейер устроен правильно (порядок шагов, белый список связки до секрета, `contents: write` только в `release`, `tools/validate release` через `Verifier` с одним снимком). Условия: **R-1** (пин подписанта `-u "<S1>!"` + проверка, что импортирован ровно S1) — маленькая правка до мержа; **R-6** — после мержа `master-key` снять из README/INSTALL-ADMIN фразы про тестовый ключ (будет конфликт в «Открытых вопросах»); **R-2** — настройки GitHub (§4 Q4) до тега, их проверит T3. R-3/R-4 — не блокируют, план v1.0. |

Blocker — 0. P1 — 3 (K-1 процедура ключей; K-2 дизайн `trust.json` для M8; K-3 барьер GitHub обходится с этой машины).
P2 — 8. Minor — 4. Ни одна находка не открывает путь через границу доверия **в коде** этого дифа; все P1 — про процедуру,
окружение и будущий дизайн.

## 1. Границы доверия и поверхность

| Актор | Что может | Через что входит | Чему доверяем |
|---|---|---|---|
| Заказчик — владелец мастера, **рабочая онлайн-машина** | сгенерировал мастер, S1, S2, `.rev`; подписал каталог; загрузил S1 в GitHub | `GNUPGHOME` на рабочем столе, буфер обмена, носитель | заказчику — да; **машине — нет**: онлайн, домашний раздел без шифрования, индексатор, Klipper, ИИ-агенты с shell под тем же uid (B13/B14) |
| GitHub Environment `release` (B11) | хранит S1, подписывает `SHA256SUMS` по тегу `v*` | тег, approve владельца | окружению — да, при условии, что approve делает человек в браузере, а не токен на машине |
| gh-токен владельца на этой машине + ИИ-агенты (B13) | push в `main` (не защищён), push тега, approve deployment и правка Environment через API | shell агентов | **не** доверяем как reviewer — сейчас ничто это не разделяет (K-3) |
| Клиент: `verify.py` + `gpgv` + `release.gpg` из пакета (B1/B14) | принимает только `VALIDSIG` с пином; код возврата ≠ 0 → отказ | `catalog.json.sig`, `SHA256SUMS.asc` | пину мастера; срок и отзыв — не контроль (подтверждено, §2) |
| Администратор bootstrap (У26) | сверяет отпечаток и `gpgv` руками | README / INSTALL-ADMIN / Release — **один и тот же GitHub** | нужен второй канал и проверка подписанта (K-… → У95) |

## 2. Что проверено практически (gpg/gpgv 2.2.40, временный `GNUPGHOME` в scratchpad, свои тестовые ключи)

| # | Сценарий | Результат | Следствие для продукта |
|---|---|---|---|
| A | Связка `data/keys/release.gpg` (847 байт, sha256 `6847a5b4…b7f7`) | `pub … cSC` мастер `5F1F…5CB0` без срока, флаги `01` (cert); `sub … s` S1 `7602…4D79` и S2 `9053…0428`, флаги `02`, срок 1 год; uid один; **секретных пакетов нет** | связка публичная и совпадает с описанием |
| B | `gpgv --status-fd 1 --keyring release.gpg catalog.json.sig catalog.json` | `GOODSIG` + `VALIDSIG 7602…4D79 … 5F1F…5CB0`, exit 0; подпись 26.09 20:13:04 MSK ключом S1 | встроенный каталог принимается `verify.py:184-205` |
| C | Подключ истёк (создан 2025-01-01, срок 1 г.), подпись сделана **до** истечения; проверка сегодня | `EXPKEYSIG` + **`VALIDSIG`**, **exit 0**; `gpg --verify` у админа — «Good signature … Note: This key has expired!», exit 0 | **Каталог после 2027-09-26 не «протухнет»**: `verify.py` смотрит код возврата (0) и `VALIDSIG` (есть) — принимает. Срок подключа на клиенте — не контроль (как и записано в §4.2) |
| D | То же, но подпись сделана **после** истечения (по старой связке) | тот же `EXPKEYSIG` + `VALIDSIG`, exit 0 | gpgv не различает «до/после»; истёкший, но украденный подключ подписывает и дальше — отзыв только списком `REVOKED_FINGERPRINTS` |
| E | Подписать истёкшим подключом (`gpg -u S1!`) | `Unusable secret key`, exit 2 | **CI встанет 2027-09-26**. S1 и S2 истекают в один день (K-4) |
| F | Подписать данные **cert-only мастером** (`gpg -u MASTER!`) | `Unusable secret key`, exit 2 | План M8 «`trust.json` подписан офлайн-мастером» нереализуем как записано (K-2) |
| G | `gpg --quick-set-expire MASTER 2y S1` → реэкспорт связки | старая подпись → `GOODSIG` по новой связке; подпись снова работает | продление срока мастером — штатный путь, отпечатки не меняются |
| H | Ключ создан «в будущем» (клиент с отставшими часами) | `ERRSIG … Bad public key`, **exit 2**, `VALIDSIG` нет; с `--ignore-time-conflict` — `GOODSIG` | Машина с часами раньше 2026-09-26 17:09:54 UTC отвергает **всё**, включая встроенный каталог; причина теряется (K-5) |
| I | Подпись с меткой времени в будущем при валидном ключе | `GOODSIG`, exit 0 | не проблема |
| J | Импорт `--export-secret-subkeys 'S1!'` в чистый дом, `gpg --detach-sign` без `-u` | подписал S1; `sec` — заглушка `#`, одна `ssb` | как в CI; но подписант не закреплён (R-1) |
| K | Подключ отозван в связке (revkey) | `REVKEYSIG` + **`VALIDSIG`**, exit 0 | сертификат отзыва клиенту ничего не даёт — подтверждает §4.2 и T-05 |
| L | `git merge-base --is-ancestor <sha объекта аннотированного тега> main` | exit 0 на main, 1 вне main | шаг «Тег стоит на коммите из main» (`ci.yml:306`) работает и с аннотированными тегами |
| M | `tools/secrets-check.sh` по `wt-keys`; `git log --all -S 'BEGIN PGP PRIVATE'`; поиск `.rev/.asc/private-keys` в дереве | чисто; в истории только публичные связки и `.asc`-подписи спайка | секретов в репо нет |
| N | Astra (по моему запросу): удалить binding-подпись S1 / испортить её / убрать back-signature в копии связки | каждый вариант → `ERRSIG`, exit 2, без `VALIDSIG` | gpgv 2.2 проверяет привязку подключей: «привить чужой подключ под мастер» без закрытого мастера не выйдет |

Хронология 26.09 (MSK): мастер создан 20:09:54 → каталог подписан 20:13:04 → секрет `GPG_SIGNING_KEY` в Environment
20:24:09 → коммит решения 20:24:21 → **файл истории Klipper записан 20:25:41** → коммит `78b6809` 20:25:50.

## 3. Находки

### 3.1 `wip/master-key` (K-серия)

**K-1 · P1 · Ключи родились на онлайн-машине без шифрования; копии нельзя исключить.**
Вектор: кто угодно с доступом к этой машине или к её диску (вредонос, форензика украденного ноутбука, процесс того же uid —
включая ИИ-агентов с shell, которые тут работают) мог/может получить **мастер, S1 и S2 разом**. Резерв S2 тогда не резерв.
Свидетельства (без чтения содержимого): `~/.local/share/klipper/history2.lst` — режим **0644**, 6824 байт, записан
20:25:41, через 92 с после сохранения секрета в GitHub (вероятно, `s1-secret.asc` вставляли через буфер; Klipper хранит
историю на диске — B14 в модели это уже знает); индексатор `baloo_file` работает (индексирует `~/`, включая рабочий стол);
`/home` — ext4 на LVM на NVMe **без LUKS** (`shred` на SSD не гарантирует стирания, `lsblk` без `crypt`); корзина не пуста;
`~/.gnupg` не проверял (по указанию) — заказчик должен сам убедиться, что ключей там нет. Клиентов бэкапа/синхронизации на
машине не видно (только Baloo). Факт утечки **не установлен** — установлено, что его нельзя опровергнуть.
Что делать: (1) сегодня — §5 п. 1–6; (2) решение заказчика до тега v0.1: **перегенерировать офлайн** по карте §5.2
(30–40 мин; сейчас смена якоря стоит один коммит, после первого релиза — ручная переустановка парком) — рекомендую я и
Astra; запасной путь — принять риск письменно в `decisions/log.md` для пилота v0.1, до v1.0 (31.10) сгенерировать
преемника M2 офлайн, вшить в связку и `PINNED_FINGERPRINTS` (это множество), после — снять пин M1. Astra верно замечает:
M2 «потом» не снимает полномочий уже утёкшего M1 до релиза без него — поэтому это именно принятие риска, не лечение.
Тест: процедурный — заказчик выполняет `gpg --list-secret-keys` (нет `5F1F…`), проверка §5 п. 6.

**K-2 · P1 (дизайн M8; мерж не блокирует) · `trust.json` от cert-only мастера невозможен, а под общим пином его подпишет S1.**
`docs/SECURITY.md:18` и `docs/threat-model.md:197-199`, `docs/plans.md:469` требуют подпись `trust.json` офлайн-мастером;
эксперимент F: gpg отказывает (`Unusable secret key`), мастер с флагом только `C` подписывать данные не умеет. Хуже (Astra):
если проверять `trust.json` тем же `Verifier("release")` с пином мастера (`verify.py:205` принимает по первичному
отпечатку), то **S1 из CI** подпишет `trust.json` с любыми `allowed/revoked/trust_epoch` — отзыв S1 станет подписываемым
самим S1. Фикс: отдельный офлайн подключ **T** (`gpg --quick-add-key MASTER ed25519 sign never` или 5y), закрытая часть —
рядом с мастером; проверка `Verifier("trust", keyring, pinned={T_fpr})` — точный пин подключа, без первичного (логика
`fpr ∈ pinned OR primary ∈ pinned` при множестве из одного T это даёт). Лучше сгенерировать T **сейчас**, в ту же церемонию,
и вшить в связку v0.1 — меньше смен связки потом. Тест: T-118.

**K-3 · P1 (окружение, вне дифа) · Required reviewer обходится с этой же машины без человека.**
`gh auth status`: токен владельца `ArrivaRUS` со scope `gist, read:org, repo, workflow` хранится в keyring этой машины;
владелец — единственный collaborator/admin и единственный reviewer; `main` без защиты; rulesets — нет;
`deployment_branch_policy` — `null`; `can_admins_bypass: true`; Actions: `allowed_actions: all`, `sha_pinning_required: false`.
Вектор: агент с shell (или prompt-injection в него) под этим uid: правит `.github/workflows` (scope `workflow`), пушит в `main`,
пушит тег `v*`, **одобряет deployment** (`POST …/actions/runs/{id}/pending_deployments`, scope `repo` достаточно) и даже
меняет правила Environment (`PUT …/environments/release`). Проверка `merge-base --is-ancestor` (`ci.yml:306`) вредоносный
коммит, уже попавший в `main`, пропустит. Фикс — §4 Q4: агентам — fine-grained PAT без `Workflows/Administration/
Deployments/Environments/Secrets`; approve — только в браузере с 2FA на ключе/passkey; Environment — только теги `v*`;
ruleset на теги `v*`; SHA pinning и «только GitHub-actions» на уровне репо; `--push` в `release.sh` запускает только человек.
Тест: T-120 (deployment policy) + Юрка перепроверяет `gh api` (§10).

**K-4 · P2 · S1 и S2 истекают в один день (2027-09-26).** `release.gpg` (A), `SECURITY.md:65-66` (`1y` обоим), календарь
`SECURITY.md:111`. По E резерв не даёт запаса времени: 2027-09-26 CI не сможет подписать ни S1, ни S2; релиз с S3 в связке
должен выйти **до** этой даты. Фикс: в церемонию — `gpg --quick-set-expire 5F1F7718559F8F57FFE1178055BB1162F17A5CB0 2y 9053EE0C0085683CE6C7042EDC2E593225D80428`
и реэкспорт связки (отпечатки не меняются, G); при перегенерации — S2 сразу `2y`; в §3/§6: «при переводе резерва в рабочие
продлить его мастером ещё на год». Тест: T-119 (предупреждение `check_keyring`, если срок S2 ≤ срока S1).

**K-5 · P2 · Отставшие часы клиента = отказ всего с невнятной причиной.** H: часы раньше 2026-09-26 17:09:54 UTC →
`ERRSIG`, exit 2 → `verify.py:184` «gpgv отверг подпись (код 2)» → `catalog.py:452` `bad-signature` «Не удалось подтвердить
подпись каталога» — и встроенный каталог, и все загрузки. Парк без NTP/с севшей батарейкой CMOS — реальный случай.
Фикс (Developer): константа `KEY_CREATED_TS = 1790442594` рядом с `PINNED_FINGERPRINTS`; при `returncode != 0` и
`time.time() < KEY_CREATED_TS` (или в stderr `in the future`/`time warp`) — `reason="Часы компьютера отстают: дата раньше
26.09.2026"`; `load_builtin` пробрасывает `reason` в UI. `--ignore-time-conflict` **не** добавлять (H: с ним принимается всё).
Astra: `ERRSIG/Bad public key` не уникален для часов (испорченная binding-подпись даёт то же) — поэтому диагностировать по
часам, а не по тексту gpgv. Тест: T-116.

**K-6 · P2 · Документы обещают то, чего не было, и содержат нереализуемое.** До мержа поправить:
`SECURITY.md:7` («мастер сгенерирован» → «сгенерирован на рабочей машине заказчика, отклонение от §4; принятый остаточный
риск и условия — `docs/security/T2-2026-09-26-master-key.md` K-1» — или, после перегенерации, честное «офлайн»);
`SECURITY.md:18` (строка `trust.json`: «офлайн-подключ T, точный пин», см. K-2); `SECURITY.md:34` («офлайн: зашифрованный
носитель» — станет правдой после §5 п. 4); `SECURITY.md:44` §3 — добавить «клиент, пропустивший релиз с новым подключом,
не примет следующий — ставить промежуточный релиз или трек администратора» (Astra #7); `SECURITY.md:66` S2 → `2y`;
`SECURITY.md:79` — `shred -u` не гарантия на SSD: «экспорт S1 — на носитель/в tmpfs, без буфера обмена, `gh secret set` из
файла»; `SECURITY.md:111` календарь (K-4); `README.md:39-41` и `data/keys/README.md:15` — «офлайн-мастер» → по факту.

**K-7 · P2 · Подписант не закреплён.** `scripts/build_manifest.py:243` — `--local-user key` без `!`: gpg вправе выбрать
другой подходящий подключ того же мастера; после подписи отпечаток не сверяется. Фикс: `key + "!"` и после `_sign` —
`gpgv --status-fd 1` с проверкой, что первое поле `VALIDSIG` = ожидаемый подключ. CI — R-1.

**K-8 · minor · Сертификат отзыва `.rev`.** Эксперимент K: клиенту он ничего не даёт (`REVKEYSIG` + `VALIDSIG`, exit 0 —
`verify.py` сигнал не использует); отзыв в продукте = `REVOKED_FINGERPRINTS` + релиз + `trust.json`. `.rev` нужен для
серверов ключей и людей — хранить **отдельно от мастера** (бумага в сейфе / третий носитель), не публиковать заранее.
В `SECURITY.md:93` формулировка верная; добавить «где хранится».

**K-9 · minor · Резервный канал уязвимостей — личный ящик** (`SECURITY.md:119`, uid ключа). Когда человек в отпуске/ушёл —
канал мёртв. Позже — ролевой адрес; не сейчас.

**Проверено, проблем нет:** пин по первичному отпечатку и логика `OR` в `verify.py:205` (Astra согласна: при множестве из
мастера обхода нет; риск — только при смешении ролей, K-2); `hit_revoked` по обоим отпечаткам до пина (`verify.py:200`);
привитый подключ (N); мастер без срока — норма при пине по отпечатку (срок на клиенте всё равно не работает, C/D); тесты
`test_model_cards_catalog.py:26`, `test_catalog_layouts.py:27`, `test_models_catalog.py:33` грузят встроенный каталог
через настоящую связку — CI поймает рассинхрон связки и подписи; `check_keyring.py` берёт пины из `verify.py` — второго
источника правды нет; секреты в репо/истории — нет (M).

### 3.2 `wip/r1-release` (R-серия) — оценка CI/деплоя, крипто и разбора входа

**R-1 · P2 · CI подписывает «чем импортировалось» и не проверяет, что импортирован ровно S1.** `ci.yml:326-328`:
`gpg --import` без проверки состава, `--detach-sign` без `-u`, финальная `gpgv` — только код возврата. Ошибочно загруженный
в секрет `--export-secret-keys` (мастер + S1 + S2) пройдёт молча; подпись «не тем» подключом того же мастера — тоже
(с `check_keyring` перед шагом это равносильно по пину, как отметил code-reviewer, но не по **составу секрета**).
Фикс (одна правка): в `env` job — `SIGNING_SUBKEY: 7602A029F0E34CD2344A9CDA657ED04689FF4D79` (ротация = одна строка);
после импорта — `gpg --batch --with-colons --list-secret-keys` → ровно одна `ssb` с этим отпечатком, `sec` — заглушка
(поле 15 = `#`), иначе `exit 1`; подпись `-u "$SIGNING_SUBKEY!"`; проверка
`gpgv --status-fd 1 --keyring ../data/keys/release.gpg SHA256SUMS.asc SHA256SUMS | grep -q "^\[GNUPG:\] VALIDSIG $SIGNING_SUBKEY "`.
Тест: T-120 (часть про `ssb`/`VALIDSIG`).

**R-2 · P2 · Настройки GitHub** — те же, что K-3, зона T3; шаги — §4 Q4. До тега.

**R-3 · P2 (Astra — P1) · Сборка и подпись в одном контейнере.** `ci.yml:313` (`build-deb.sh --dh`: pip по `wheels.lock`,
`tools/elf-audit`, `scripts/sbom.py` — код из `main` и колёса) исполняется **до** `ci.yml:316-329` с секретом в том же
контейнере: вредоносный шаг сборки может подменить `gpg` в `PATH` или оставить процесс, который дождётся шага с ключом.
У32 сознательно выбрал «сборка и подпись в одном job», чтобы артефакт не ездил, — это защищает артефакт, а не ключ.
Для v0.1 принимаю (колёса `--require-hashes`, Actions по SHA, база `debian:12`, код только из `main`); для v1.0 —
раздельный job `sign` в Environment на свежем контейнере: скачивает артефакт job `build`, сверяет sha256 с выводом
(`outputs`) build, подписывает, публикует. Расхождение с Astra — в сроке, не в сути. Тест: T-121 (v1.0).

**R-4 · minor · `release.gpg` уезжает в Release без проверки «нет секретных пакетов».** `ci.yml:318-322` копирует связку
в ассеты; `check_keyring.py:60-80` смотрит только `pub/sub` (чистый секретный экспорт он отвергнет как «нет первичного
ключа», склейку публичного и секретного блоков — нет). Фикс: в `check_keyring.py` — `gpg --list-packets`, отказ при
`secret key packet`/`secret sub key packet`; ровно один `pub`. Тест: T-119.

**R-5 · minor · `gh` ставится из `cli.github.com` после подписи** (`ci.yml:334-338`). Артефакты уже подписаны, подмена в
upload не пройдёт клиентскую проверку; остаток — доверие к скачанному установщику. Альтернатива без сети к третьей стороне:
`curl` к REST API с `GH_TOKEN`. Только заметка.

**R-6 · доки, обязательно при мерже.** `README.md:87` «Сейчас в `data/keys/release.gpg` тестовый ключ» и
`docs/INSTALL-ADMIN.md:632` (открытый вопрос 1 «мастер не сгенерирован») противоречат `wip/master-key`; при слиянии второй
веткой ждать конфликт в нумерации «Открытых вопросов». Порядок Юрки (сначала `master-key`) верный.

**R-7 · P2 (Astra #4; по INSTALL-ADMIN обеих веток) · Ручной bootstrap слабее кода.** `docs/INSTALL-ADMIN.md:146-149`
велит искать «Good signature»: gpgv доверяет **всем** ключам переданной связки, и связка с добавленным посторонним `pub` +
подпись им даст «Good signature» при верном отпечатке мастера внутри. А единственный источник отпечатка (`:120`) — тот же
GitHub. Фикс в инструкции: «в связке ровно один первичный ключ: `gpg --show-keys --with-colons ./release.gpg | grep -c '^pub:'`
= 1 и его отпечаток = опубликованному»; «`gpgv --status-fd 1 … | grep VALIDSIG` — последнее поле = отпечаток мастера»;
второй канал публикации отпечатка (страница/письмо заказчика, договор). Тест: T-117.

**Проверено, проблем нет:** порядок job `release` (checkout `fetch-depth: 0` → тег = changelog → ancestry с `origin/main`
(L) → `check_keyring` → сборка → подпись → `gpgv` связкой из репо → публикация); `contents: write` только у `release`,
`ci_lint.py` (T-06) это держит; `environment: release` + `if: startsWith(github.ref, 'refs/tags/v')`;
`tools/validate release` (`tools/validate:81-107` — `lstat`+`O_NOFOLLOW`+`fstat`+лимит, один снимок всех файлов,
`Verifier` с пинами из кода, покрытие `SHA256SUMS` и запрет путей/повторов, `dpkg-deb -f` по копии, строгий `latest.json`);
`release_latest_json.py` — без разбора недоверенного входа; `release.sh` — dry-run по умолчанию, CI сверяется по коммиту,
`--push` явный. `latest.json` неподписан, но покрыт `SHA256SUMS` — контракт говорит об этом честно.

## 4. Ответы на вопросы Юрки

**Q1 (остаточные риски процедуры).** См. K-1 и список §5. Коротко: следы на этой машине — буфер обмена (файл истории
Klipper с правами «читают все»), индекс Baloo, незашифрованный SSD (удаление не гарантирует), корзина, возможно `~/.gnupg`;
плюс сама машина онлайн и с агентами. `.rev` создан автоматически — хранить отдельно от мастера, продукту он не нужен (K-8).
Носителей — два, в разных местах, оба зашифрованы или ключи под паролем. Клиентов резервного копирования/синхронизации на
машине не нашёл.

**Q2 (срок подключей 2027-09-26 и встроенный каталог).** Проверено (C, D): после истечения S1 gpgv выдаёт `EXPKEYSIG`, но
**вместе с `VALIDSIG` и exit 0** — `verify.py` принимает; каталог и релизы, подписанные S1, на установленных машинах
**продолжают проверяться**. Это не P1. Что действительно случится 2027-09-26: **CI перестанет подписывать** (E) — и S1, и S2
(один срок, K-4). Решение: «продлевать срок подключа мастером» — да, это штатно и не меняет отпечатков (G): за 60 дней
`--quick-set-expire` для S2 (и/или S3), реэкспорт связки в очередной релиз, релиз должен выйти **до** даты. «Подписывать
каталог с учётом срока» — не нужно. «Срок каталога в самом каталоге» — это другая задача (свежесть/откат), уже закрыта
`serial`/`trust_epoch`; срок-как-дата в каталог не вводить (модель §4.3: «expiry — не нужен»). Единственная клиентская
хрупкость по времени — обратная: часы **раньше** создания ключа (K-5).

**Q3 (мастер `[C]` без срока и без пароля).** Без срока — нормально для якоря, закреплённого отпечатком: срок на клиенте
не работает (C/D), а продление мастера потребовало бы реэкспорта в каждом релизе; «мёртвая рука» через expiry нужна для
серверов ключей, которых здесь нет. Вместо крипто-срока — **дата пересмотра якоря** в календаре §6 (v1.0 и далее раз в
3 года) и преемник M2 в связке (K-1). Без пароля — для **S1 в CI** нормально (пароль лежал бы в том же секрете); для
**мастера и S2** — нет: пароль или шифрование носителя, лучше оба (§5 п. 4). Потеря мастера (не кража) — не катастрофа до
2027-09-26: S1 ещё подпишет релиз с новым мастером в связке и в `PINNED`; кража — да, катастрофа (§5 SECURITY.md).

**Q4 (GitHub).** Да, ограничить окружение тегами `v*` — но это лишь один слой (K-3). Шаги в интерфейсе (владелец, в браузере):
1. *Environment.* Settings → Environments → `release` → «Deployment branches and tags» → выбрать **Selected branches and tags**
   → «Add deployment branch or tag rule» → Ref type **Tag** → Name pattern `v*` → Add rule. (Required reviewers оставить;
   «Prevent self-review» с одним reviewer включать нельзя — некому одобрять.)
2. *Теги.* Settings → Rules → Rulesets → New ruleset → **New tag ruleset** → Name `release-tags`, Enforcement **Active** →
   Bypass list: **Repository admin** → Target tags → Add target → Include by pattern `v*` → Rules: **Restrict creations**,
   **Restrict updates**, **Restrict deletions** → Create. (Защищает от ошибок и от токенов без admin; токен владельца — admin —
   обходит, поэтому п. 5.)
3. *`main`.* Там же → New branch ruleset `main-guard` → Active → Bypass: Repository admin → Target: Default branch →
   Rules: **Restrict deletions**, **Block force pushes** → Create. (Требовать PR с approve при одном участнике невозможно.)
4. *Actions.* Settings → Actions → General → Actions permissions: **Allow ArrivaRUS actions and reusable workflows** +
   «Allow actions created by GitHub» (в workflow только `actions/*`); включить **Require actions to be pinned to a
   full-length commit SHA**; Workflow permissions — оставить **Read** (уже так).
5. *Учётная запись.* Settings → Password and authentication: 2FA — **passkey или аппаратный ключ** как основной метод, SMS
   убрать, recovery codes — на бумагу к носителю с ключами; Sessions → завершить чужие; Applications → Authorized OAuth Apps
   и Personal access tokens → пересмотреть. (Через API флаг 2FA не читается — токен без scope `user`; GitHub требует 2FA
   у всех, кто пушит код, — вопрос в **методе**.)
6. *Токен для агентов (K-3).* Settings → Developer settings → Personal access tokens → **Fine-grained** → Generate:
   Resource owner ArrivaRUS, Only select repositories (`astra-voice`, при нужде `astra-cowork`), Repository permissions:
   **Contents R/W, Pull requests R/W, Issues R/W, Actions Read, Metadata Read**; **Workflows — No access, Administration —
   No access, Deployments — No access, Environments — No access, Secrets — No access**; срок ≤ 90 дней. На машине:
   `gh auth login --with-token < файл` (не через буфер), затем `shred -u файл`; старый `gho_`-токен отозвать
   (Settings → Applications → GitHub CLI → Revoke). Изменения workflow тогда идут только руками владельца в браузере/PR.
   Одобрять deployment — только в браузере, сверив в run коммит тега и `.github/workflows/ci.yml` на нём.

**Q5 (что обновить).** Строки — §8 (test-plan) и §9 (threat-model/SECURITY). Отчёт — этот файл.

## 5. Заказчику — что сделать (простыми словами)

### 5.1 Сегодня, на этой машине (15 минут)
1. **Буфер обмена.** Значок буфера обмена в трее (Klipper) → «Очистить историю». Потом в терминале:
   `shred -u ~/.local/share/klipper/history2.lst`. В настройках Klipper снять «Сохранять историю при выходе». Зачем: текст
   ключа, скорее всего, копировали для вставки в GitHub, а история буфера лежит на диске в файле, который читают все.
2. **Индексатор файлов.** В терминале: `balooctl purge` (если не найдётся — `balooctl6 purge`). Зачем: он мог прочитать файлы
   ключей на рабочем столе.
3. **Корзина** — очистить. **Домашняя папка** — убедиться, что файла с закрытой частью S1 (`…secret….asc`) и папки ключей нигде
   не осталось: `find ~ -xdev \( -iname '*secret*.asc' -o -iname '*.rev' -o -iname 'astra-voice-gnupg*' \) 2>/dev/null` —
   результат должен быть пустым.
4. **Флешка с ключами.** (а) сделать **вторую** копию папки на второй носитель; хранить в разных местах (сейф в офисе и дома);
   (б) поставить пароль на ключи — в терминале с подключённой флешкой:
   `gpg --homedir /media/<флешка>/astra-voice-gnupg --edit-key 5F1F7718559F8F57FFE1178055BB1162F17A5CB0` → команда `passwd`
   → ввести пароль → `save`; пароль записать на бумагу — в сейф (забытый пароль = потерянный ключ); (в) если есть возможность —
   зашифровать сами флешки (в Astra: «Диски» → создать зашифрованный раздел; или аппаратно шифрованная флешка).
5. **Сертификат отзыва** (файл `openpgp-revocs.d/5F1F….rev`) — распечатать или скопировать на третий носитель и хранить
   **отдельно** от флешек с ключами; с флешек его удалить. Он не нужен программе — он для людей и серверов ключей.
6. **Свой личный GPG.** `gpg --list-secret-keys` — в списке **не должно быть** `5F1F 7718 …`. Если есть — значит ключи
   попали и в личную связку: `gpg --delete-secret-and-public-keys 5F1F7718559F8F57FFE1178055BB1162F17A5CB0`.
7. **Диск.** Раздел `/home` не зашифрован; удалённые файлы на SSD стираются не сразу. Выполнить `sudo fstrim -v /home`
   (просит диск освободить стёртое). Гарантии это не даёт — поэтому п. 5.2.
8. **GitHub** — §4 Q4 п. 5 (2FA на ключе/passkey, сессии, приложения). Технические п. 1–4, 6 сделает Юрка с вами.

### 5.2 Решение до тега v0.1 (30.09): новый мастер офлайн — или принять риск письменно
Рекомендация (моя и Astra): **сделать ключи заново правильно** — сейчас это стоит один коммит, после первого релиза смена
якоря = ручная переустановка на каждой машине парка. Карта церемонии (40 минут):
- машина **без сети** (выключить Wi-Fi и кабель, лучше — загрузиться с live-USB Astra); флешка, **уже** зашифрованная;
- `export GNUPGHOME=/media/<флешка>/astra-voice-gnupg; mkdir -p "$GNUPGHOME"; chmod 700 "$GNUPGHOME"`;
- мастер: `gpg --quick-gen-key "Astra Voice Release <alekovalev@astralinux.ru>" ed25519 cert never` (спросит пароль — задать);
- `MASTER=$(gpg --list-keys --with-colons | awk -F: '/^fpr:/{print $10; exit}')`;
- подключи: `gpg --quick-add-key "$MASTER" ed25519 sign 1y` (S1), `gpg --quick-add-key "$MASTER" ed25519 sign 2y` (S2),
  `gpg --quick-add-key "$MASTER" ed25519 sign 5y` (T — для `trust.json`, остаётся офлайн, K-2);
- публичная связка: `gpg --export "$MASTER" > release.gpg`; отпечатки: `gpg --list-keys --with-subkey-fingerprints "$MASTER"`;
- секрет S1 для CI — **на ту же флешку, не в буфер**: `gpg --export-secret-subkeys --armor '<S1-fpr>!' > s1-secret.asc`;
  проверить состав в чистом доме: `gpg --homedir "$(mktemp -d)" --import s1-secret.asc && gpg --homedir … --list-secret-keys`
  → одна `ssb` S1, `sec#` — заглушка, S2/T отсутствуют;
- на онлайн-машине (Юрка): `gh secret set GPG_SIGNING_KEY --env release --repo ArrivaRUS/astra-voice < s1-secret.asc`,
  затем `shred -u s1-secret.asc` на флешке; `release.gpg` + отпечатки — в ветку, как в этот раз;
- `.rev` — отдельно (п. 5); вторая копия флешки; проверить, что копия открывается.
Если решено **не** перегенерировать: запись в `decisions/log.md` «риск K-1 принят для пилота v0.1; преемник M2 — до v1.0»,
и K-6 в `SECURITY.md` — честная формулировка.

### 5.3 Дальше, раз в год (карточка к флешке)
- За 2 месяца до 26.09.2027: с флешки — продлить S2 на год (`gpg --quick-set-expire <мастер> 1y <S2>`), добавить S3, экспортировать
  связку в релиз; после релиза перевести CI на S2 (новый секрет из `--export-secret-subkeys '<S2>!'`).
- Мастер нужен только для этого и для `trust.json` (с M8). В остальное время флешка — в сейфе.

## 6. Границы `wip/r1-release` для T3 (перед первым тегом — отдельно)
T3 проверит: настройки GitHub из Q4 применены (`gh api …/environments/release` → policy tags `v*`; rulesets; `sha_pinning_required`);
R-1 в `ci.yml`; R-6 в доках; ветка `master-key` слита первой; `tools/validate release` на скачанных ассетах; секрет-скан пакета.

## 7. Второе мнение GPT-6 Astra (xhigh, read-only; вывод — гипотезы, сверены)
- **Добавила и принято:** проверка привитого подключа (N) — обход пина через структуру связки не работает; «M2 потом» не
  снимает полномочий уже утёкшего M1 → перегенерация до первого доверенного релиза, иначе явное принятие риска (K-1);
  API-обход барьера GitHub шире approve — правка Environment тем же токеном, защита `main`/workflow, ancestry не проверяет
  происхождение коммита (K-3); при общем пине мастера **S1 подпишет `trust.json`** — нужен точный пин T (K-2); ручной
  bootstrap: «Good signature» при связке с лишним `pub` + отпечаток только из GitHub (R-7); сборка и подпись в одном
  контейнере (R-3); подписант без `!` (K-7/R-1); ротация требует прохождения промежуточных релизов (K-6 §3); `ERRSIG` не
  уникален для часов (K-5); `shred` на NVMe и церемония без буфера (§5.2).
- **Расхождения:** R-3 — Astra P1, я P2 для v0.1 с планом на v1.0 (колёса по хэшам, Actions по SHA, код из `main`;
  раздельный signing job — переделка конвейера перед самым тегом рискованнее, чем принять на пилот). `.rev` — Astra: «не
  бесполезен вообще (gpg и REVKEYSIG)», я: «клиенту не нужен» — принял формулировку Astra в K-8.
- **Astra не оспорила:** пин `OR` (`verify.py:205`), мастер без срока, вывод по Q2 (каталог не протухает).
- Источник команды: `codex exec -m gpt-6-astra -c model_reasoning_effort=xhigh -c service_tier=default -s read-only`, 113 713 токенов.

## 8. Строки для `docs/test-plan.md` (раздел «Безопасность», после T-115)

| T-116 | U | негативный кейс (У92): связка с ключом, созданным «в будущем» относительно часов (фикстура генерирует ключ через `gpg --faked-system-time`) → gpgv `ERRSIG`, exit 2 → `Verifier` = отказ с причиной «часы отстают»; `load_builtin` пробрасывает причину; в argv gpgv нет `--ignore-time-conflict` (assert) | M7 / hotfix v0.1 | [ ] |
| T-117 | M | bootstrap (У95): администратор по `INSTALL-ADMIN.md` получает связку с добавленным посторонним `pub` и `SHA256SUMS.asc`, подписанным им → по инструкции отказ (ровно один `pub` = опубликованный отпечаток; `VALIDSIG … <мастер>` по `--status-fd`); эталонный релиз → успех | R1 | [ ] |
| T-118 | U | `trust.json` (У93): подпись подключом S1 → отказ; подпись офлайн-подключом T (точный пин) → успех; подпись cert-only мастером → `Unusable secret key` (фиксируем невозможность) | M8 | [ ] |
| T-119 | U | связка (У90, R-4): `check_keyring.py` отвергает файл с секретными пакетами (`--list-packets`) и файл с двумя `pub`, один из которых не в пине; принимает эталон; предупреждение, если срок S2 ≤ срока S1 | R1 | [ ] |
| T-120 | M | CI (У91): workflow из ветки (не тега) с `environment: release` не получает окружение (deployment policy `v*`); в job `release` после импорта секрета ровно одна `ssb` = `SIGNING_SUBKEY`, `sec` — заглушка; подпись `-u "<S1>!"`; `VALIDSIG` первое поле = S1, последнее = мастер | R1 (T3) | [ ] |
| T-121 | M | (У94, v1.0) артефакт из job `build` в job `sign`: sha256 совпадает с `outputs` build; подменённый артефакт → отказ до подписи | v1.0 | [ ] |

## 9. Строки для `docs/threat-model.md` и `docs/SECURITY.md`

**У26** (строка 126): статус `➕ R15` → `✅ 26.09 — отпечаток мастера в README/INSTALL-ADMIN/`data/keys/README.md`; остаток → У95 (второй канал публикации, проверка подписанта)`.

**§4.2** (после строки 188, «Факт `man gpgv`»): «Проверено 26.09 (gpg 2.2.40, T2 master-key §2): `EXPKEYSIG`/`REVKEYSIG`
приходят **вместе** с `VALIDSIG` и exit 0 — `verify.py` принимает, срок и отзыв на клиенте не контроль; gpg **не подписывает**
истёкшим подключом и **cert-only мастером** (→ `trust.json` только офлайн-подключом T с точным пином, У93); ключ, созданный
позже часов клиента → `ERRSIG` exit 2 (У92); binding-подписи gpgv проверяет — привитый подключ не проходит (Astra).
S1 и S2 истекают в один день — при переводе резерва в рабочие продлять его мастером (`--quick-set-expire`, отпечатки не меняются).»

**Новые строки §3 (или новый §11 «T2 — смена якоря, 2026-09-26»):**

| У90 | B13/B14 · A5 | I·S | **Закрытые ключи подписи на онлайн-рабочей машине**: Klipper пишет историю буфера на диск (0644), Baloo индексирует, `/home` без шифрования (удаление на NVMe не гарантирует), ИИ-агенты с shell под тем же uid, вставка секрета через буфер. Факт утечки не установлен, опровергнуть нельзя. **P1** | гигиена (T2 master-key §5.1); перегенерация офлайн до первого доверенного релиза **или** письменное принятие риска + преемник M2 в `PINNED_FINGERPRINTS` до v1.0; экспорт S1 без буфера (`gh secret set … < файл`); пароль/шифрование носителя мастера и S2 | 🆕 T2 26.09 | — (процедура) |
| У91 | B13/B11 | S·E | **Токен gh владельца (`repo`+`workflow`) на машине с агентами**: push в незащищённый `main` и `.github/workflows`, тег `v*`, approve deployment и правка Environment через API — required reviewer обходится без человека (prompt-injection агента). **P1** | fine-grained PAT для агентов без Workflows/Administration/Deployments/Environments; approve только в браузере с 2FA на ключе; Environment — теги `v*`; rulesets на `v*` и `main`; SHA pinning + только GitHub-actions на уровне репо; `release.sh --push` — только человек | 🆕 T2 26.09 | T-120 |
| У92 | B1/B14 | D | **Часы клиента раньше 2026-09-26 17:09:54 UTC** → gpgv `ERRSIG` exit 2 на любую подпись; встроенный каталог `bad-signature` без причины. **P2** | причина «часы отстают» в `verify.py`/`catalog.py` по сравнению с датой создания ключа; `--ignore-time-conflict` запрещён | 🆕 T2 26.09 | T-116 |
| У93 | B11 · A5 | T·E | **`trust.json`: cert-only мастер подписать не может; при общем пине мастера его подпишет S1 из CI** — отзыв S1 подписываемый самим S1. **P1 (дизайн M8)** | офлайн sign-подключ T, `Verifier("trust", pinned={T})` — точный пин без первичного; T в связке заранее | 🆕 T2 26.09 (Astra) | T-118 |
| У94 | B11 | I | **Сборка и подпись в одном контейнере**: код сборки (`main`, колёса, apt) исполняется до шага с секретом; подмена `gpg`/фоновый процесс. Astra: P1; принято **P2 для v0.1** | v0.1: колёса `--require-hashes`, Actions по SHA, база `debian:12`; v1.0: отдельный job `sign` на свежем контейнере с проверкой digest артефакта | 🆕 T2 26.09 | T-121 |
| У95 | B1 | S | **Ручной bootstrap слабее кода** (уточнение У26): «Good signature» gpgv при связке с лишним `pub`; отпечаток только из того же GitHub. **P2** | INSTALL-ADMIN: ровно один `pub` = опубликованный отпечаток, `VALIDSIG … <мастер>` по `--status-fd`; второй канал публикации отпечатка | 🆕 T2 26.09 (Astra) | T-117 |

**`SECURITY.md`** — правки K-6 (строки 7, 18, 34, 44, 66, 79, 93, 111) и §6 календарь: «пересмотр якоря — v1.0, далее раз в 3 года; преемник M2 — до v1.0».
**`docs/plans.md:469`** (M8 `trust.py`): «подписан офлайн-подключом T (не мастером — он cert-only), точный пин `TRUST_FINGERPRINTS`».

## 10. Что перепроверит Юрка руками · к живому ИБ-практику
После мержа (и после перегенерации, если будет):
1. `gpg --no-options --show-keys --with-subkey-fingerprints --with-colons data/keys/release.gpg` — один `pub`, отпечаток =
   `PINNED_FINGERPRINTS`; подключи `s`; ни одной `sec`/`ssb`; `gpg --no-options --list-packets data/keys/release.gpg | grep -c 'secret'` → `0`.
2. `gpgv --status-fd 1 --keyring "$PWD/data/keys/release.gpg" data/catalog.json.sig data/catalog.json` → `VALIDSIG`, последнее
   поле = мастер, exit 0. `grep -rn CB951AD7 . --exclude-dir=.git` → пусто. `python3 scripts/check_keyring.py data/keys/release.gpg` → OK.
3. Без перегенерации sha256 `data/keys/release.gpg` = `6847a5b4c01043e321e7ee2b2b5b7e424d5dbaa54dff04f071eed982c326b7f7`,
   `catalog.json.sig` = `c513ea0c…9f91`.
4. После настроек GitHub: `gh api repos/ArrivaRUS/astra-voice/environments/release --jq '.deployment_branch_policy'` ≠ `null`;
   `gh api repos/ArrivaRUS/astra-voice/rulesets --jq 'map(.name)'` → `release-tags`, `main-guard`;
   `gh api repos/ArrivaRUS/astra-voice/actions/permissions --jq '.sha_pinning_required'` → `true`; `gh auth status` → новый токен.
5. Заказчик подтвердил §5.1 п. 1–6 (словами; файл истории Klipper отсутствует: `ls ~/.local/share/klipper/`).

**Живому ИБ-практику** (за рамками агентской оценки): (1) достаточно ли для ФСТЭК-контекста мастера, рождённого на онлайн-машине,
при письменном принятии риска — или церемония обязательна; (2) Ed25519 vs ГОСТ для v1.1 (уже в test-plan); (3) требования к
хранению ключей у заказчика (сейф, два носителя, `.rev`) как организационная мера — формулировка для документа админа ИБ.

## §11. Сверка закрытия условий (26.09, вечер)

> Без нового полного прохода. Проверено: `wt-release` `git diff 557a55e..22d3896` (R-1, R-4), `wt-keys` `git diff d73145f..dd597fc`
> (K-6 и доки), `decisions/log.md` (записи K-1 и K-3 от 26.09), `gh api` (read-only) по окружению, rulesets и Actions;
> прогон юнит-тестов в `venv-a` (Makefile): `wt-release` — `test_check_signing_secret.py`, `test_check_keyring.py`,
> `test_validate_release.py`, `test_release_script.py` — **75 passed**; `wt-keys` — `test_release_keyring.py`, `test_verify.py`,
> `test_model_cards_catalog.py` — **41 passed**. Новые скрипты дополнительно прогнаны руками на живой связке и на моих тестовых
> ключах из scratchpad (позитив и негатив). Наблюдения по гигиене — только `stat`/имена файлов, содержимое не читал.

### 11.1 `wip/master-key` (`dd597fc`)

| Условие | Статус | Что проверено · что осталось |
|---|---|---|
| K-6 формулировки до мержа | **закрыто** (с одним хвостом не к мержу) | `SECURITY.md`: статус (:7) честно — «M1 на рабочей машине, отклонение от §4, риск принят письменно, M2 офлайн до 31.10»; строка `trust.json` (:18) → подключ T с точным пином; §2 хранение M1/S2 по факту; §3 — переподпись каталога S2 до релиза без S1, пропущенный промежуточный релиз, продление S2 (`--quick-set-expire … 2y …`); §4 — экспорт S1 на носитель/`tmpfs` без буфера, `gh secret set` из файла, честно про `shred`; §5 — `trust.json` подключом T, `.rev` отдельно (K-8); §6 календарь — M2 до v1.0, продление S2, пересмотр якоря раз в 3 года. `README.md:36-42`, `data/keys/README.md:15`, `INSTALL-ADMIN.md:136` — происхождение M1 названо. `plans.md` A-06 и M8 `trust.py` — T вместо мастера. **Хвост:** скрипт церемонии `SECURITY.md:76-81` не обновлён — S2 по-прежнему `sign 1y`, подключа T нет, uid `security@example.org`; нужно до церемонии M2 (31.10), мерж не держит. |
| K-1 решение до тега | **закрыто** | `decisions/log.md` 26.09 «K-1 … риск принят для v0.1»: пилот на текущих ключах; до v1.0 — M2 офлайн (сеть выключена, зашифрованная флешка с паролем, T для `trust.json`, S2 на 2 года), рядом с M1 в связке и пинах, затем снятие пина M1; вопрос ФСТЭК — живому ИБ-практику. Соответствует K-1/K-2/K-4. |
| §5.1 гигиена машины «сегодня» | **не закрыто** (сторона заказчика) | На 21:30 MSK следы те же: `~/.local/share/klipper/history2.lst` — 0644, 6824 байт, mtime **20:25:41** (история не очищена); корзина — 533 файла; Baloo — «Индексирование выполняется» (не `purge`). Хорошее: поиск по именам в `~` (без `.git`, `~/.gnupg`, кэшей) — `*secret*.asc`, `astra-voice-gnupg*`, `private-keys-v1.d`, `openpgp-revocs.d` **не найдены**. Осталось: п. 1 (Klipper), 2 (`balooctl purge`), 3 (корзина), 4 (пароль/шифрование носителя, вторая копия), 5 (`.rev` отдельно), 6 (`gpg --list-secret-keys`), 7 (`fstrim`) — **до тега v0.1**; T3 спросит словами. |
| K-7 `build_manifest.py` `!` | **закрыто** | `scripts/build_manifest.py:244` — `key + "!"`; тест `test_build_manifest_sign_pins_exact_key`. |
| T-116…T-121, У26, §4.2, §11 У90…У95 | **закрыто** | `docs/test-plan.md:219-224`, `docs/threat-model.md:126, 191, 328, 567-578` — дословно как в §8/§9. |
| K-2 (дизайн M8), K-4 (срок S2), K-8 (`.rev`) | **закрыто в доках**, ключи — при церемонии M2 | Реализация T — M8; продление S2 — церемония M2 (до неё `check_keyring` предупреждает: «все подписывающие подключи истекают в один день (2027-09-26)» — проверено на живой связке). |
| K-5 часы клиента | **не закрыто** (не условие мержа) | Кода нет; T-116 запланирован «M7 / hotfix v0.1». Дёшево до тега: одна строка в `INSTALL-ADMIN.md` «если дата на машине раньше 26.09.2026, проверка подписи не пройдёт — выставьте время»; код — v0.2. |
| R-7 ручной bootstrap (`INSTALL-ADMIN.md:145-157`) | **не закрыто** (P2, R1/T3) | По-прежнему «нужна строка Good signature»; нет «ровно один `pub`» и `VALIDSIG … <мастер>`. Правка tech-writer до тега; T3 проверит. |
| K-9 ролевой ящик | открыто, позже | — |

`test_release_keyring.py:59` требует **ровно один** `public key packet` в связке — при добавлении M2 (v1.0) тест придётся ослабить до «все первичные закреплены»; сейчас верно.

### 11.2 `wip/r1-release` (`22d3896`)

| Условие | Статус | Что проверено · что осталось |
|---|---|---|
| R-1 пин подписанта + состав секрета | **закрыто** | `scripts/check_signing_secret.py`: `secret` — в доме CI ровно один `sec` **и он заглушка** (`#`), закреплён, не отозван; ровно один настоящий `ssb` с `s`, не отозван/истёк; подключ **присутствует в публичной связке под этим мастером** (защита от «секрет другого мастера»); `signature` — `Verifier` + отпечаток подключа = выбранному + первичный в пинах. `ci.yml:327-330`: отпечаток берётся из проверки, подпись `--local-user "${SIGNING_FPR}!"`, после `gpgv` — `signature`-проверка. Решение лучше моего (`SIGNING_SUBKEY` в `env`): ротация без правки workflow. Ручной прогон: дом «`sec#` + один `ssb`» → отпечаток; дом с настоящим секретом мастера → отказ («секрет мастера не является заглушкой»); чужой пин → отказ; подпись верная → пусто; «не тот подключ» → «подпись выполнена другим подключом». 339 строк тестов, зелёные. |
| R-4 связка без секретных пакетов | **закрыто** | `check_keyring.py:44-56` — `--list-packets`, отказ на `:secret key packet:`/`:secret sub key packet:`; несколько `pub` допустимы, если **все** закреплены и не отозваны (решение Юрки под M2 — согласен, это и есть план K-1); предупреждение об одной дате истечения (K-4). Ручной прогон: живая связка → `ok`, предупреждение про 2027-09-26; файл с секретным подключом → «в публичной связке есть секретные пакеты». `release.sh:60` глушит stdout — предупреждение (stderr) остаётся видно. |
| R-2 настройки GitHub п. 1–4 | **закрыто** (проверено API) | Environment `release`: `deployment_branch_policy = {custom_branch_policies: true}`, политика одна — `v*` типа **tag**; required reviewer ArrivaRUS сохранён (`prevent_self_review: false` — ожидаемо при одном reviewer). Rulesets: `release-tags` (id 24047043) — `creation`/`update`/`deletion` на `refs/tags/v*`, active, обход `RepositoryRole` id 5 (Repository admin); `main-guard` (id 24047044) — `deletion`/`non_fast_forward` на ветке по умолчанию, active, тот же обход. Actions: `allowed_actions: selected`, `github_owned_allowed: true`, `verified_allowed: false`, шаблонов нет, **`sha_pinning_required: true`**; workflow permissions `read`. Совместимость: в `ci.yml` только `actions/checkout`, `actions/cache`, `actions/upload-artifact`, все по полному SHA — под политику подходят. |
| K-3 остаток (п. 5–6) | **не закрыто** (по плану) | 2FA на passkey/ключе — за заказчиком (API не показывает). Токен `gh` на машине по-прежнему `gho_` со scope `gist, read:org, repo, workflow` — fine-grained PAT без Workflows/Administration/Deployments/Environments по решению ставится **после** слияния `wip/r1-release`; до этого агенты с этой машины всё ещё могут менять workflow и одобрять deployment. `release.sh --push` — только человек. |
| R-6 доки релизной ветки | **не закрыто** (ожидаемо, при слиянии) | `README.md:87` «тестовый ключ», `INSTALL-ADMIN.md:135` (`[уточнить]` про несгенерированный мастер) и `:632-633` (открытый вопрос 1) — устаревшие; при слиянии второй веткой после `master-key` брать версию `master-key` (конфликт в блоке «Важно» и в нумерации «Открытых вопросов»). |
| R-3 раздельный job `sign`, R-5 `gh` из cli.github.com | открыто по плану (v1.0 / заметка) | — |

### 11.3 Итоговый вердикт по мержу

- **`wip/master-key` `dd597fc` — PASS.** Условия мержа закрыты (K-6 в доках, решение K-1 записано, тесты и реестры на месте).
  До тега v0.1 (T3): §5.1 гигиена заказчиком, R-7 и строка про часы в `INSTALL-ADMIN.md`, K-3 п. 5; до церемонии M2 — §4 скрипт.
- **`wip/r1-release` `22d3896` — PASS.** R-1 и R-4 закрыты и проверены руками, настройки GitHub п. 1–4 подтверждены API.
  Порядок: сначала `master-key`, затем `r1-release` с разрешением R-6 в пользу `master-key`; после слияния — fine-grained PAT
  для агентов (K-3 п. 6), затем T3 перед первым тегом.
