# Astra Voice — ИБ-ревью T2, инкремент 22–24.09 (2026-09-26)

> security-analyst (Fable 5.1) · триггер **T2** по `security-gate` (диф трогает захват ввода, десериализацию файла и IPC,
> подпись/отзыв каталога, ФС/права, буфер обмена, упаковку) · ревизия `main` `d077ba2` (диапазон `1e8c619^..d077ba2`,
> 52 файла, +7701/−418) · метод: чтение кода (Read/Grep), сверка с `docs/threat-model.md` §3, PRD §9.5, контракт 1.2;
> тесты не запускались (машина в режиме демо: GUI/X/звук/шина не трогались), код не правил, git не трогал.
> Второе мнение GPT-6 Astra (xhigh, read-only) — §4. Новые угрозы для реестра — `docs/threat-model.md` §10 (У83–У86, П12,
> T-111…T-114). В рабочем дереве на момент ревью была чужая незакоммиченная правка `PRD.md` — не моя, не трогал.

## 0. Вердикт

**✅ Мерж/выкладка инкремента не блокируется.** Blocker — 0, major — 0, minor — 4 (У83–У86), «ok» — по пяти пунктам из
восьми. Ничего из найденного не открывает нового пути через границу доверия: везде либо тот же uid (B2, вне границы),
либо гигиена. Список правок для developer — §2; что можно отложить — §3.

## 1. Восемь пунктов брифа: угроза → мера → вердикт

| № | Что | Угроза (STRIDE) | Мера в коде (файл:строка) | Вердикт · как показать |
|---|---|---|---|---|
| 1 | **Перехват ввода при смене клавиши из «Общих»** — `ui/hotkey_capture.py`, `core/capture_watchdog.py`, `SettingsBridge.save_capture_combo` | (I) keylogging-поверхность: пока поле открыто, `XGrabKeyboard` отдаёт Voice все нажатия; (D) залипший захват при зависшем GUI (У47); (T) строка комбинации как вход в настройки | Сторож — отдельное X-соединение в daemon-потоке, все обращения к Xlib только там (`capture_watchdog.py:21-42`); срок `min(timeout, 30 с)` (`:59`), опрос ≤ 1 с, по дедлайну **сначала рвётся сокет** (`:310-330`) — сервер снимает захват независимо от GUI (закрывает У47); GUI получает только `combo`/`cancel`/`hint`/`expired` через queued-сигнал (`hotkey_capture.py:58`, `:202-215`), сырые keycode не журналируются (`_key_event` возвращает фиксированный словарь `Ctrl/Shift/Alt/Super + [A-Z0-9]/Space/F1–F12`, `:240-307`); комбинация проходит `is_valid_combo` и живой пробник до записи (`hotkey_capture.py:143-157`, `bridges.py:526-536`); захват снимается до пробы и сохранения (`:137-138`); повторный `begin` не продлевает срок (`runtime.py:1258-1261`) | **minor (У83).** Отмена только на `Hide`/`Close`/сворачивание (`hotkey_capture.py:68-82`); при Alt+Tab захват на корне держится до 30 с — клавиатура сессии «мертва», нажатия уходят нам (не пишутся). T-24 обещает «по потере фокуса». Показать: открыть поле в «Общих», Alt+Tab в Kate, печатать — ничего ≤ 30 с; в журнале ничего. Остальное — ok |
| 2 | **`core/measurements.py` + `paths.measurements_path()`; `worker/ipc.py` протокол 2** | (T) подмена/порча файла замеров → неверные цифры/крах; (D) гигантский/бесконечный файл, FIFO, глубокий JSON; (I) 0644 в домашнем каталоге; (D) воркер другой версии → цикл перезапусков | Чтение: `lstat` — только обычный файл ≤ 1 МиБ, чтение ограничено `MAX+1`, `RecursionError`/`UnicodeError` пойманы, значения проверяются по типу на месте (`measurements.py:23-47`, `:89-98`, `catalog.py:132-142`); fail-open осознанный — файл влияет только на карточку и на повод перемерить, не на допуск/безопасность (`ramMb` для OOM-гейта берётся из подписанного `min_ram_mb`, `model_downloads.py:875-880`). Запись: каталог `ensure_private_dir` (0700, не symlink, свой uid — `paths.py:35-47`), `NamedTemporaryFile` рядом + `fchmod 0600` + `fsync` + `os.replace`, временный файл убирается в `finally` (`measurements.py:180-202`); `os.replace` поверх подсунутой ссылки заменяет саму ссылку, не цель. IPC: `hello.protocol ≠ 2` → `PROTOCOL_MISMATCH` (`ipc.py:153-155`), супервизор завершает ожидания, убивает воркер и уходит в `stopped` **без** цикла перезапуска (`supervisor.py:291-311`); схема `measured` — `vm_hwm_kb: int`, отрицательное отбрасывается (`measurements.py:141-143`); длина кадра проверяется до копирования тела (`ipc.py:204-231`, У35) | **ok.** Показать: положить в `~/.local/share/astra-voice/measurements.json` FIFO / 2 МиБ / `{"a":[[[[…` — старт и диктовка не страдают, в журнале одно предупреждение; запустить воркер с `protocol: 1` (фейк) → одно сообщение «Программа обновлена — перезапустите», процессов-зомби нет |
| 3 | **`models/catalog.py`: `merge_measurement`, `mirrors` (T-80), fail-open отзыва (`revoked[]`/`trust.json`), У6** | (T) зеркало вне allowlist / URL-инъекция через `url_path`; (T) обход отзыва; (T) path traversal (У6); (D) `log()` от нуля | `_mirrors`: только `host_allowed` (метки `[a-z0-9-]`, поддомены лишь у HF; IDN невозможен), без дублей и без основного хоста (`catalog.py:277-289`, `net/hosts.py:13-31`); URL = `"https://" + host + url_path` (`downloader.py:255-260`), `url_path` проверен: начинается с `/`, не `//`, без `?`/`#`/пробелов/Cc/Cf, ≤ 8 слоёв percent-декодирования, без `..`, содержит ревизию (`catalog.py:340-368`); `merge_measurement` — `log()` только при `speed > 0`, `threads` строго `int` (`:145-177`). Отзыв: `Catalog.is_revoked` в UI, `Installer._check_revoked` первым шагом (`installer.py:274-278`, `:299`, `:374`), рантайм — `resolve_model_request(revoked=…)` → `ModelRevoked` → модель не грузится (`runtime.py:285-292`, `:657-661`). У6: `_check_path` (`catalog.py:323-338`) + `_path_parts`/`_file_path` с `lstat` каждой части и `resolve` внутри каталога, повторная проверка после создания родителей (`installer.py:74-120`) | **minor (У84).** Если каталог не загрузился (`CatalogError`: подпись, схема, `stale-catalog`), `model=None` и `set_revoked_check` не подключается (`app.py:812-830`) — рантайм грузит текущую модель **без** проверки отзыва, пользователю только строка в журнале. Реалистичный путь без атаки: откат пакета после принятого сетевого каталога → `stale-catalog`. `trust.json` — M8, пока нет. Показать: испортить `catalog.json.sig` (копия дерева, не машина заказчика) → отозванная в каталоге ревизия грузится молча. Остальное (зеркала, `merge`, У6) — ok |
| 4 | **Проба захвата** `tests/xvfb/test_paste_grab_probe.py` и `platform/x11.py::focus_client/target_window` | (S) увод вставки в чужое окно; (I) FocusOut у цели закрывает попапы/выделение (P0 22.09) | `target_window()` = `_NET_ACTIVE_WINDOW`, иначе клиент фокуса ввода, поднятый до окна из `_NET_CLIENT_LIST_STACKING`, не override-redirect, не корень (`x11.py:594-640`); цель фиксируется при старте записи; перед XTest `_focus_mismatch` требует, чтобы **реальный** фокус лежал внутри именно этого окна (по id, не по классу) и `WM_CLASS` не изменился (`paste.py:315-363`); `_keyboard_busy` пробует захват на окне фокуса (не на корне) и сразу снимает (`:374-386`), тест подтверждает только `NotifyGrab/NotifyUngrab` (`test_paste_grab_probe.py:179-191`); ошибки/исчезновение окна → без XTest; журнал — только коды веток (`:520-538`) | **ok.** Увести вставку можно только туда, где фокус пользователя, и только в окно, запомненное при нажатии; второе окно того же класса → `focus-outside`. Тест сам отказывается работать на `:0` (`:32-35`) |
| 5 | **P2 вставки** `platform/paste.py` (`_TrackedMimeData`, `FETCH_TIMEOUT_MS=1500`, `reason=not-fetched`) | (I) фраза дольше лежит в CLIPBOARD/PRIMARY — кто прочитает; (T) чужой ранний запрос буфера как ложный «забрал» → ранний возврат (П6); (I) `fetched_ms` в журнале | Забор фиксируется только запросом `text/*` (`:232-240`); ждём до 1500 мс, прерываемся при смене владельца или раннем `restore_pending` (`:674-699`); при `not-fetched` CLIPBOARD и PRIMARY **не** возвращаются (`:706-707`, `:725-733`) — фраза остаётся для ручной вставки, прежний секрет пользователя не переиздаётся (У40 не ухудшен); публикация всегда с `x-kde-passwordManagerHint=secret` и Fly-типом (`:625-630`), Klipper/fly-clipboard историю не пополняют; журнал — `fetched_ms`/коды, без текста | **minor, принято (У85/П12).** Окно чтения любым X-клиентом того же uid (П1) выросло со ~150 мс до 1,5 с, а при `not-fetched` — до следующего копирования; это и есть цена фикса инцидента 23.09 (потеря диктовки). Правка — только `PRIVACY.md`. Ложный `fetched` от менеджера буфера Fly (если он запрашивает `text/*`) — функциональный риск П6, проверить в K5 Fly |
| 6 | **`tools/validate catalog --switch`** (пишет `current.json`, блокировка приложения, SIGTERM) | (T) гонка с работающим приложением за `current.json`; (D) прерывание оставляет чужой выбор модели; (I) запись замеров/уведомлений из проверки | `QLockFile(paths.lock_path())` — тот же замок, что у приложения; занят → отказ «Закройте Astra Voice» (`tools/validate:1731-1748`); только для боевого хранилища (`--store` иной путь — без замка); исходные байты `current.json` читаются до изменений, id проверяются `ID_RE` (`:1495-1506`); `store.set_current` — атомарная запись 0600 через временный файл + fsync каталога (`store.py:83-97`, `:365-372`); возврат — `mkstemp` + `os.replace` + fsync при **заблокированных** SIGINT/SIGTERM/SIGHUP (`:1541-1560`, `:1689-1709`); `measurements_path` подменён на `None`, уведомления — на заглушку (`:1588-1591`) | **ok.** Показать: запустить проверку при работающем приложении → код 2 и подсказка; `kill -TERM` посреди переключения → `current.json` байт-в-байт прежний |
| 7 | **`ui/model_downloads.py` состояние 20 «вне каталога» и OOM; `Installer.install_from_path`** | (T) модель без записи в подписанном каталоге грузится в воркер; (T) чужие файлы/ссылки в папке «из папки» (У43); (D) OOM всей сессии | Записи вне каталога строятся из имён каталогов хранилища, прошедших `ID_RE` (`store.py:335-352`), `files=()`, `host=""`, `min_ram_mb` = максимум каталога (консервативно, `model_downloads.py:393-435`); их нельзя выбрать в очередь загрузки (`toggleModel`, `:1288`), перепроверить (`recheck_entries` исключает — `:464-478`) и «обновить»; сделать рабочей — можно (файлы проверялись sha256 при установке, лежат в 0700-store того же uid). Перед загрузкой **любой** модели воркер делает то же, что для каталожной: скан ONNX на `external_data`/лишние файлы (`engine.py:20`, `:58-64`, У7), `RLIMIT_AS`, `oom_score_adj`, `RLIMIT_CORE=0` (`worker/main.py:41-85`, У8/У9), самопроверка. `install_from_path`: отзыв первым (`installer.py:374`), `_check_contents(src, allow_parts=False)` отвергает всё вне списка, вложенность/ссылки — `_file_path` на каждом уровне, копирование с подсчётом sha256 и лимитом размера (`:218-240`), staging внутри store и не пересекается с источником (`:389-393`), в `finally` staging убирается | **ok.** Модель вне каталога = ревизия, снятая с каталога (F8.5), а не «неподписанные файлы»: sha256 сверены при установке, воркерные контроли те же. Остаточный риск «модель как источник команд» для F17 — в `threat-model.md` §9 У70 (там она уже major, потому что адресат — агент с shell). OOM: пауза по `MemAvailable`, подсказка «более лёгкая модель» — DoS-контроль на месте |
| 8 | **Сборка/пакет: purge оставляет `/var/lib/astra-voice/staging` (root)** | (T/E) чужой каталог под root после удаления пакета; (I) мусор помощника | Каталог из `astra-voice.dirs:1`, `root:root 0700` (`rules:58-62`; на машине: `drwx------ root root`); dpkg удаляет каталоги пакета при remove/purge, если они пусты; непустой останется (только следы помощника M8); родитель `/var/lib/astra-voice` — `root:root 0755`, пользовательских компонентов в пути нет | **ok / minor-гигиена (У86).** Риска нет: в 0700 root никто не попадёт, эксплуатировать нечего. В M8: помощник чистит транзакционный каталог на всех исходах; `postrm purge` → `rm -rf /var/lib/astra-voice` — безопасно (нет user-writable компонентов пути) |

Дополнительно посмотрено попутно (без находок): `paths.runtime_dir()` на Astra с `XDG_RUNTIME_DIR` → символьная ссылка
уровня МРД — проверяется только лист `astra-voice` (владелец, не ссылка), как и должно; `worker/state.py:742-756` читает
только `/proc/self/status|smaps_rollup`; `store._directories` отбрасывает ссылки и чужие имена.

## 2. Правки для developer

**Blocker:** нет. **Major:** нет.

**Minor (в один микрошаг каждый, ревью Claude обязательно):**
1. **У83** — `ui/hotkey_capture.py:68-82`: отменять захват при `QEvent.WindowDeactivate` (и `ActivationChange`, когда
   `isActiveWindow()` ложь) и при `FocusOut` поля; unit + xvfb-кейс T-111. Строку T-24 в test-plan привести к факту.
2. **У84** — три места, не одно (уточнение Astra, проверено): (а) `core/model_source.py:150-160` `_not_revoked()` глотает
   **любое** исключение проверки и возвращает запрос — различить «не отозвана» и «проверка неизвестна» (второе → снимок
   или предупреждение, не молчаливая загрузка); (б) `models/catalog_state.py`: хранить снимок `revoked[]` последнего
   принятого каталога в `catalog-state.json`; (в) `app.py:812-830`: при `CatalogError` подключать `set_revoked_check` по
   снимку; в трее/«Общих» показывать «Список моделей недоступен — проверка отзыва не выполняется» (U T-112). Известный отзыв
   не должен забываться при ошибке каталога.
3. **У85/П12** — `docs/PRIVACY.md`: «после неподтверждённой вставки фраза остаётся в буфере до следующего копирования;
   1500 мс — срок ожидания приёмника, не срок удаления текста»; в модели угроз П6 остаётся открытым: `_TrackedMimeData`
   отмечает чтение **любым** потребителем (менеджер буфера, другой клиент), это не квитанция вставки (Astra). K5 Fly —
   проверка ложного `fetched` (T-113). Кода не требует.
4. **У86** — в план M8 (DoD помощника): чистка транзакционного каталога на всех исходах + `postrm purge` (T-114). Сейчас
   кода не требует.

## 3. Что можно отложить

- У84 и У86 — до M7/M8 соответственно (там появляются сетевой каталог и помощник, без которых сценарии не воспроизводятся).
- У85 — правка только документа; сделать вместе с ближайшей правкой PRIVACY (V6 или R1).
- У83 — можно в хвост M6 (одна правка + тест), не блокирует m6.13 у заказчика.

## 4. Второе мнение GPT-6 Astra

`codex exec -m gpt-6-astra -c model_reasoning_effort=xhigh -c service_tier=default -s read-only`, 2026-09-26, один вызов на
T1 и T2 (полная таблица расхождений по T1 — `docs/threat-model.md` §9.6). По T2 Astra **согласна: новых blocker нет**,
оценки п. 2, 4, 6, 7, 8 подтверждены («замеры не задают полномочий; IPC mismatch останавливает воркер; mirrors проверяются;
focus-проверки сохраняют принятую гонку X11; `catalog --switch` защищает штатное переключение; состояние 20 само по себе не
новая компрометация; purge staging — гигиена»). Добавления Astra, принятые после сверки с кодом:

| Пункт | Astra | Сверка | Итог |
|---|---|---|---|
| п. 1 / У83 | согласна (minor); проверить также автоматический lock, зависание GUI, SIGSTOP; не путать свои `NotifyGrab` с деактивацией | — | T-111 расширен; связка с У75 (захват мешает локеру) |
| п. 3 / У84 | fail-open есть и при подключённом callback: `_not_revoked()` подавляет любое исключение (`model_source.py:150`) | **подтверждено** (`:150-160`, `except Exception: … return request`) | правка §2 п. 2 расширена на три места |
| п. 5 / У85 | `_TrackedMimeData` отмечает чтение любым потребителем — не квитанция; 1500 мс — срок ожидания, не удаления; П6 не закрыт | **подтверждено** (`paste.py:236-240`) | формулировка П12/PRIVACY уточнена |
| п. 2 | `lstat→open` — гонка того же uid, оценку не повышать | согласен | ok |

Расхождений по T2 нет. Что Astra не подтвердила и я не принял: ничего по T2.

**HEAD во время ревью:** `d077ba2` → `7fd5bc4` (влит `wip/m6-card`, пересняты эталоны): в `src/` изменились только
`models/catalog.py` (`merge_measurement` упрощён до `measuredRtfx`, проверка типов сохранена) и `ui/model_downloads.py`
(карточка) — границ доверия не касается, выводы остаются в силе.

## 5. Что перепроверит Юрка руками

- П. 1: поле захвата в «Общих» + Alt+Tab → клавиатура возвращается ≤ 30 с (сейчас) и ≤ 100 мс (после У83).
- П. 5 во Fly (K5): менеджер буфера Fly не создаёт ложный `fetched` (журнал `fetched_ms` пуст без реального Ctrl+V),
  `~/.fly/clipboard` без фразы.
- П. 6: `tools/validate catalog --switch` при работающем приложении → код 2; `kill -TERM` посреди → `current.json` прежний
  (только на дев-хранилище, не на машине заказчика в режиме демо).
