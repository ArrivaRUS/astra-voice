# Status — Astra Voice

> Живой лог исполнения. Засеян так, чтобы другой прогон продолжил без чтения чата.
> **Обновлено:** 2026-09-09 — M0 в работе: S3 принят, S1 идёт в KDE.

## Current phase
- **Фаза 4 «Разработка» — M0 `[~]`**: S3 принят 2026-09-09 (`arch/spikes/S3.md`), S1 идёт в KDE; S2, S4, S5 и Fly-части не начаты. Первый невыполненный milestone — `docs/plans.md` § M0.
- Фаза 3 закрыта: планы #1/#2 → синтез (`arch/plan-synth.md`) → челлендж Astra (22 находки) → T1 (`docs/threat-model.md`) → G4 → execution-pack (P13).
- Гейты: G0, G1, G2a, G2, G3, G4 пройдены; следующий ⛔ — **G5 на v0.1 (30.09)**. Открытый вопрос заказчику: согласие ГК «Астра» на имя (L1).

## Done
- 2026-09-07: интейк (`PROJECT.md`), публичный репо `ArrivaRUS/astra-voice`, Discovery (5 отчётов, `research/summary.md`), G2a (имя Astra Voice, знак 06 «Слоги», лого-пак), G1 (PRD 0.2, stories, backlog), лицензия GPL-3.0-or-later.
- 2026-09-08: G2 (направление A «Панель»), G3 (макет A 48 экранов, `design/spec.md` 0.2, `design/tokens.json` 2.1.1, `design/refs/` 48 png), решения У1–У14, цифры каталога.
- 2026-09-09: `arch/plan-claude.md` (#1) и `arch/plan-codex.md` (#2); синтез `arch/plan-synth.md` (Р1–Р13, §7 22 контракта, §8 Fly); T1 `docs/threat-model.md` (40 угроз, T-01…T-36); G4 закрыт (v1.0 = `.deb` + polkit, user-bundle v1.1); Fly-адаптеры; PRD **0.5** (S17, память Р5, манифест onnx-asr, `serial`/`trust_epoch`/`revoked[]`, `trust.json`), stories 0.4 (US-6.6, US-12.5), backlog 0.4; урок `.patches/001`; допуски заказчика на спайки; execution-pack: `docs/plans.md` (M0–M11, R1–R3, T1 §5 по вехам), `docs/status.md`, `docs/test-plan.md` (T-01…T-36, S1–S17).
- 2026-09-09: **M0.S3 принят** (`arch/spikes/S3.md`, журнал «M0.S3 принят») — ORT 1.24.4: 3 `.so`, max GLIBC_2.27, 0 isoc23, Requires-Dist 5/5 из apt; три GigaAM v3 @ `322c3b29…` 677 109 053 Б, 16 файлов с sha256; p95 e2e_rnnt 228/253/247 мс, e2e_ctc 229/153/194 мс (1/2/4 потока), VmHWM 366–404 МиБ при ≤ 2 потоках (467 при 4); отмена `terminate` 0,5–4,1 мс (патч +28 строк); `RLIMIT_AS` 512/768/2048 МиБ; дефолт `e2e_rnnt`, `SherpaEngine` не нужен; `intra_op=2`, `allow_spinning=0`, `RLIMIT_AS` 3 ГиБ, дедлайн `engine.load`; `scripts/measure_model.py` в репо.

## In progress
- **M0.S1** — живой прогон пилюли + трея в KDE на машине заказчика (ок заказчика получен); Fly-часть — после перелогина.

## Next
- → **S1-Fly** (перелогин заказчика) → **S2** (после подготовки тестового пакета/помощника/`.policy` и одного `sudo` заказчика) → **S4**, **S5** (KDE сразу, Fly — при том же перелогине) → **M1** (скелет/упаковка/CI) с параметрами S3: `intra_op=2`, `allow_spinning=0`, `RLIMIT_AS` 3 ГиБ, дедлайн `engine.load` 10 с, каталог на вариант, патч отмены в `vendor/patches/`, `tools/benchmark` из `scripts/measure_model.py`.
- После M0: запись решений в `decisions/log.md`, `arch/spikes/S*.md` в git → M1 (скелет/упаковка/CI).

## Decisions (decisions/log.md)
- 2026-09-09 «Архитектура принята: синтез + челлендж Astra + T1; допуски на спайки» — execution-pack по синтезу; T1 §5 → plans (по вехам), §6 → test-plan; T2 на каждый диф `helper/`, `security/`, `updates/`, `net/`; T3 перед v0.2; пин `onnx-asr` 0.12.0 + `onnxruntime` 1.24.4; S3 ~680 МБ; S2/Fly на машине заказчика, `sudo` вводит он; ключ — офлайн-мастер у заказчика, подключ в GitHub Environment `release`.
- 2026-09-09 «G4 закрыт» — v1.0 вариант A (`.deb`, интерфейс по спеке, polkit-обновлятор); relocatable-раскладка в M1; user-bundle C1 — v1.1.
- 2026-09-09 «Fly-сессия: адаптеры» — детект по `XDG_CURRENT_DESKTOP`, тема `~/.fly/paletterc`, трей явных цветов, клип-менеджер fly-wm, лаунчер ставит `QT_QUICK_CONTROLS_STYLE`, риск R17 (блокировка интерпретаторов).
- 2026-09-09 «Требование: KDE и родной Fly» — S17, E2E в обеих сессиях; 2026-09-09 «Установка без прав администратора — мягкое требование».
- 2026-09-08 G3 · У1–У14 · цифры каталога · токены; 2026-09-07 G1 · G2a · рамки (оба трека обновления, PyQt5 из apt, пилюля по умолчанию с выключателем, `Ctrl+Space`, модель в памяти, автозапуск v0.2, ЗПС-трек после v1.0).

## Assumptions
- Полный список — `docs/plans.md` § Assumptions **A-01…A-16**: ORT 1.24.4 (подтверждено S3) · 3 `.so` без шима · шрифты из apt (`fonts-pt-root-ui`, `fonts-pt-mono`) · детект сессии `XDG_CURRENT_DESKTOP` с резервом по атомам root (план #1 §13.1: во Fly переменная может быть пуста) · relocatable с M1 · GPG/gpgv + пины + `trust.json` (M8) · каталог `serial`/`trust_epoch`/`revoked[]` (PRD 0.5) · requests + jsonschema за фасадом · память Р5 · отмена Р4 · маркеры/CLI создаются с функцией · микрозадачи дробит Developer · спайки на машине заказчика · меню трея системное · звуки `.ogg` (способ воспроизведения — M9).
- Путь помощника: план/синтез — `/usr/libexec/astra-voice/update-helper`; PRD 0.5 §9.2 пишет `/usr/libexec/astra-voice-update-helper` — берётся путь плана (архитектура), PRD поправить при следующей ревизии.

## Commands (copy-paste)
- lint: `ruff check src tests scripts tools && mypy --strict src && qmllint qml/**/*.qml`
- unit: `pytest -m unit -q`
- xvfb: `xvfb-run -a -s "-screen 0 1600x1000x24" pytest -m xvfb -q`
- engine: `pytest -m engine -q`
- build: `make deb && lintian dist/astra-voice_*.deb && tools/elf-audit --strict dist/astra-voice_*.deb`
- validate: `tools/validate <тема> [--session kde|fly]` · `tools/benchmark --model gigaam-v3-e2e-rnnt-int8 --runs 50 --threads 4`
- virtual mic up: `pactl load-module module-null-sink sink_name=av_test sink_properties=device.description=av_test && pactl load-module module-remap-source master=av_test.monitor source_name=av_test_mic source_properties=device.description=av_test_mic`
- e2e dictation: `xdotool keydown ctrl+space; paplay --device=av_test data/test/test-ru-6s.wav; xdotool keyup ctrl+space; astra-voice --stats`
- virtual mic down: `pactl unload-module module-remap-source; pactl unload-module module-null-sink`
- release: `scripts/release.sh vX.Y.Z && tools/validate release --version X.Y.Z`
- До M1 команд в репо нет — все помечены «создать» в `docs/plans.md` (Validation Assumptions).

## Blockers
- 2026-09-22: три engine-теста на CPU с `amx_int8` помечены `xfail(strict=True)` — CI больше не красный из-за раннеров Xeon, а дефект не спрятан: как только распознавание на AMX заработает, тест упадёт «неожиданно прошёл» и пометку снимут. Заказчик просил убрать письма о красном CI.
- **Движок на CPU с AMX/AVX-512 VNNI (Intel Xeon 8573C) возвращает пустой текст** (onnxruntime 1.24.4, int8 GigaAM v3): воспроизведено в CI 14.09 с диагностикой; риск для серверов/VDI; ресёрч → решение архитектора (самопроверка при загрузке + обход) до R2.
- **S2**: нужен один `sudo` заказчика на его машине (положить `.policy`, помощник, создать `/var/lib/astra-voice/staging` 0700) — файлы и команду готовит команда, команда отката прилагается.
- **S1/S4/S5 во Fly**: нужен вход заказчика во Fly-сессию (перелогин через fly-dm); без него Fly-вердикты спайков не закрываются.
- Чистая ВМ ALSE 1.8 (сток Fly) для Ц3 и §14.5 плана #1 — не выделена; нужна к R2 (15.10).
- **Ключи подписи**: офлайн-мастер (cert-only) + подключи S1/S2 генерирует заказчик по `docs/SECURITY.md` (M1); S1 — в GitHub Environment `release`. Без этого R1 (30.09) не подписать — процедура вне кода, нужна дата.
- Согласие ГК «Астра» на имя (L1) — открытый вопрос к G5.

## Audit log
- 2026-09-09: architect-claude — execution-pack собран по синтезу: `docs/plans.md` (M0 спайки с критериями/Fly/ручными шагами заказчика, M1–M11, R1–R3, требования T1 §5 по вехам M1/M2/M4/M6/M7/M8/M0-S2/T3), `docs/status.md`, `docs/test-plan.md` (уровни, фикстуры, smoke, T-01…T-36 дословно, S1–S17 × тест, готовность v0.1/v0.2/v1.0). Код не писался, ничего не устанавливалось, GUI не запускался. Учтены PRD 0.5 / stories 0.4 / backlog 0.4 (US-6.6, US-12.5), появившиеся во время сборки.
- 2026-09-09: architect-claude — по приёмке S3 узким диффом обновлены `docs/plans.md` (M0.S3 `[x]` → `arch/spikes/S3.md`; M0 `[~]`; M2: `intra_op=2`, `allow_spinning=0`, `RLIMIT_AS` 3 ГиБ, дедлайн `engine.load` 10 с, каталог на вариант, без 7 ресемплеров, `vendor/patches/onnx-asr-cancellable.patch`, `tools/benchmark` из `scripts/measure_model.py`; scratch `~/.cache/astra-voice-spike/`; A-01 подтверждено) и `docs/status.md` (Done/In progress/Next).
- 2026-09-09: developer (M1-A) — живой прогон DoD M1 на KDE-сессии заказчика (допуск в чате 16:40), `spikes/m1_live/` (README + 20 файлов `out/`, скриншот окна, `live_kde_summary.json`). Пакет `0.1.0~m1` из apt, схема сессии `AstraDark`. Замеры: клиентская геометрия 900×588 (внешняя 908×620), RSS/VmHWM 167 720 КБ (порог 122 880), 9 потоков, второй запуск 0,152 с код 0, старт поверх устаревших `lock`/`ipc` — успешный. Схему сессии **не менял**: шаг «живая тема» сделан подставным `kdeglobals` (детект верен: `AstraDark`→dark, `AstraLight`→light), потому что окно тему всё равно не получает. Состояние машины до = после (схема, процессы 0, runtime-каталог удалён, `~/.config/astra-voice` пуст). Ничего не коммитил.
- 2026-09-09: developer (M1-A) — по решению Юрки на отчёт Optimizer `spikes/m1_live/rss.md` GUI по умолчанию переведён на программный рендер Qt Quick: `bootstrap.py` для команды `app` (не `worker`/`helper`) до импорта Qt делает `setdefault` для `QT_QUICK_BACKEND=software` и `QT_XCB_GL_INTEGRATION=none`; переключатель `ASTRA_VOICE_RENDER=gl` возвращает аппаратный рендер, заданные пользователем значения не перебиваются. `scripts/astra-voice` больше не дублирует переменные Qt — единственный источник правды `bootstrap.py`. В `app.py` установлен `qInstallMessageHandler`: две ожидаемые строки про OpenGL уходят в журнал на уровне debug, остальные сообщения Qt — в наш журнал, а не в stderr. Замер в изолированном KWin: **software 102 544 кБ** против **gl 177 924 кБ**, окно 900×588 и тёмная тема в обоих режимах, stderr пуст, RMSE картинок 4,5 %. Тесты: unit 146 passed, xvfb 18 passed + 1 skipped (offscreen), `make lint` зелёный.

- 2026-09-14: Юрка — разбор красного CI на `732a116`/`1459e2a` (прогоны 34836423604/34836482501). Job `unit` (debian:12): 35 failed / 2045 passed — обе причины в окружении, не в коде: `test_pill.py::test_real_qml_clears_root_and_presentation_labels` грузит настоящий `qml/Pill.qml` в подпроцессе, а в контейнере нет ни одного `qml-module-*` (`Pill.qml:86: QtObject is not a type`); 34× `test_tray_icons.py::test_empty_theme_falls_back_to_both_files` — нет `libqt5svg5` (плагин `imageformats/libqsvg.so`), `QPixmap.loadFromData(…, "SVG")` пуст. На машине заказчика оба пакета стоят — локально 2083 passed; гейт `scripts/ci_require_imports.py` ловит только Python-импорты, не Qt-плагины/QML-модули (тот же класс, что урок `.patches/005`). Job `engine`: **плавающий сбой** — на `732a116` три теста `test_engine_gigaam.py` вернули пустой текст (`'проверка' in ''`) при кэше модели с проверенным sha256 и тех же колёсах; на `1459e2a` (тот же код, минутой позже) 11 passed; то же было 2026-09-11 на `e117e1b`. Гипотеза: зависимость int8-ядер onnxruntime 1.24.4 от CPU раннера GitHub (парк смешанный: AMD EPYC avx2 без VNNI / Intel Xeon avx512+VNNI); машина заказчика — Intel Core Ultra 7 255U, avx2+avx_vnni, без avx512 — распознаёт всегда. Решение: пакеты в job `unit`, явный `libqt5svg5` в `Depends`, гейт Qt-окружения в `tests/conftest.py`, диагностика CPU в job `engine` и в сообщениях assert'ов; assert'ы не ослаблять. Пакет `0.1.0~m4` собран на хосте (`--host`, все гейты пройдены: sha256 VAD, ELF 3, Р3, SBOM 31 компонент), скопирован в `~/Desktop`; после правки `Depends` пересобрать.

- 2026-09-14: Юрка — **пробел M4, найден при подготовке пробы заказчиком**: GUI (`DictationRuntime.start()`) никогда не шлёт `model.load`; единственный отправитель — отладочный `--debug-transcribe` (`app.py::_debug_model_request`); воркер на `recognize` без движка отвечает `no-model`. В сборке `0.1.0~m4` хоткей запишет звук, а распознать нечем; `dictate50.sh` («дождитесь загрузки модели») невыполним. Каталог/онбординг — M5, но цикл диктовки без загрузки модели живьём не принять. Решение: минимальный мостик — общий построитель запроса `core/model_request.py`, `model.load` при старте и после перезапуска воркера по `model_dir`/`model_id`+`model_revision` из `settings.extra`, пилюля `loading-model`/`error` простым языком, без UI выбора модели (это M5). Сборка `0.1.0~m4.1`.

- 2026-09-14: Юрка — CI на `514956a` **зелёный 6/6** (run 34843657009): job `unit` с гейтом Qt-окружения, `engine` 11 passed. Первый факт для гипотезы о плавающем сбое: зелёный `engine` шёл на **AMD EPYC 7763, 4 vCPU, SIMD только avx2** (без VNNI/avx512). Значит, если сбой зависит от CPU, красные прогоны (`732a116`, `e117e1b`) были на Intel Xeon (avx512+VNNI); подтвердится строкой «CPU раннера» на следующем сбое.

- 2026-09-14: Юрка — пилюля: DesignReviewer **PASS** (0 расхождений; «›» в `error` как «×»; снимки детерминированы), ревью Claude PASS (два minor в тесте закрываются перед коммитом). Отложенные nit вне дифа: `src/astra_voice/ui/pill.py` `_visibility_changed` падает `RuntimeError: wrapped C/C++ object … QQuickView has been deleted`, если `QQuickView` разрушен раньше `Pill` (порядок разборки в тестах правильный, в программе проверить при shutdown — M5/M11); minor 3 ревью CI (`qml-module-qtquick-window2`/`qtgraphicaleffects` не подтверждены гейтом).

- 2026-09-14: Юрка — **плавающий сбой `engine` воспроизведён с диагностикой** (run 34844710157 на `6f335f9`, тот же код, что зелёный 34843657009): 3 теста `text=''`, `infer_ms≈200`, `cancelled=False`, providers CPU; **CPU раннера — Intel Xeon Platinum 8573C** (amx_int8/amx_bf16/amx_tile, avx512f/bw/cd/dq/vl, avx512_vnni, avx_vnni); зелёный прогон — AMD EPYC 7763 (только avx2); машина заказчика — Core Ultra 7 255U (avx2+avx_vnni). Вывод: не гонка и не кэш — неверный результат квантованных ядер onnxruntime 1.24.4 на пути AMX/AVX-512 VNNI. **Продуктовый риск**: серверы/VDI на Xeon Sapphire/Emerald Rapids дадут пустое распознавание молча. Дальше: ресёрч (известные баги MLAS, ручка отключения AMX, чистые версии ORT), затем решение архитектора — самопроверка при загрузке модели (короткий эталонный WAV → непустой текст, иначе fail-closed с понятной ошибкой/обходной путь). CI: job `engine` будет красным примерно в половине прогонов до обхода — известное состояние, не маскировать.

- 2026-09-14 17:40: Юрка — **проба заказчика m4.1: диктовка не поднялась.** Журнал: «Не удалось запустить диктовку…» без traceback. Причина: в установленном дереве `from astra_voice import bootstrap` (`worker/supervisor.py:17`, с M2) падает — `rules` переносит `bootstrap.py` из пакета в `/usr/lib/astra-voice/bootstrap.py`. Dev-дерево и все стенды зелёные; job `deb` проверял только `--version`. Пакеты M2–M4 были неработоспособны незаметно. Починка → `0.1.0~m4.2` + смоук импорта всех модулей из распакованного пакета в CI и в `build-deb.sh`; урок 008.

- 2026-09-14 17:40: Юрка — CI: на `b80d477` job `xvfb` завис на 47 мин без вывода после `xvfb-run … pytest -m xvfb` и держал очередь main (concurrency; `e4e0ea7`/`82ea580` вытеснены как pending) — прогон снят вручную, причина не найдена (журнал пуст), на `673ee03` xvfb прошёл за 76 с; решение — `timeout-minutes` на все job'ы. На `673ee03`: `unit` 1 failed / 2214 — `test_worker_main.py::test_eof_closes_audio_and_socket` (`thread.join(1)` на загруженном раннере — флак по времени, класс 005/test_supervisor; в починку); `engine` красный на Xeon 8573C (AMX, известно).

- 2026-09-14 18:05: Юрка — починка раскладки сдана (`supervisor.launcher_path()` без импорта `bootstrap`, traceback в журнале, `packaging/smoke-installed.sh`: 41 модуль + `--version` в `build-deb.sh` и job'ах `deb`/`release`, AST-гейт `test_deb_layout_imports`, `timeout-minutes` ×7, флак EOF-теста → `wait_until` 20/20; стенд из распакованного m4.2 — модель за 873 мс). Попутно найден второй дефект класса 008: `rules` удаляет `data/icons`, а трей во Fly читает SVG именно оттуда → во Fly трея из пакета не было бы; чинится в m4.2 (поиск в `/usr/share/icons/hicolor`, затем dev-каталог) + смоук ресурсов. Урок 008 дополнен правилом 5.

- 2026-09-14 18:30: Юрка — пакетная починка закоммичена `ae3e021` (ревью Claude №4 PASS: собрал установленное дерево по `rules`, отрицательные случаи смоука и AST-гейта подтверждены). `0.1.0~m4.2` (sha256 32270b9c…) на `~/Desktop`; заказчику — install + запуск. Отложено (nit): `icon_theme_dir()` в dev над репо; гейт раскладки видит только `mv`; регэксп рецепта; иконка приложения в смоуке.

- 2026-09-14 18:35: Юрка — CI на `ae3e021` (run 34861452945): 5/6 зелёные, включая `deb` с новым смоуком установленного дерева и `xvfb` за 66 с; `engine` красный — третий CPU с AMX: **Intel Xeon 6973P-C** (Granite Rapids; amx_int8, avx512_vnni), `text=''` при `infer_ms` 150–201 — тот же дефект MLAS ≤ 1.24.4, третий независимый образец (8573C ×2, 6973P-C ×1; зелёные — только EPYC 7763).

- 2026-09-14 18:50: Юрка — **проба заказчика m4.2**: старт ок (трей зарегистрирован, модель загружена за 767 мс), но нажатие хоткея (18:45) → `WARNING core.dictation: отправка команды не удалась` без подробностей. Воспроизведено на установленном пакете: `record.start` собирается с `device: None`, а схема `ipc.py` принимает `device` только строкой → `FrameError «Неверный тип поля device»`; `limit_s/silence_db/insert` схема отбрасывает, воркер их не читает. Unit-тесты оркестратора подменяли `_send` — настоящий `ipc.encode` на сообщениях GUI не выполнялся (класс урока 007). Починка → `0.1.0~m4.3` (поле только строкой, `exc_info`, контрактный тест всех сообщений GUI через настоящий encode).

- 2026-09-14 19:10: Юрка — CI на `11e91bd` (m4.3) **зелёный 6/6** (run 34866767083; `engine` попал на AMD-раннер). `0.1.0~m4.3` на `~/Desktop`, ждём пробу заказчика.

- 2026-09-14 19:15: **первая живая диктовка у заказчика — работает** (`0.1.0~m4.3`, KDE, Handy остановлен): фраза «Раз, два, три, четыре, пять.» вставлена в окно чата Astra Cowork по `Ctrl+Space` (PTT); слово заказчика «работает!». GUI 87880 + воркер 87888 живы; `--stats` ещё пуст (таймер записи 30 с). Приёмка Ц2 (50 диктовок, p95 ≤ 0,5 с) и T-50/T-56/T-58 — впереди, по «ок» заказчика.

- 2026-09-14 19:28: Юрка — **живой прогон Ц2 запущен** («ок» заказчика): `virtual_mic.sh up` → Kate (новый документ, фокус через xdotool) → `dictate50.sh --session kde --yes` (оценка скрипта 19 мин: сброс статистики раз в 30 с). Замечание к e2e: `virtual_mic.sh up` создаёт модули, но **не переключает** системный вход; сценарий ждёт, что в программе выбран `av_test_src`, а в `settings.json` ключа `device` нет и на лету он не перечитывается → диктовка 1 писалась со встроенного микрофона; переключил `pactl set-default-source av_test_src` вручную на ходу (возврат — `down`). Починить: `up` переключает вход или `dictate50 --yes` пишет `device` (ключ читается только при старте программы — тоже дефект). Моя оболочка: `XDG_RUNTIME_DIR`/`DBUS_SESSION_BUS_ADDRESS` искажены суффиксом — для прогона заданы явно.

- 2026-09-14 19:33: Юрка — прогон Ц2 прервался на диктовке 1 (EXIT 2, машина восстановлена: `virtual_mic down`, вход Mic1): программа не видит `xdotool keydown ctrl+space` — 12 мс между Control и пробелом мало для грабa; с `--delay 100` и с раздельными нажатиями запись начинается (проверено 3 контрольными нажатиями: «пусто»/ok со встроенного микрофона, t 262–264 мс). Второй пробел e2e: `virtual_mic.sh up` не переключает вход. Правки сценариев → developer-codex; повтор прогона после них.

- 2026-09-14 20:01: **живой прогон Ц2 (попытка 2) завершён**: 50/50 ok, 0 пусто, 0 ошибок микрофона; t_ms min 251 · p50 269 · p90 522 · **p95 523 (порог 500) · max 777**; вердикт «не уложились» на 23 мс. Распределение бимодальное с шагом ~250 мс (34 × 251–270, 16 × 502–523, 2 × ~770) — квантование по тику (`worker/main.py POLL_INTERVAL = 0.25`?), не шум. **M4 остаётся `[~]`**: debugger → фикс → повтор Ц2. Доказательства: `spikes/m4_live/`. Машина восстановлена (`virtual_mic down`, вход Mic1).

- 2026-09-15 12:20: Debugger — **причина хвоста Ц2 доказана**: `worker/main.py` `select(…, POLL_INTERVAL=0.25)` не будится событиями из потоков executor/захвата (`SimpleQueue`), `result` уходит по таймауту → `t_ms ≈ 22 + k·250` (6 с WAV: k=1/2 в зависимости от R≈170–230 мс; 20 с: k=3). Трейс с метками на копии пакета в изолированном стенде; H2 (Xlib/тик 200 мс), H3 (таймеры), H4 (вставка — вне t_ms), H5 (VAD) отвергнуты. Фикс — self-pipe (`socketpair` в `select`), на копии p50 194 / p95 241 без ступени. Sol (xhigh) согласен. Побочно: сторож отмены `engine.py:567–582` `cancel.wait(0.05)` не будится `stopped` (до 50 мс), join захвата до 20 мс, SIGSEGV на SIGTERM в стенде («shared QObject was deleted directly») — отдельные задачи. → developer-codex `0.1.0~m4.4`.

- 2026-09-15 12:40: Юрка — фикс хвоста сдан и принят: self-pipe в `WorkerLoop` (`socketpair` в `select`, `_put_event`), 3 регресс-теста (доставка `result` из фонового потока 0,1–0,3 мс против 250,3 на старом коде; переполнение будильника; закрытие fd), 2244 unit; ревью Claude №6 PASS (гонка на закрытии недостижима: `worker.close()` дожидается захвата и executor до закрытия fd; ёмкость socketpair для однобайтовых посылок 278 байт → одного `recv(4096)` достаточно; T2 не нужен). `0.1.0~m4.4` собран, гейты зелёные → `~/Desktop`; **повтор Ц2 у заказчика — по «ок»**. Отложено (nit): TODO-якоря на `.patches/009` вместо status.md; тест на осушение при BlockingIOError; fd в `__init__` до `AudioCapture`.

- 2026-09-15 12:30: Юрка — CI на `10f55f1` (m4.4) **зелёный 6/6** (run 34952657851). Ждём установку m4.4 и «ок» заказчика на повтор Ц2.

- 2026-09-15 12:43: Юрка — заказчик поставил m4.4 после перезагрузки машины (uptime 12:41); Handy автостартовал (12:41:38) раньше `astra-voice` (12:42:41) → «Хоткей не захвачен (busy)», трей NOKEY, модель загружена за 941 мс. **Продуктовый пробел (US-1.5b/US-1.9)**: программа не пытается перезахватить хоткей после освобождения клавиши — нужен перезапуск; в M5 — повторная проба захвата по таймеру/по действию из трея и онбординг поверх Handy. Для повтора Ц2: остановить Handy, перезапустить программу.

- 2026-09-15 13:15: **Ц2 на `0.1.0~m4.4` — PASS**: 50/50 ok, p50 233 · p90 285 · **p95 291 (порог 500)** · max 329, ни одной > 400 мс, paste p50 159; было (m4.3) p50 269 / p95 523 / max 777 — ступень +250 мс исчезла (урок 009). Машина восстановлена (`virtual_mic down`, Mic1). Доказательства `spikes/m4_live/`. До **M4 ✅** остались ручные T-50/T-56/T-58 и пробник `Ctrl+Alt+Space`.

- 2026-09-15 13:40: Юрка — ручные проверки M4 (по «давай» заказчика). **Пробник S18-A9**: на живом KDE при остановленном Handy `Ctrl+Alt+Space` — **занята** (как и `Alt+Space` = KRunner), `Ctrl+Shift+Space`/`Ctrl+Alt+D`/`Ctrl+Alt+V` — свободны; в чистой X (изолированный стенд) с нашим захватом `Ctrl+Space` (маски [4,6,20,22]) `Ctrl+Alt+Space` свободна → блокирует не Handy (S4 ошибался) и не мы; в `kglobalshortcutsrc`/kglobalaccel/`keyshortcutrc` записи нет; кандидаты — демоны Fly в KDE-сеансе (`fly-notifications`, `fly-getexe`, `fly-dm`, `fly-shutdown-sc…`), владелец не установлен. Для F17: дефолт «Команда» `Ctrl+Alt+Space` на этой машине занят → живой пробник в онбординге обязателен (US-1.5b), PM — пересмотреть дефолт (кандидат `Ctrl+Shift+Space`). **T-58 (путь вставки)**: после 100 диктовок через буфер в истории Klipper **0** записей с тестовой фразой (23 записи, все чужие), текущий буфер несёт `x-kde-passwordManagerHint` — У11/У12 подтверждены живьём; пункт «Скопировать последний текст» — ждём клик заказчика.

- 2026-09-15 13:30: Юрка — **T-58 PASS** («Скопировать последний текст»: буфер несёт последнюю фразу + `x-kde-passwordManagerHint`, история Klipper 23 записи, с фразой 0). **T-56 (микрофон пропал во время записи, живьём через выгрузку виртуального источника)**: ошибки `audio-no-device` НЕ было — PipeWire молча перевёл поток на устройство по умолчанию (встроенный микрофон), запись продолжилась (audio_ms 4210, результат `empty`, mic_errors 0); хоткей не залип, `Escape` и второе нажатие работают (вторая диктовка началась штатно). **Находка для ИБ/У44–У45**: смена устройства «на лету» не замечается и не сообщается пользователю (У57-путь `audio-no-device` на PipeWire практически недостижим для «пропал микрофон») — вопрос security-analyst/M5: проверять имя устройства потока после `audio.ready{changed}` и/или показывать «микрофон сменился». **Клавиша Win (вопрос заказчика)**: одиночный `Super_L` на живом KDE захватывается (XGrabKey ок), но тап по Win — меню приложений KDE (ModifierOnlyShortcuts) и старт-меню Fly; `Super+Space` свободна и в KDE (живой пробник), и во Fly (`keyshortcutrc`: Mod4 только F5/Shift+</>); `Super+V`/`Super+D` заняты KDE. Рекомендация для F17: дефолт «Команда» = `Win+Space` (или `Ctrl+Shift+Space`), одиночный Win — нет; решение заказчика/PM.

- 2026-09-15 13:36: **T-50 PASS** (живьём): полоска выключена (`pill_enabled=false` в файле — переключатель в окне настроек значение **не сохраняет**, mtime файла не менялся: пробел окна настроек → M5), перезапуск, запись без полоски → `kill plasmashell` → **полоска 224×78 всплыла ≤ 0,5 с**, держится; после `systemctl --user start plasma-plasmashell` трей перерегистрировался (13:35:48). Настройки и звук возвращены, программа перезапущена в обычном режиме. **Все четыре ручные проверки закрыты → M4 ✅.** Открытые находки: У44 (смена устройства на лету, PipeWire) — security-analyst; окно настроек не сохраняет; перезахват хоткея после Handy; Win как клавиша «Команда» (решение заказчика, спайк S7).

- 2026-09-15 14:20: Юрка — **M5 стартовал**: PM (PRD 0.8) + три зоны developer-codex (A сеть/модели, B QML онбординга, C рантайм/мосты) с фиксированными контрактами (см. HEARTBEAT); спека §10 есть, референсы 08-onboarding ×10, 10-dialogs ×2.

- 2026-09-15: Юрка — **M5, код трёх зон написан и зафиксирован в `main` до ревью** (сессия остановлена по просьбе заказчика: «ближайшие шаги сделай и останавливай работу»). Зоны `developer-codex` (GPT-6 Astra, high/xhigh, `-s workspace-write`, сеть в песочнице не включалась, git не трогали) шли параллельно с 14:20; в 15:10 все три упали по лимиту API (429, сброс 16:30) и были возобновлены в 16:33 с указанием проверить фактическое состояние файлов — процессы `codex exec` были убиты вместе с агентами. **Зона A** (~5150 строк): `net/{gate,http}.py`, `models/{catalog,store,downloader,installer}.py`, подписанный `data/catalog.json` (тестовый ключ спайка S2, закрытая часть только в `~/.cache/astra-voice-spike/gpg/`), `tools/validate downloads|privacy`, 514 тестов; сама поймала четыре дефекта, которых не видел прогон микрошага (см. `.patches/010`), включая **дыру в сетевом шлюзе**: `policy.conf` отдаёт `offline` строкой `"true"`, код сравнивал с булевым `True`, политика «офлайн» сеть не закрывала. **Зона B**: `qml/onboarding/` (5 шагов + обрамление §10), 5 компонентов, 45 тестов на виртуальном экране, 12 детерминированных снимков в `design/refs/impl/onboarding/`. **Зона C**: `ui/bridges.py` (`SettingsBridge` F11.7 + `OnboardingController` шаги 1/2/3/5), `core/model_source.py`, смоук О2 с вшитым эталоном `data/smoke/`, F4.9 самопроверка модели, F2.11 автоповтор захвата раз в 30 с (кнопки «Повторить» нет — решение заказчика), F6.5 стоп записи при смене микрофона. Валидация перед фиксацией (прогон Юрки): `ruff` + `ruff format` + `mypy` зелёные, **3259 unit** (было 2244), **113 xvfb** (было 57), `gen_theme --check` совпал, `tools/validate downloads --faults` 7/7, `privacy --network-denied` 9/9, `build-deb.sh --host` 18 ресурсов, `gpgv` подписи каталога — действительная. **Ревью, ИБ T2 и дизайн-ревью НЕ проводились** — первое дело следующей сессии. Не начато: шаг 4 «Микрофон» со стороны Python (дерево чистое, стартует с нуля), `OnboardingBridge` как контекстное свойство, `docs/ui-bridge.md`. Открытые расхождения: `min_ram_mb`/`variant` в коде (768 / `gigaam-v3-e2e-rnnt`) против PRD §7.3 (1024 / `e2e_rnnt`); трактовка `HF_HUB_OFFLINE=1` как ужесточения (У23) ждёт подтверждения ИБ; У44 честно недостижим со стороны GUI — тихий перевод потока средствами PipeWire не порождает событий воркера вовсе, нужна задача воркеру сверять подпись устройства во время записи. Живая установка заказчика (`0.1.0~m4.4`) не затронута: пакет не пересобирался.

- 2026-09-15: Честное обнаружение смены микрофона в середине записи (F6.5 / У44) со стороны GUI недостижимо — нужна доработка воркера: периодическая сверка подписи устройства потока во время записи с отдельным событием; ветка остановки по таймеру снята как источник ложных срабатываний.

- 2026-09-16: Юрка — **M5, исправления по трём проверкам зафиксированы (`1c22383`)**. Проверки `b8e3a11` шли 15.09 вечером на Opus + Codex (решение заказчика: беречь Fable). ИБ T2 (второе мнение GPT-6 Astra, три находки Astra перепроверены прогонами): блокер 1 — обход шлюза через неподписанную `catalog.schema.json` (jsonschema 4.10.3 резолвит внешний `$ref` собственным `requests.get` мимо `NetworkGate`, воспроизведено при `offline=true` и `HF_HUB_OFFLINE=1`); блокер 2 — капля 1 Б/0,2 с: через 35 с чтение висит, «Отмена» не отменяет, `QThread.wait()` без таймаута вешает выход; major — gzip вопреки `identity` (×1000 по памяти), шлюз на `stored` вместо `effective`, `locked=values` вместо `locked_keys`, права `policy.conf` не проверяются (M7). Ревью кода: BL-1 рантайм без `model_store` + путь `data_dir()/store` против `data_dir()/models` (модель после онбординга не подхватывается никогда); BL-2 Protocol `dir: str` против `Path` — mypy отвергает настоящее хранилище; BL-3 стоп записи по `_t0+0.3` — `audio.ready{changed}` рождается только при открытии устройства, ложное срабатывание при гонке WirePlumber, а настоящая смена (T-56) не ловится вовсе, тест на невозможных данных (урок 007); BL-4 успех перебит на ERROR; V-1 `NO_PROXY` не действует при `trust_env=False`; V-5 эталон 6 с при норме ≤ 2 с, провал самопроверки необратим; V-9 выход висит до 70 с; V-13 три обязательные кнопки уведомлений отсутствуют. Дизайн-ревью: FAIL 6/6 (превью пилюли на шаге 5 пропало вместе с карточкой автозапуска; «Источник:» вместо «415 МБ ОЗУ · замерено»; композит рамки в тёмной теме на два тона темнее; ghost-disabled без фона; 7 геометрических). Tech-writer (`docs/ui-bridge.md`): кнопка «Повторить» уцелела в `General.qml` вопреки решению заказчика. Исправления — три зоны developer-codex параллельно, коммит `1c22383` (71 файл, +4020/−548). Валидация: ruff, format, mypy 120, 3757 unit, 113 xvfb, gen_theme, validate 7/7 и 18/18, gpgv, build-deb --host (52 модуля, 19 ресурсов). Повторные проверки только по фиксам запущены 13:25. Открыто: отмена внутри sha256 (зона A); «Подробности» не открывает раздел «Отладка»; честное обнаружение смены микрофона — задача воркеру (см. plans). Пакет `0.1.0~m5` собирать после повторных проверок.

- 2026-09-16 18:20: Юрка — **`0.1.0~m5` собран (`92ea477`) и выложен на `~/Desktop`**, живой проверки ещё не было. День: проверки 2–4 (ИБ снял блок; ревью 4 блокера цикла 2, блокер «голая клавиша» цикла 3, `preparing` круга 4; ИБ круга 4 — `cancel()` после UI-портов, голая клавиша ниже моста, `Text.AutoText` → HTTP-запрос от Qt мимо шлюза (PoC); дизайн — 18 из 48 референсов протухли, макеты приведены к токенам, набор переснят, `reshoot.py` в репо) → циклы фиксов 2, 3 и финал тремя зонами developer-codex. Уроки 011–014. Валидация: 4269 unit / 141 xvfb / refs 48/48 / build-deb. Открыто: живая проверка (12 пунктов + чеклист ИБ), `xvfb` на машину (sudo заказчика), «занята в KDE», хвосты M5 в `plans.md`, T-64.

- 2026-09-16 19:00: Юрка — **живая проверка `0.1.0~m5` у заказчика нашла блокер за 20 минут**: онбординг открылся чисто, модель скачалась (226 МБ, sha256 сошлись), но пробное распознавание провалилось — `worker/engine.py check_layout` отвергает каталог модели из-за **нашего собственного** `state.json`, который туда пишет `models/store.py` (`ExtraFileError`, защита У6 с M2). Скачанная модель не загрузится никогда; каталог побайтно равен рабочему каталогу спайка, разница только в служебном файле. Не поймали, потому что в тестах установщика пробное распознавание — заглушка, а ручные проверки шли на каталоге спайка. Сопутствующее: `warning` без `exc_info` (урок 008 правило 3), текст пользователю про администратора вместо «движок не загрузился». Фикс + `0.1.0~m5.1` — первым делом. Замечания заказчика по интерфейсу (карточка «Занимает места», проверка микрофона без модели, заслон на шаге 2, прогресс загрузки на всех шагах) — в `decisions/log.md`.

- 2026-09-21: Юрка — **M5 доведён до `0.1.0~m5.4`**. Волна 1 (мосты, хвосты блокера, макеты) и волна 2 (экраны QML) сданы;
  ревью кода (MAJOR-1/2 + 10 minor) и ИБ T2 (M1/У62 + У63–У69) закрыты волной фиксов `8f977c5`; экраны `e131b3b`.
  Живая проверка у заказчика: **диктовка по `Ctrl+Space` работает**, автоперезахват клавиши (F2.11) сработал вживую,
  `m5.2` установлен без диалога `needrestart`, Astra Voice в автозагрузке вместо Handy (шаг к Ц1).
  **Две находки второй машины:** пакет не ставился без `python3-jsonschema` (проверка схемы вынесена за фасад,
  пакет в `Recommends` — `da7b6cd`); значок трея не отрисовывался на тёмной панели (`8a22910`). Оба дефекта видны
  только на чужой машине — свою мы годами «долечивали» вручную.
  **Два дефекта оснастки** найдены и закрыты по ходу: сторож `pulseaudio` ронял весь прогон (1601 ошибка, урок 015),
  тест трея держал `Mock` без `tooltip`. Codex исчерпал лимит аккаунта до 24.09 — остаток волны фиксов, экраны,
  обе находки и сборки доделаны Юркой вручную.
  Решения заказчика: согласие на имя есть (он CPO «Астры»); громкость микрофона — в состав программы тремя
  ступенями (PRD 0.9; ступени 1–2 → M6, тумблер → v1.0); ИБ-проверки не запускать до четверга.

## Smoke / demo checks
- [~] M0: `arch/spikes/S*.md` — 5 вердиктов; S1/S4/S5 отдельно для KDE и Fly; S3 `[x]` (таблица p95/VmHWM на 1.24.4 — `arch/spikes/S3.md` §4).
- [~] M1: живой прогон на KDE заказчика 2026-09-09 (`spikes/m1_live/`, лог `out/live_kde_*`): `--version` и `apt install` — PASS; окно **900×588** клиентской + рамка 4/4/28/4 = **908×620** внешней, `WM_NAME`/`_NET_WM_NAME` «Astra Voice», `_KDE_NET_WM_DESKTOP_FILE=astra-voice`, `min 900×588`, журнал «сеанс=KDE политика=absent», QML-warnings 0; single-instance — код 0 за **0,15 с**, «получена команда show» в журнале, процессов 1; CI на `e03d6ae` зелёный (run 34358041504). **RSS закрыт** программным рендером (`spikes/m1_live/rss.md`): 167 740 → **102 580 кБ**, запас 16,5 % до порога 122 880. **Не закрыто:** окно светлое при схеме сессии `AstraDark` (`themeSource` не подан в QML); `show` не разворачивает свёрнутое окно; процесс переживает закрытие окна; `settings.json` не создаётся. Восемь дефектов — `spikes/m1_live/README.md`.
- [~] M2: живой прогон DoD 2026-09-11 — распознавание, отмена и `kill -9` воркера/GUI выполнены; пункт про `RLIMIT_AS` выполнен частично. Факты и расхождения — в разделе «M2. Живой прогон Definition of Done» ниже.
- [ ] M3: виртуальный микрофон → `result`; тишина → `silent`; 0 аудиофайлов.
- [ ] M4: 50 диктовок в Kate (KDE) и `fly-term` (Fly) — p95 ≤ 0,5 с; фокус на месте; маркерная фраза нигде не осела.
- [ ] M5: чистый профиль → онбординг → скачивание → первая фраза; `tcpdump` 0 до тумблеров; секундомер Ц3.
- [ ] R1: `v0.1.0` в GitHub Releases с подписью; заказчик диктует вместо Handy; E2E-чек Fly (Should).
- [ ] M6–M9 / R2: каталог 12 карточек, ≥ 3 замера; апгрейд через одно окно polkit; автозапуск после перелогина (KDE+Fly); T3; P21½ в обеих сессиях.
- [ ] M10–M11 / R3: `profile=secure` → 0 соединений и 6 карточек; S17 A1–A9; DesignReviewer 24×2; soak 8 ч; G5.

## M2. Живой прогон Definition of Done

**2026-09-11, машина заказчика, офлайн.** Изолированное окружение: свои `XDG_RUNTIME_DIR`, `XDG_CONFIG_HOME`, `XDG_DATA_HOME`, `XDG_CACHE_HOME` во временном каталоге; `QT_QPA_PLATFORM=offscreen`, дисплей `:0`. D-Bus и микрофон не затрагивались.

Модель: `gigaam-v3-e2e-rnnt` int8 из `~/.cache/astra-voice-spike/gigaam-v3/e2e_rnnt`. Интерпретатор: `~/.cache/astra-voice-dev/venv-e` (`onnxruntime` 1.24.4, `onnx-asr` 0.12.0). Результаты получены живым прогоном агента-разработчика 2026-09-11 на машине заказчика в изолированном окружении. Сырые логи каждого сценария лежат в `spikes/m2_live/out/`: `01-debug-transcribe.log`, `02-serve.log`, `02-kill-worker.log`, `03-kill-gui.log`, `04-worker-cancel.log`, `05-rlimit-load.log`, `05-rlimit-load-full.log`, `06-cpu-wall.log`, `07-soak.log`.

### Факты прогона

| пункт DoD | факт | вердикт |
| --- | --- | --- |
| CLI: распознавание файла | `--debug-transcribe data/test/test-ru-6s.wav --model-dir <модель> --variant gigaam-v3-e2e-rnnt --threads 2`: напечатано «Проверка связи. Проверка связи. Проверка связи.», `t_ms=157`, полный цикл процесса 1,108 с, код возврата 0. Контрольный прогон оркестратора: 206 мс, 1,14 с. | Выполнено. |
| `kill -9` воркера: рестарт, перезагрузка модели, одна ошибка; `oom_score_adj` | У живого воркера `oom_score_adj` = 500. Новый pid поднялся не позднее чем через 0,0033 с при пороге ≤ 1 с. Модель перезагружена: `load_ms=719,7` в новом поколении 2. Событий `error` наружу ровно одно: `worker-crashed` «Распознавание перезапущено». | Выполнено. |
| `kill -9` процесса-GUI: завершение воркера (У36 / T-34) | Воркер исчез за 0,052 с при пороге ≤ 1 с; осиротевших воркеров 0. | Выполнено. |
| Отмена ≤ 1000 мс | Создан `tools/validate`, подкоманда `worker-cancel`. От `send(record.cancel)` до события `cancelled` — 0,419 мс, код 0. Способ: `transcribe.file` на `data/test/test-ru-20s.wav`. Для сравнения S3 давал 0,5–4 мс. | Выполнено. |
| `RLIMIT_AS`, `threads=4`, дедлайн загрузки 10 с | 768 МиБ (значение из плана) — модель загрузилась за 0,708 с, дедлайн не сработал; 700 МиБ — молчаливое зависание, дедлайн выдал `error{code=load-timeout}` за 10,0005 с; 640 МиБ — модель загрузилась за 0,707 с; 512 МиБ — `error{code=engine-failed}` за 0,184 с; 384 МиБ — `error{code=engine-failed}` за 0,124 с; 256 МиБ — `error{code=engine-failed}` за 0,0067 с. | Частично. Смысл требования «не зависает, отказ виден» выполнен на всех пределах. Ошибка по дедлайну на 768 МиБ не воспроизводится; на 700 МиБ механизм доказан сквозным сценарием. |
| `cpu/wall` ≤ 2 при `threads=2` | `cpu_ms=880,0`, `wall_ms=751,4`, отношение 1,171. Загрузка модели 703,8 мс. Движок: `onnx-asr` 0.12.0 / `onnxruntime` 1.24.4, режим отмены `fallback-wrapper`. Интервал включает чтение WAV и доставку результата по IPC, а не только инференс. | Выполнено. |
| Долгий сценарий: память и число сессий | 20 подряд распознаваний 6-секундного файла в одном воркере: `VmHWM` 392212 → 412844 кБ (прирост 20632 кБ), `Pss` 378781 → 399350 кБ, `sessions` 3 → 3 (ожидание 3). `sessions` — снимок `LoadResult` последней загрузки, а не обход живых объектов. Очистка буферов PCM снаружи не наблюдаема и покрыта unit-тестом `test_soak_releases_pcm`. | В снимке число сессий соответствует ожиданию. Порог прироста памяти в плане не задан; вердикт по нему не выставлен. |
| Время запроса в долгом сценарии | `t_ms`: медиана 156 мс, p95 207 мс. Это время всего запроса, не чистого инференса. | Факт записан; пункт DoD про p95 инференса этим замером не подтверждён. |
| Холодная загрузка: уточнённый критерий | По решению от 2026-09-11 «M2: DoD по холодной загрузке исправлен по факту; отмена работает и без вендорного патча» в [журнале решений](../decisions/log.md) новый критерий — 3 `InferenceSession` вместо 10 и холодная загрузка ≤ 1,0 с. Старый пункт плана «холодная загрузка ≤ 0,3 с быстрее S3» снят как ошибочный: расчёт исходил из затрат на 7 сессий ресемплера, а замер показал, что загрузку держит энкодер. Четыре замера: 693,5 мс, 700,9 мс, 703,8 мс и 719,7 мс (последний — перезагрузка после `kill -9`); число сессий 3. Новый критерий выполнен с запасом. | выполнено (по уточнённому критерию) |
| Сборочные проверки | `make lint` зелёный: ruff, ruff format 84 файла, mypy 50 файлов. `make test`: 504 passed, 25 deselected. `pytest -m engine`: 6 passed (тесты зоны B). | Проверки зелёные. |
| T-21 и T-34 | T-21 — зелёный: `tests/unit/test_ipc.py`, 83 теста, плюс негативные сценарии супервизора: кадр 2 ГиБ, чужой `utterance_id`, `text` 50 МБ. T-34 подтверждён живым замером 0,052 с при уничтожении GUI. | Выполнено. |
| T-10 и T-11 | Зона B; в этот живой прогон не входили. | Вердикт этого прогона не выставлен. |

### Расхождения с планом и оговорки

**`RLIMIT_AS` 768 МиБ.** Буквальная формулировка плана — «под 768 МиБ при 4 потоках завершается ошибкой по дедлайну» — на этой ревизии **не воспроизводится**. После того как зона B убрала лишние сессии ресемплера (3 `InferenceSession` вместо 10), потребление упало и 768 МиБ модели теперь хватает. Формулировку плана надо уточнить по факту: предел, на котором воспроизводится зависание, сместился вниз. Риск S3-R1 (молчаливое зависание) воспроизведён на 700 МиБ; именно там доказана работа дедлайна сквозным сценарием. Поведение немонотонно по пределу: 700 МиБ — зависание, 640 МиБ — загрузка. Граница узкая, исчерпывающий поиск не проводился.

**Команда Validation.** `pytest -m unit tests/worker tests/unit/test_supervisor.py` ссылается на каталог `tests/worker`, которого нет. Unit-тесты воркера лежат в `tests/unit/`: `test_ipc.py`, `test_worker_state.py`, `test_worker_main.py`, `test_supervisor.py`. Команда в плане оставлена без изменения.

**CLI и p95.** `astra-voice --debug-transcribe` готов и прогнан; `tools/validate worker-cancel` создан и прогнан. `tools/benchmark` в этой вехе не создавался — это зона движка (зона B), поэтому маркер CLI остаётся `[~]`. P95 инференса зона B записала в `arch/spikes/S3.md`. P95 этого прогона, 207 мс, — время всего запроса через IPC; напрямую сравнивать его с p95 чистого инференса нельзя. Пункт про выгрузку по простою также остаётся `[~]`: `ping` и `model.unload` есть, таймер в GUI относится к M4.

**«Ошибка показана один раз».** Стенд изначально ожидал событие `error` со штампом старого поколения, а реализация штампует его новым поколением. Требование DoD выполнено: наружу поступило ровно одно событие. Штамп новым поколением безопаснее: GUI, фильтрующий события по текущему поколению, такую ошибку не отбросит.

**Долгоживущий GUI для M4.** Путь с загруженной моделью: `spikes/m2_live/dod.py serve --model-dir <каталог> --variant <вариант> --threads 2` под своим XDG-окружением и `QT_QPA_PLATFORM=offscreen`. Он держит супервизор, печатает pid воркера, факты перезапуска и каждое событие `error`.

**Предупреждение об отмене.** На каждом прогоне движок пишет в журнал, что отмена работает через обходной путь (`fallback-wrapper`). Вендорный патч `cancellable()` в dev-дереве не применён. Это ожидаемо и относится к зоне B / сборке пакета.
