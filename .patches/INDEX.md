# Уроки — индекс

> Одна строка на урок: `NNN · тема · ПРАВИЛО`. Читается на старте сессии вместо всех файлов.

| 001 | агенты пишут артефакт в конце, обрыв теряет всё | в брифе: писать файл частями; codex exec → сразу в файл; после рестарта сначала git status/ls |
| 002 | спайк трея мигнул на панели заказчика — изолирован дисплей, не D-Bus | спайки с треем/уведомлениями/хоткеями/polkit — в изолированном X И dbus-run-session; чеклист изоляции в брифе; об инцидентах говорить сразу |
| 003 | параллельные агенты делят scratchpad — перезаписали чужой child.sh, дисплеи пересеклись | временные файлы каждого агента — в свой подкаталог `<зона>-<pid>/` (mktemp -d), сокет `av-<зона>`, в брифе явно |
| 004 | микрофон не сбрасывался между записями — звук из паузы попал бы в следующую диктовку | долгоживущий захват при новом использовании закрывать или сбрасывать (решение — в контракт); тест «две операции подряд в одном процессе»; занятое устройство без индикации = дефект приватности |
| 005 | локально зелено, в CI красно 3/6 — три job в трёх окружениях (без Qt/Xlib/yaml), гейт ловит свою фикстуру | перед сдачей три прогона: venv-a, чистый venv (`--collect-only`, `-m engine`), гейт импортов; потребность в Qt/X — в коде тестов; гейты проверять на чистом venv; переименовал имя — grep по tests/; **доп. 14.09**: требования вне `import` (плагин Qt, QML-модуль) — гейт в conftest, а не строка apt job'а; плавающий сбой CI — сначала диагностика в лог, не retry |
| 006 | пустой текст распознавания при нормальном времени — на Intel Xeon с AMX (дефект MLAS onnxruntime ≤ 1.24.4, PR #27671) | плавающий сбой → диагностика в лог (CPU/версии/repr), не retry; пустой вывод = SIMD-путь, не декодер; нижняя граница версии движка с причиной; ассерт на непустой текст навсегда; самопроверка при загрузке модели (M5) |
| 007 | мостик модели: тест зелёный на невозможных данных (`Settings(extra={"model_id"})`), перенос загрузки на `hello` открыл окно «нажатие до первого события» (микрофон до 120 с), защитный счётчик без выхода | фикстуры только через настоящий конструктор из данных; при событийной инициализации — тест на окно до первого события; блокировать вход, не выход («стоп» всегда проходит); у любой защиты «после N неудач» — жест пользователя; фон не трогает индикатор при занятом оркестраторе |
| 008 | заказчик поставил m4.1 — диктовка не поднялась: `rules` кладёт `bootstrap.py` рядом с пакетом, а `supervisor.py` импортирует его как модуль; смоук установки = `--version`; `except` без traceback | установленный пакет — четвёртое окружение: смоук = импорт всех модулей из распакованного `dist/` + запуск лаунчера (CI job `deb` и `build-deb.sh`); перекладки в `rules` — с тестом на импорт; `except … продолжаем без` — только с `exc_info`; выдача заказчику — после прогона из распакованного пакета |
| 009 | Ц2 p95 523 > 500: `select` воркера (POLL_INTERVAL 0,25) не будится событиями из потоков — `result` уходит по таймауту, ступень ровно +250 мс; тест слал `ping` и маскировал | будильник (self-pipe) в любом select-цикле с межпоточной очередью; тест на задержку доставки ≪ интервала без побочных пробуждений; ровный шаг в распределении = константа таймера; чётко определять, что входит в метрику |
| 010 | бриф микрошага без `mypy` и чистого окружения: `offline="true"` сравнивали с булевым — политика «офлайн» не закрывала сеть; `gpgv` с относительным путём = ложная «плохая подпись» | в бриф каждого микрошага: `mypy` (покрывает tests) + сбор в чистом venv; значения из конфиг-файла проверять через настоящий разбор файла; внешним инструментам — только абсолютные пути; «красное» от песочницы Codex на сети/сокетах — не дефект, полную сюиту гоняет зона на машине |
| 011 | зелёный xvfb на подставном мосте: фейк подгонялся под QML (`endCapture()` без аргумента, `peak`/`testDuration`, `pickInstallPath`), находки tech-writer'а в бриф зоны не попали — «Отмена» на шаге 3 бросала ошибку, «Из файла» тихий no-op, выдуманные «−18 дБ» | контракт QML↔Python — тестом: QML-ссылки ⊆ мета-объект настоящего моста, фейк ⊆ мета-объект; контракт в брифы обеих зон дословно до старта, фейк не расширять; находки проверяющего — в бриф следующей зоны списком целиком; xvfb доказывает раскладку, не поведение |
| 012 | `git add -A` при трёх работающих зонах: три коммита журналов утащили 8 чужих файлов на середине правки и временные каталоги снимков `.pill-*` | пока работает хоть одна зона — коммитить только явными путями, код одним коммитом на цикл после полной валидации; `git status` глазами перед коммитом; временные артефакты тестов — не рядом с эталонами (`.gitignore` + `tmp_path`); зонам по возможности свои worktree |
| 013 | «переснимать только затронутые экраны»: 18 из 48 референсов протухли (8 обрезаны старым инструментом до 40–60 % экрана), общий CSS макетов отстал от токенов по 3 переменным — дизайн-ревью три круга гонялось за макетом | правка общего CSS → переснимать весь набор; перед заменой — побайтовый контроль механизма на непротухших кадрах; признаки старого инструмента (1506 px, ширина ≠ 951/923) в README; при необъяснимой Δ — сверять коммит HTML против PNG; скрипт съёмки в репо |
| 014 | бриф без `offscreen` — агент прогнал xvfb-тесты на живом `:0` заказчика (захват клавиатуры уводил фокус), потом поднял `Xephyr`; попутно `test_pill_states.py` переписал 10 снимков в репо | команда валидации в брифе — только полная копипастом с `QT_QPA_PLATFORM=offscreen`; явная строка «`:0` не трогать, X-серверы не поднимать» в каждом брифе с Qt; тесты-писатели артефактов уважают `ASTRA_VOICE_SNAPSHOT_DIR`; решить про `xvfb` на машине; **агент не запускает GUI проекта вообще** — 16.09 в сеансе заказчика полчаса висел экземпляр из dev-дерева, оставленный агентом, и мешал его запуску |
| 015 | сторож `pulseaudio` в `conftest` читал через `Path.read_text` и падал внутри чужих подмен, а упавшая hook-обёртка сломала сессию: 1601 ошибка, прогон 30+ мин вместо 2 | оснастка не зависит от API, которые тесты подменяют у себя (`os.open`, не `Path.read_text`); сторож в хуке при любой неожиданности отвечает «не знаю» (`return None`), а не роняет чужой тест |
| 016 | прогон unit-тестов записал `catalog-state.json` заказчика с serial рабочего дерева — программа отвергла встроенный каталог; два агента в одном рабочем дереве роняли тесты друг друга, чужая строка попала в коммит | `pytest_configure` уводит все XDG-каталоги во временный корень; параллельные пишущие агенты — только в разных `git worktree`; чужие файлы перед коммитом — `git diff` по каждому |
| 017 | три сигнала с Fly в коде, зелёном на KDE: `_NET_ACTIVE_WINDOW` меняет fly-wm, экран «Общие» не читал `devices` с M1, знак трея тёмный на тёмной панели Fly | ветки Fly — пометка «не проверено вживую» до отчёта с Fly + строка T-xx; тест «экран использует данные моста»; исходы, зависящие от оболочки, — в журнал строкой `key=value` без данных; факты про Fly сначала искать на своей машине (`fly-wm`, `/usr/share/fly-wm`, `~/.fly`) |
| 018 | Codex завис после сна ноутбука (соединение умерло, процесс S/0 %), агент ждал 11 ч; другой агент сторожил Codex по строке в ps и находил себя | ожидание Codex — по pid из запуска и росту лога, 15 мин без роста → kill по pid + `codex exec resume`; после «закрывал крышку» — проверить процессы Codex |
| 019 | `Shape` в программном рендере не обрезался Flickable, целиком ушедшие за край иконки печатались поверх шапки и не стирались (три «✓» на m6.17); 24 снимочных теста зелёные — `QQuickWindow.grabWindow()` рисует кадр заново | векторные элементы внутри `clip: true` — со слоем (`layer.enabled` при Software); снимок «что на экране» — `primaryScreen().grabWindow(winId)`; прокрутка до конца + смена раздела — в чеклист и снимочные сценарии |
| 020 | CI на main красный с `4bf0308` (segfault xvfb-теста после карточки A), ~15 push подряд — письма заказчику; оркестратор после push CI не смотрел, локально offscreen зелёный | после каждого push — `gh run list` по коммиту, красный → debugger и первая строка статуса; ветки с QML — CI на ветке до слияния; статус CI в отчёте сборки deb и в HEARTBEAT |
| 021 | лямбда с замыканием на `engine.destroyed` (провайдер иконок): GC PyQt по совпадению адреса обнулил `__closure__` живой лямбды → segfault при уничтожении движка (CI, и возможно при выходе у пользователя) | не вешать лямбды на сигналы объектов без удерживаемой обёртки; реакция — метод живого владельца или удержание списком + `sip.isdeleted`; ревьюер ищет `destroyed.connect(lambda` |
