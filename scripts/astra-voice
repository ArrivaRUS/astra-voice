#!/bin/sh
# Лаунчер GUI. Ставится сборкой как /usr/bin/astra-voice (см. packaging/build-deb.sh).
# -I: изолированный режим — без PYTHONPATH и без user site-packages (модель угроз У3).
exec /usr/bin/python3 -I /usr/lib/astra-voice/bootstrap.py app "$@"
