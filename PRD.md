# PRD — Astra Voice v1 (голосовой ввод для Astra Linux SE)

> Версия PRD **0.4 (ревизия 4 — Fly-сессия, память при смене модели, манифест onnx-asr)** · 2026-09-09 · ProductManager (команда Юрки) · статус: **принят на G1 (0.2, 2026-09-07)**; ревизии 3–4 — данные каталога (`research/catalog-numbers.md`), требование заказчика «KDE и родной Fly» (`decisions/log.md` 2026-09-09) и правки по синтезу архитектуры (`arch/plan-synth.md` §3 Р5/Р9/Р10, §6; `arch/plan-claude.md` §4.1).
> Тип продукта: **internal / B2B десктоп-утилита** (одиночный пользователь, без сервера, открытый код).
> Входы: `PROJECT.md`, `decisions/log.md` (рамки — не пересматриваются), `research/tech-models-claude.md`,
> `research/tech-platform-claude.md`, `research/competitors-ux.md`, `research/legal.md`; эталон поведения —
> исходники Handy 0.8.3 (`~/Документы/handy-gigaam/vendor/Handy/`); в ревизии 2 учтены `research/tech-codex.md`
> (Researcher #2, GPT-5.6 Sol) и `research/summary.md` (свод Юрки с разрешёнными расхождениями).
> Это договор о том, **что** строим. Как строим — архитекторам (`arch/`), как выглядит — UXAnalyst (`design/`).

---

## 1. Фон и цели

### 1.1 Почему сейчас
На машинах заказчика голосовой ввод — переделанный Handy (Tauri + React + Rust, форк `ArrivaRUS/handy-gigaam`).
Распознаёт отлично (GigaAM v3, фраза 6 с → ~130 мс, пунктуация, «ё»), но это чужой форк: тяжёлая сборка,
которую `tauri build` не собирает, deb перепаковывается руками, английский UI с недопереводом, чужая
айдентика, ярлык автозапуска переписывается при каждом старте (на машине живут два ярлыка), раздел
LLM-постобработки с API-ключами, который в корпоративную сборку нельзя. Заказчик 2026-09-07 попросил
**свою утилиту с нуля под ALSE** с пятью конкретными требованиями (см. `PROJECT.md`). Решение G0 — GO,
Discovery только техническое.

### 1.2 Целевые пользователи (роль · черта · сценарий)
| Роль | Черта | Сценарий |
|---|---|---|
| **П1 «Диктующий сотрудник»** | Русскоязычный пользователь Astra Linux SE 1.8 (KDE Plasma 5 / Fly, X11). Прав администратора **нет**. Машина может быть 4 ядра / 8 ГБ ОЗУ. Интернета может не быть (закрытый контур). | Диктует письма, документы, сообщения в мессенджер, заметки в 1С/браузер. Зажал клавишу — сказал — текст появился там, где курсор. |
| **П2 «Опытный пользователь / заказчик»** | В группе `astra-admin`, ЗПС выключена, есть интернет (через туннель), 14 потоков / 30 ГБ. Пользовался Handy. | Ждёт паритета с Handy по скорости и качеству, сам ставит обновления из GUI, меняет модели, смотрит честные цифры. |
| **П3 «Администратор ИТ/ИБ»** | Разворачивает утилиту на парке ALSE, часть машин с включённой ЗПС, корпоративное зеркало, регламент обновлений по приказам ФСТЭК. Сам не диктует. | Ставит подписанный deb, регистрирует ключ, задаёт политику (офлайн, корпоративный URL каталога), получает документ «влияние на среду функционирования». |

**Не целевые в v1:** пользователи Wayland; macOS/Windows; англоязычные (английский UI — вторым языком, но не целевая аудитория); контуры с гостайной.

### 1.3 Бизнес-цели и метрики (SMART)
| # | Цель | Метрика · способ замера | Срок |
|---|---|---|---|
| Ц1 | Заменить Handy на рабочих машинах заказчика — **в обеих сессиях ALSE: KDE Plasma и родной Fly** (заказчик 2026-09-09) | На 100 % машин заказчика (n ≥ 1) Astra Voice — единственный голосовой ввод, Handy удалён; S17 «Fly-сессия» зелёный на той же машине; фиксируется чеклистом приёмки G5 | v1.0 → **2026-10-31** |
| Ц2 | Паритет скорости с Handy | Задержка «отпустил клавишу → текст в окне» **p95 ≤ 0,5 с** для фразы 6 с на GigaAM v3 e2e_rnnt, модель уже в памяти, эталонная машина заказчика (Core Ultra 7 255U); замер — встроенная локальная статистика `t_ms` (§10) по ≥ 50 диктовкам | v0.1 → **2026-09-30** |
| Ц3 | Быстрый старт нового пользователя | Время от `apt install` до первой вставленной фразы **≤ 5 мин** при сети 10 Мбит/с (скачивание 222 МБ ≈ 3 мин) и **≤ 2 мин** при установке модели из файла; замер — ручной прогон сценария S1 на чистой ВМ ALSE 1.8 | v0.2 → **2026-10-15** |
| Ц4 | Ноль лишней сети | При прогоне P21½ `nethogs`/`tcpdump` фиксируют исходящие соединения **только** к хостам из `PRIVACY.md` (huggingface.co + CDN, api.github.com/github.com, корпоративный URL) и **ни одного** при включённом офлайн-режиме | v0.2 → **2026-10-15** |
| Ц5 | Работоспособность под ЗПС — **после v1.0** (заказчик 2026-09-07: ГОСТ-подпись артефактов и прогон на ВМ вынесены в v1.1) | **1 успешный прогон** «установка по инструкции администратора → диктовка работает» на ВМ ALSE 1.8 (Fly-сессия по умолчанию) с `astra-digsig-control enable` | v1.1 → ориентир **2026-12-31** (допущение A10) |
| Ц6 | Честный каталог | Каталог **≥ 8 моделей** (состав v1 — 9 Must + 1 Should, §7.2); у 100 % карточек — размер, лицензия, язык, статус ОЗУ с пометкой достоверности; **≥ 3 моделей замерены** на эталонной машине (ОЗУ + RTFx) | v0.2 → **2026-10-15** |
| Ц7 | Дешёвое сопровождение | Сборка deb — **одна команда**, ≤ 5 мин на эталонной машине; релиз (тег → deb + SHA256SUMS + подпись → GitHub Release) — по одной инструкции без ручной перепаковки | v0.1 → **2026-09-30** |

### 1.4 Проверка гипотезы (обязательный блок)
- **Гипотеза.** Своя утилита на Python + PyQt5 (apt) + `onnx-asr` даёт тот же пользовательский опыт, что Handy + GigaAM, при на порядок более дешёвой сборке и честном русском UI, и её можно довести до корпоративных ALSE (ЗПС, офлайн).
- **Ожидаемый эффект.** Заказчик пользуется Astra Voice ежедневно вместо Handy ≥ 14 дней без возврата; корпоративный пилот (если будет) не упирается в сеть/права/ЗПС.
- **Метрики и способ замера.** (а) p95 задержки ≤ 0,5 с (локальная статистика); (б) откатов на Handy за 14 дней = 0 (слово заказчика); (в) дефектов P0/P1 после v1.0 за 14 дней ≤ 2 (issues репо); (г) сборка deb — одна команда ≤ 5 мин (Ц7).
- **Срок проверки.** **2026-11-15** (14 дней после плановой даты v1.0). ProductManager возвращается к блоку в срок (P25), Юрка ставит напоминание.
- **Решение при недоборе (названо заранее).** (а) не достигнуто → менять рантайм (`sherpa-onnx` вместо `onnx-asr`) и/или дефолт на e2e_ctc — **развивать**; (б) > 0 откатов → разбор причин, откат на Handy до фикса, повторная проверка через 14 дней — **менять**; (г) не достигнуто → пересмотр упаковки в v1.1 — **менять**; если (а)+(б) провалены одновременно → **убрать**: проект парковать, Handy остаётся.

---

## 2. Обзор требования (≤ 30 слов)
Офлайн-утилита голосового ввода для Astra Linux SE: хоткей → речь → текст в активном окне; честный каталог моделей, обновления модели и утилиты из GUI, автозапуск, русский интерфейс.

---

## 3. Рамки (из `decisions/log.md`, не пересматриваются)
| Рамка | Значение |
|---|---|
| Имя | **Astra Voice**; slug/репо `astra-voice`. Риск товарного знака заказчик принял; задача комплаенса — письменное согласие правообладателя + дисклеймер до публичного релиза (§9.6) |
| Стек | Python 3.11 + PyQt5 5.15 (QtQuick/QML) из apt Astra 1.8; движок — `onnx-asr` (запасной `sherpa-onnx`); всё CPU |
| Лицензия | Репо **GPL-3.0-or-later**; атрибуции компонентов и моделей — `NOTICE` |
| Обновление утилиты | **Оба трека в v1**: без ЗПС — GitHub Releases → .deb → один шаг polkit → apt из GUI; с ЗПС / без прав — уведомление + «скачать пакет и передать администратору». Релизы подписываются (minisign/GPG). **ГОСТ-подпись артефактов для ЗПС, ключ организации и прогон на ВМ с ЗПС — после v1.0** (уточнение заказчика 2026-09-07) |
| Сеть по умолчанию | Тумблеры «проверять обновления утилиты / моделей» **пусты во всех профилях**; пользователь включает сам; требования №1/№4 действуют после включения (заказчик 2026-09-07) |
| Хоткей по умолчанию | **`Ctrl+Space`**, push-to-talk (как в Handy заказчика); toggle — второй режим (Юрка 2026-09-07) |
| Модель в памяти | Держится постоянно; выгрузка по простою — опция (заказчик 2026-09-07) |
| Автозапуск | Включён по умолчанию на последнем экране онбординга; релиз **v0.2** (заказчик 2026-09-07) |
| Индикация записи | Пилюля с визуализацией **по умолчанию, отключаемая**; трей + звук остаются. Цвет «слушаю» — бирюза `#12B3A0`, красный — только ошибка |
| Модель по умолчанию | GigaAM v3 **e2e_rnnt** int8; e2e_ctc — «быстрее на ~25 %» |
| Каталог v1 | ≥ 8 моделей; CC-BY — с атрибуцией; Silero (CC-NC) исключён; зеркало весов в GitHub Releases допустимо; корпоративный URL каталога — в v1 |
| ОЗУ/скорость | Замер на машине после первого прогона (VmHWM воркера) с пометкой «замерено / заявлено / оценка» |
| Вне v1 | Постобработка LLM; история буфера Klipper — Should (защита истории) |
| Мы делаем **свою** утилиту | Берём проверенное у Handy (строка обновления, схема вставки, карточка модели, single-instance), убираем лишнее (LLM, английские хвосты, перезапись ярлыка автозапуска) |

---

## 4. Декомпозиция: роли, объекты, путь пользователя

**Роли.** Пользователь (П1/П2 — один и тот же UI, различие только в правах на установку пакета);
Администратор (П3 — не через UI утилиты, а через пакет, `policy.conf` и инструкцию); Система (фоновые
проверки, воркер распознавания).

**Ключевые объекты и CRUD.**
| Объект | Create | Read | Update | Delete |
|---|---|---|---|---|
| Модель (локальная) | скачать / установить из файла | карточка, статус | обновить до новой ревизии (с откатом) | удалить с диска |
| Каталог (манифест) | — | встроенный + обновлённый | обновить с URL (наш / корпоративный) | — |
| Настройки | при первом запуске (онбординг) | экран настроек, трей | любая строка | «Сбросить настройки» |
| Сессия диктовки | по хоткею | пилюля/трей (состояние) | отмена | автоматически — аудио не сохраняется |
| Обновление утилиты | — | строка внизу, панель | «пропустить версию» | — |
| Ярлык автозапуска | тумблер вкл | состояние тумблера | — | тумблер выкл |
| Статистика (локальная) | автоматически | «О программе → Статистика» | — | «Очистить» |

**Матрица прав (B2B).**
| Действие | П1 без admin | П2 admin | П3 (policy) |
|---|---|---|---|
| Диктовка, настройки, модели, автозапуск | да | да | — |
| Установка обновления из GUI (без ЗПС) | нет → «скачать пакет для администратора» | да (polkit-диалог) | ставит сам через apt |
| Обновление при ЗПС | нет → пакет администратору | нет → пакет администратору | ставит штатно через apt; подпись артефактов для ЗПС — по процедуре заказчика (ГОСТ-подпись на нашей сборке — после v1.0) |
| Сетевые проверки | по своим тумблерам | по своим тумблерам | может **запретить** (`policy.conf`: офлайн, корпоративный URL, только отечественные модели) |

Изоляция данных: один пользователь = один каталог `~/.config/astra-voice/`, `~/.local/share/astra-voice/models/`,
`~/.local/state/astra-voice/`, `~/.cache/astra-voice/`. Ничего общесистемного, кроме пакета и `/etc/astra-voice/policy.conf`.

**Путь пользователя (story map, горизонталь).**
Установка → Первый запуск (онбординг) → Диктовка → Вставка → Управление (трей/настройки) → Модели →
Обновления (модель · утилита) → Автозапуск → Ошибки/восстановление → Удаление.

---

## 5. Детальный дизайн фич

Формат каждого модуля: описание · user story · приоритет (P0 = без этого не релиз v0.1, P1 = v0.2, P2 = v1.0/после) ·
бизнес-правила (исчерпывающе) · флоу (успех + исключения). Acceptance в Given-When-Then — в §6 по сценариям
(ссылки `S#`), для модулей без отдельного сценария — здесь.

### F1. Установка и первый запуск (онбординг) — P0
**Описание.** Один deb-пакет; первый запуск проводит через 5 коротких экранов, каждый — один вопрос.
**Story.** Как П1, я хочу за несколько минут получить работающую диктовку, чтобы не читать инструкции.
**Бизнес-правила.**
1. Пакет `astra-voice_X.Y.Z_amd64.deb` ставится штатно (`apt install ./…`); зависимости — только из репозиториев Astra 1.8 плюс вендоренные python-пакеты внутри пакета; `postinst` **не выходит в сеть** и не запускает GUI.
2. Модели в пакет не входят. Пакет без моделей ≤ 80 МБ (допущение, проверить прототипом).
3. Онбординг показывается, если нет ни одной установленной модели **или** не завершён флаг `onboarding_done`. Возврат — из настроек «Пройти настройку заново».
4. Экраны: **(1) Приветствие и сеть** — язык интерфейса (ru/en, авто по `LANG`) и два тумблера «Проверять обновления утилиты» / «Проверять обновления моделей» (подпись: не чаще раза в сутки; хосты huggingface.co, github.com). **Оба тумблера пусты по умолчанию во всех профилях** — пользователь включает сам; пока не включил — утилита в сеть не выходит (кроме явного «Скачать» на экране 2). Требования заказчика №1/№4 (проверка при запуске) действуют после явного включения. Если `policy.conf` задаёт `offline=true`/`profile=secure`, тумблеры показаны заблокированными («Задано администратором»). **(2) Модель** — рекомендованная карточка сверху (GigaAM v3 RNN-T), остальные по размеру; кнопки «Скачать» (показывает хост и объём) и «Установить из файла/папки…»; шаг нельзя пропустить без модели. **(3) Горячая клавиша** — предзаполнена **`Ctrl+Space`, режим «удерживать» (push-to-talk)**; поле захвата другой комбинации, переключатель «удерживать / нажать-нажать»; проверка конфликта с глобальными шорткатами KDE (`kglobalshortcutsrc`) — предупреждение, не запрет. **(4) Проверка микрофона** — выбор устройства, живой индикатор уровня, кнопка «Сказать тестовую фразу» → распознанный текст на экране (без вставки). **(5) Автозапуск** — тумблер (по умолчанию включён, с текстом, что именно создаётся) и «Готово».
5. Каждый экран, кроме (2), можно пропустить; пропущенное доступно в настройках.
6. По завершении — окно сворачивается в трей, показывается системное уведомление «Astra Voice готов: зажмите <хоткей> и говорите».
**Исключения.** Нет сети на шаге 2 → карточки «Скачать» неактивны с текстом «Нет доступа к <хост>», активен «Установить из файла»; обрыв загрузки → докачка (F7); микрофон не найден → F6; ЗПС блокирует запуск воркера → текст «Часть компонентов не подписана для ЗПС — обратитесь к администратору (инструкция)».

### F2. Захват речи и горячие клавиши — P0
**Описание.** Глобальный хоткей в X11 (XGrabKey через `python3-xlib`), два режима: push-to-talk (удержание) и toggle (нажал — говоришь — нажал).
**Story.** Как П1, я хочу зажать клавишу и говорить, чтобы текст появился без мыши и переключения окон.
**Бизнес-правила.**
1. Режим по умолчанию — push-to-talk; toggle — второй режим (отдельная комбинация или переключатель режима в настройках).
2. Комбинация по умолчанию — **`Ctrl+Space`** для push-to-talk (как в Handy заказчика; решение 2026-09-07); toggle — второй режим на отдельной комбинации (по умолчанию не назначена, задаётся в настройках); проверка конфликтов с KDE обязательна; альтернатива в подсказке — `Scroll Lock`.
3. Захват работает поверх любого X11-окна, не требует root и группы `input`; `evdev` — опциональный «надёжный режим» (P2).
4. Запись короче **0,3 с** игнорируется молча (случайное нажатие): без вставки, без ошибки, пилюля не показывается дольше 150 мс.
5. Лимит одной записи — **120 с** (настройка 30–300 с); по достижении — автостоп + распознавание + подсказка в пилюле «Достигнут лимит записи».
6. Фразы до 20 с распознаются одним куском; длиннее — сегментация по паузам (VAD) с сохранением порядка; лимит окна модели (24 с у GigaAM) не должен резать слова.
7. Аудиоустройство открывается **только при нажатии хоткея**, не при старте (гонка WirePlumber, F6). Опция «держать микрофон открытым 30 с после записи» — P1.
8. Отмена записи: `Esc` во время записи (пока хоткей зажат) или «×» в пилюле, или «Отмена» в трее — аудио отбрасывается, вставки нет.
9. Повторное нажатие хоткея во время распознавания — новая запись становится в очередь (максимум 1); вставка идёт в порядке записи.
10. Пока идёт распознавание, хоткей не блокируется; пока GUI-окно настроек в фокусе, хоткей работает так же (вставка пойдёт в поле настроек — допустимо).
**Исключения.** Комбинацию не удалось захватить (занята) → в трее статус «Горячая клавиша не захвачена» + уведомление с кнопкой «Выбрать другую»; потеря X-соединения → перезахват при восстановлении; клавиша отпущена раньше, чем открылся микрофон → см. п. 4.

### F3. Индикация: пилюля, трей, звук — P0
**Описание.** Три канала: пилюля-оверлей с живым уровнем (по умолчанию включена, отключаемая), иконка трея с состояниями, опциональные звуки.
**Story.** Как П1, я хочу видеть, что микрофон меня слышит, чтобы не диктовать в пустоту.
**Бизнес-правила.**
1. Пилюля появляется ≤ 100 мс после нажатия, у нижнего края экрана по центру (настройка: снизу / сверху / выключена), поверх всех окон, **не забирает фокус** и не появляется в панели задач/Alt+Tab.
2. Состояния пилюли: «слушаю» (бирюза `#12B3A0`, столбики уровня), «распознаю» (нейтральный, пульс), «готово» (краткое подтверждение 500 мс — решение G2 2026-09-08, затем скрытие), «ничего не распознано» (1 с), «ошибка» (красный + короткий текст, 3 с, клик открывает подробности). Точные размеры/анимация — дизайн-спека.
3. В пилюле есть «×» — отмена записи.
4. Иконка трея (SNI/`QSystemTrayIcon`): 4 состояния — «готов», «слушаю» (бирюза), «распознаю», «ошибка/микрофон недоступен» (красный). Тема иконки — по теме панели (светлая/тёмная). В панели Fly — SNI, если поддерживается, иначе XEmbed: перекраска под тему не требуется, состояние читается формой значка и подсказкой (S17-A3). Регистрация в трее с ретраем до 30 с после старта (гонка с панелью — KDE и Fly).
5. Звуки «начало/конец записи» — настройка, по умолчанию **выключены**; при отключении пилюли утилита один раз предлагает включить звук («Вы отключили индикатор — включить звуковой сигнал?»).
6. Отключение пилюли **не** отключает индикацию в трее: иконка «слушаю» обязательна всегда (запрет скрытой записи, §9.5).
7. Тема пилюли и окна — в KDE из `~/.config/kdeglobals` (не из палитры Qt), во Fly — из настроек темы Fly без `kdeglobals` (S17-A7; при невозможности определить — светлая + настройка «Тема: авто / светлая / тёмная»), применяется до первой отрисовки (без «вспышки светлой темы»).
**Исключения.** Композитор не даёт «поверх всех» → пилюля всё равно показывается (может быть перекрыта); трей недоступен > 30 с → окно настроек открывается само с баннером «Системный трей недоступен, приложение работает; закрыть окно = свернуть».

### F4. Распознавание (движок, воркер, модель) — P0
**Описание.** Инференс в отдельном процессе-воркере (`onnx-asr`), модель держится в памяти, GUI не блокируется и не падает вместе с ORT.
**Story.** Как П2, я хочу ту же скорость и качество, что в Handy с GigaAM, чтобы замена не была шагом назад.
**Бизнес-правила.**
1. Воркер — отдельный процесс; крах воркера не роняет GUI: автоперезапуск (до 3 раз за 10 мин), пользователю — ошибка в пилюле/трее.
2. Модель по умолчанию — GigaAM v3 e2e_rnnt int8. Активная модель загружается в память при старте в фоне (после готовности трея) и **держится в памяти постоянно** (решение заказчика 2026-09-07); выгрузка по простою — опция «выгружать после: 5 / 15 / 60 мин / никогда», по умолчанию «никогда».
3. Целевая задержка: «отпустил → текст» p95 ≤ 0,5 с (6-с фраза, эталонная машина, модель в памяти); холодный старт модели ≤ 5 с с индикацией «Загружаю модель…» в пилюле.
4. Потоки ORT — по умолчанию `min(4, физических ядер)`; настройка в «Продвинутых».
5. Результат — текст UTF-8 как выдала модель; единственная обработка: обрезка пробелов по краям, опция «пробел в конце» (по умолчанию вкл — допущение), опция «первая буква заглавной» для моделей без пунктуации (P1). Никакой LLM-постобработки.
6. Пустой результат → «ничего не распознано», вставки нет.
7. Каждое распознавание пишет в локальную статистику `(model_id, audio_ms, t_ms, ts)`; первый прогон после загрузки помечается «холодным» и в медиану не входит.
8. После первого прогона каждой модели снимается `VmHWM` воркера → карточка получает «ОЗУ: N МБ (замерено на этом компьютере)».
**Исключения.** Модель не загрузилась (битый файл, несовпадение входов ONNX) → карточка «Повреждена — переустановить», активной становится предыдущая рабочая модель, если есть; нехватка ОЗУ (`MemoryError`/OOM) → ошибка «Недостаточно памяти для модели X (нужно ~N МБ)» и предложение более лёгкой модели из каталога.

### F5. Вставка текста в активное окно — P0
**Описание.** Через буфер обмена: сохранить буфер → положить `text/plain` → послать комбинацию вставки → восстановить буфер.
**Story.** Как П1, я хочу, чтобы текст оказался там, где курсор, в терминале, браузере и LibreOffice, а мой буфер обмена остался нетронутым.
**Бизнес-правила.**
1. В буфер кладётся **только `text/plain`** (без форматирования — LibreOffice вставит чистый текст без диалога).
2. Комбинация вставки — настройка: `Ctrl+V` (по умолчанию) / `Ctrl+Shift+V` / `Shift+Insert` / «не вставлять, только в буфер».
3. Задержка перед вставкой — настройка, по умолчанию 50 мс; после вставки через 100 мс восстанавливается исходное содержимое буфера (если оно было текстом; нетекстовое содержимое не восстанавливается — допущение, §12).
4. Автоопределение терминалов по `WM_CLASS` (`konsole`, `fly-term`, `xterm`, `yakuake`, `alacritty`, список редактируемый) → `Ctrl+Shift+V` — P1.
5. `xdotool type` (посимвольный ввод) — только как явно помеченный аварийный вариант в «Продвинутых» (кириллица ненадёжна) — P2.
6. Приватность буфера: опция «Не оставлять распознанный текст в истории буфера (Klipper)» — P1, реализация через `x-kde-passwordManagerHint`; до неё — честное предупреждение в настройках. Во Fly-сессии Klipper отсутствует — опция неактивна с подписью «нет менеджера истории буфера», обращений к Klipper нет (S17-A8).
7. Последний распознанный текст хранится **только в памяти** до выхода; «Скопировать последний текст» в трее; на диск не пишется (история — вне v1).
8. Перед показом пилюли запоминается активное окно (`_NET_ACTIVE_WINDOW`); вставка идёт в него, если оно ещё существует, иначе — в текущее активное. Пилюля никогда не активируется.
**Исключения.** Активного окна нет (рабочий стол) → текст только в буфере + пилюля «Скопировано в буфер»; окно не приняло вставку — утилита не может это узнать, поэтому текст остаётся в буфере 100 мс до восстановления; опция «оставлять текст в буфере» (не восстанавливать) — настройка.

### F6. Ошибки микрофона и аудио — P0
**Описание.** Микрофон открывается лениво; каждая ошибка ведёт к действию, а не в тупик; известная гонка WirePlumber диагностируется явно.
**Story.** Как П1, я хочу понять, почему меня не слышно, и починить это одной кнопкой.
**Бизнес-правила.**
1. Устройство ввода — настройка «Микрофон: системный по умолчанию / конкретное»; список — из PipeWire/ALSA.
2. Ошибки и тексты (русский, путь ALSE/KDE):
   - нет устройств: «Микрофон не найден. Подключите микрофон или гарнитуру и попробуйте снова» + кнопка «Обновить список»;
   - устройство занято/ошибка открытия: «Микрофон недоступен (код). Повторить» — ретрай 3 раза по 300 мс автоматически;
   - **тишина**: если за 2 с записи пик уровня < −60 дБ (порог настраиваемый) → пилюля «Микрофон молчит» и уведомление с кнопкой **«Перезапустить звуковую службу»** (`systemctl --user restart wireplumber`, без root) и ссылкой «Что это» (гонка при загрузке, профиль карты). После перезапуска — автоматическая повторная проверка уровня.
3. Первая запись после входа в систему при автозапуске: если открытие устройства не удалось — повторить через 1 с (до 3 раз) до показа ошибки.
4. Утилита никогда не пишет аудио на диск (ни временных wav); буфер записи живёт в памяти и очищается после распознавания или отмены.
**Исключения.** `wireplumber` не установлен/не отвечает → кнопка неактивна, текст «Обратитесь к администратору: служба звука не отвечает»; уровень есть, но результат пустой → «ничего не распознано» (это не ошибка микрофона).

### F7. Каталог моделей — P1 (минимум для v0.1: одна встроенная модель по умолчанию)
**Описание.** Экран «Модели»: секции «Установленные» и «Доступные», карточки с честными цифрами, скачивание с докачкой и проверкой sha256, удаление, выбор активной.
**Story.** Как П2, я хочу видеть размер, память, качество и скорость каждой модели, чтобы выбрать под свою машину, а не гадать.
**Бизнес-правила — карточка (обязательные поля).**
| Поле | Правило |
|---|---|
| Имя и производитель | «GigaAM v3 RNN-T · Сбер»; чужие знаки — только как имена моделей |
| Назначение | одна фраза: «Русская диктовка с пунктуацией — по умолчанию» |
| Бейджи | «Рекомендуем», «Активна», «Новое» (появилась в каталоге ≤ 30 дней), «Обновление доступно» |
| Размер на диске | из манифеста, МБ (десятичные, одна единица во всём UI) |
| ОЗУ в работе | **три уровня достоверности**: «замерено на этом компьютере» (жирным; VmHWM воркера после первого прогона, дополнительно PSS из `smaps_rollup`), «заявлено автором» (обычным, со ссылкой), «оценка ~N» (серым; ориентир ×1,85 от int8-весов по единственной точке, реальный разброс 1,5–3× по Researcher #2 — поэтому только с «~»); до замера — справочное |
| Полоска «качество» | из WER на **одном наборе для всех** (Russian LibriSpeech, бенчмарк onnx-asr), `quality = clamp((30 % − WER) / 25 %, 0, 1)` — WER ≤ 5 % = полная полоска, ≥ 30 % = пустая, пороги показаны в «Как мы считаем»; подпись источника по клику; отдельный значок «пунктуация: да/нет» — качество и пунктуация не смешиваются |
| Полоска «скорость» | из RTFx: замеренная медиана по ≥ 5 прогонам на этой машине (жирным) либо бенчмарк x64 int8, пересчитанный коэффициентом машины (серым); шкала логарифмическая, RTFx 1 = 0 %, 60 = 100 %; текст «≈20× быстрее речи: фраза 6 с → 0,3 с». Нет int8-бенчмарка → подпись «по бенчмарку fp32»; нет данных по протоколу → полоска не рисуется, подпись «нет данных по протоколу» (до собственного замера, US-5.4) |
| Язык(и) | «Только русский» / «ru + kk/ky/uz/en» / «Многоязычная» |
| Лицензия | SPDX + правообладатель; CC-BY — значок «требует указания авторства» |
| Происхождение | «отечественная / зарубежная» — **по юрлицу правообладателя** (решение 2026-09-08, контекст Указа № 166); подпись может уточнять: «зарубежная · команда из России» (Vosk); используется фильтром «только отечественные», §9.4 |
| Статус | не скачана / скачивается (прогресс, скорость, «Отмена») / проверяется (sha256) / установлена / активна / обновление доступно / повреждена |
| Действия | Скачать · Установить из файла · Выбрать · Удалить · Обновить (если доступно) · «Подробнее» (ссылка на карточку HF, протокол замера) |
**Бизнес-правила — поведение.**
1. Загрузка во временный `.part` с докачкой по `Range` (продолжать только при 206 с корректным `Content-Range`; на 200 — сначала); URL закреплён на `/resolve/{revision}/…`, не на `main`; после завершения — размер и sha256 против манифеста, `fsync`, атомарный `os.replace`; модель хранится в каталоге `<ревизия>/`, активная ревизия — указатель `current.json` (откат = смена указателя); несовпадение → удалить и показать ошибку, не «доклеивать».
2. Источники по очереди: HF → GitHub Releases (наше зеркало) → корпоративный URL; переход к следующему при ошибке сети/429/404; отображается, откуда качаем. Запись каталога привязана к рантайму (`engine`): экспорт под `onnx-asr` (istupakov) и под `sherpa-onnx` (csukuangfj) — разные записи, не взаимозаменяемые.
3. Установка из файла/папки: те же файлы, та же проверка sha256 по манифесту; неизвестный набор файлов (нет в манифесте) → «Своя модель» без полосок, P2.
4. Удаление активной модели запрещено, пока не выбрана другая; удаление освобождает диск и снимает «замерено».
5. Выбор модели — переключение воркера с индикацией «Переключаю…»; пока идёт — хоткей работает на старой модели.
6. Перед скачиванием проверяется свободное место (размер × 1,2); недостаточно → ошибка с цифрами.
7. Проверка на минимальную ОЗУ машины: если `MemTotal` < `min_ram_mb` модели — предупреждение, не запрет.
8. Фильтр по языку (P2) и переключатель «только отечественные» (P1, автоматически в защищённом профиле).
9. Каталог всегда работает офлайн: встроенный манифест из пакета + последний скачанный; «нет сети» = состояние секции «Доступные» с кнопкой «Установить из файла», а не пустой список.
**Состав каталога v1** — §7.2. **Формат манифеста** — §7.3.
**Исключения.** Манифест с URL не прошёл подпись/схему → используется предыдущий, в статусной строке «Каталог не обновлён: <причина>»; диск заполнился во время загрузки → пауза, ошибка с кнопкой «Повторить» после освобождения места.

### F8. Проверка обновлений моделей при старте — P1
**Описание.** Раз в сутки, в фоне, не блокируя работу: (A) новая ревизия закреплённой модели; (B) обновлённый манифест каталога с новыми моделями; (C) сигнал «у автора вышла близкая модель».
**Story.** Как П2, я хочу узнавать при запуске, что вышла новая ревизия моей модели или близкая модель, чтобы не следить за HF вручную.
**Бизнес-правила.**
1. Работает только при включённом пользователем тумблере «Проверять обновления моделей» (по умолчанию выключен; онбординг, экран 1); в офлайн-режиме, при `policy.conf: offline` или `HF_HUB_OFFLINE=1` в окружении — никогда.
2. Не чаще **1 раза в 24 ч**, запуск через случайную задержку 0–30 мин после готовности GUI, общий таймаут 3 с, ≤ 1 повтор; при 429 — не повторять до конца окна; кэш `~/.local/state/astra-voice/update-cache.json`. User-Agent `astra-voice/X.Y.Z (+repo URL)`. Прокси — из окружения.
3. **(A) Новая ревизия.** Для активной и всех установленных моделей сравнивается коммит **нужной ветки** (`sha` из `GET /api/models/{repo}/revision/{branch}`, `targetCommit` из `/refs` или `X-Repo-Commit` в HEAD; для `ai-sage/GigaAM-v3` варианты = ветки, `main` не показатель) с `pinned_revision`. Отличие → бейдж «Обновление доступно» на карточке + строка внизу «Обновление модели: GigaAM v3 RNN-T» (нейтральный цвет) + одно системное уведомление. **Ничего не скачивается само.**
4. Установка обновления модели: по кнопке → скачать в staging → sha256 → **смоук-распознавание** вшитого тестового wav (ожидаемые слова) → атомарная подмена → старая ревизия хранится до следующего успешного запуска (откат кнопкой «Вернуть предыдущую», P2) ; провал смоука → откат автоматически + ошибка «Новая ревизия не прошла проверку, оставлена текущая».
5. **(B) Манифест.** Обновляется с `catalog_url` (наш GitHub raw/Release или корпоративный) по той же частоте; новые записи получают «Новое», удалённые из манифеста — остаются у пользователя, если установлены, с пометкой «снята с каталога».
6. **(C) Близкая модель (P2).** Запросы `GET /api/models?author=<автор>&sort=lastModified&direction=-1` по белому списку авторов из манифеста (`update_watch.authors`: `ai-sage`, `csukuangfj`, `istupakov`; поиск по `author=salute-developers` **не** находит `ai-sage/GigaAM-v3` — Researcher #2); GitHub `salute-developers/GigaAM` без Releases и тегов — не источник. Новый репозиторий того же семейства (`giga-am`/`gigaam`), которого нет в манифесте → информационный баннер в «Моделях»: «У Сбера вышла новая модель: GigaAM-Multilingual (14.07.2026). В каталог Astra Voice ещё не добавлена» + ссылка. **Не устанавливается** из UI до появления в манифесте (веса могут иметь другие входы ONNX).
7. «Пропустить эту ревизию» — не напоминать про конкретный sha.
**Исключения.** Нет сети/DNS/таймаут → тихий пропуск, ничего не меняется в UI (кроме `last_checked_at` в «О программе»); 429/403 → пропуск с backoff; ответ HF без `x-repo-commit` → считать «неизвестно», не считать обновлением.

### F9. Проверка и установка обновлений утилиты — P1 (трек без ЗПС), P2 (трек ЗПС)
**Описание.** Строка внизу окна (как у Handy) с тремя состояниями; установка — **только по явному действию** пользователя; два трека по среде; «обновить из файла» для офлайн.
**Story.** Как П2, я хочу увидеть внизу «Доступна версия X» и поставить её из утилиты; как П1 в контуре с ЗПС — скачать пакет и передать администратору.
**Бизнес-правила.**
1. Источник — GitHub Releases API `releases/latest` (или `latest.json` с корпоративного URL); `tag_name` нормализуется (один префикс `v`) и сравнивается как SemVer с `__version__` пакета; `ETag`/`If-None-Match`, лимит анонима GitHub 60 запросов/час на IP. Только при включённом пользователем тумблере «Проверять обновления утилиты» (по умолчанию выключен); ≤ 1 раза в 24 ч; при старте — сначала показывается **кэшированный** результат, свежая проверка — в фоне с задержкой 0–10 мин; ручная кнопка «Проверить сейчас» — без ограничения частоты, но с уважением 429. Никаких токенов в клиенте.
2. Состояния строки: «Проверяю обновления…» → «Установлена последняя версия» (только при ручной проверке, 3 с) / **«Доступна версия X.Y.Z»** (единственный цветовой акцент внизу) / «Проверка обновлений отключена» / **«Источник обновлений недоступен»** (не молчать) / «Загрузка… 42 %» / «Установка…» / «Перезапустить».
3. Клик по «Доступна версия» → панель: «Что нового» (из тела релиза), размер, и действия по треку:
   - **Трек A (без ЗПС, пользователь может пройти polkit):** «Скачать и установить» → скачать `.deb` + `SHA256SUMS` + подпись в `~/.cache/astra-voice/updates/` → проверить sha256 и подпись релиза (ключ вшит в пакет; minisign или GPG — архитекторам) → **один** вызов root-помощника `/usr/libexec/astra-voice-update-helper` через `pkexec` с собственным polkit-действием (`auth_admin`, одно окно пароля агента polkit текущей сессии — KDE или Fly); помощник копирует пакет в root-staging, **повторно** проверяет подпись/sha256/имя пакета/архитектуру/повышение версии и выполняет `apt-get install --no-remove ./astra-voice_X.Y.Z_amd64.deb` → «Установлено. Перезапустить сейчас / позже». Если агент polkit не запущен — «Не найден агент авторизации (KDE: polkit-kde-authentication-agent-1; Fly: агент polkit Fly)» + подсказка «Обновить вручную: sudo apt install ./…»; первая установка — штатным apt (помощник появляется с первым пакетом).
   - **Трек B (ЗПС включена, или нет прав администратора, или `policy.conf: updates=admin`):** «Скачать пакет для администратора» → те же файлы + `INSTALL-ADMIN.md` (инструкция: проверка подписи релиза, `apt install`; при включённой ЗПС — подпись ELF/`.so` по процедуре заказчика; в v1 артефакты **не** подписаны ГОСТ-ключом на сборке — это v1.1) → «Открыть папку» + текст «Установку выполняет администратор. Подпись пакета проверена». Кнопки «Установить» **нет**.
   - Всегда: «Пропустить эту версию», «Напомнить позже».
4. Определение трека: ЗПС = `DIGSIG_ELF_MODE ≠ 0` (если файл конфигурации читаем) **или** `policy.conf: zps=on`; при невозможности определить — вопрос пользователю «На этом компьютере включена замкнутая программная среда? Да / Нет / Не знаю (→ трек B)». Права = членство в `astra-admin`/`sudo`.
5. **Никогда**: тихая установка, установка без проверки подписи, установка при несовпадении sha256 (файл удаляется, ошибка «Пакет не прошёл проверку»), запуск `dpkg -i` (только `apt-get install ./файл`), привязка polkit-действия напрямую к `/usr/bin/apt` (произвольные аргументы), `auth_admin_keep`, JS-правила в `/etc/polkit-1/rules.d`.
6. «Обновить из файла…» (меню строки и «О программе»): выбрать `.deb` (+ рядом `.sig`/`SHA256SUMS`) → та же проверка → трек A/B. Без валидной подписи GUI **отказывает** и подсказывает штатный `apt` администратору.
7. После установки автоперезапуск не делаем без согласия: кнопка «Перезапустить».
**Исключения.** polkit отменён/пароль неверный → «Установка отменена», deb сохранён, «Открыть папку»; `apt` завершился с ошибкой → текст с последними строками лога и путём к логу; GitHub недоступен → «Источник обновлений недоступен» + подсказка «Обновить из файла»; NAT-лимит 403/429 → показывать кэш и время следующей попытки.

### F10. Автозапуск — P1 (v0.2; заказчик 2026-09-07: включён по умолчанию на последнем экране онбординга)
**Описание.** Тумблер создаёт/удаляет `~/.config/autostart/<app-id>.desktop`, указывающий на канонический `/usr/bin/astra-voice`.
**Story.** Как П1, я хочу, чтобы диктовка была доступна сразу после входа в систему, чтобы не запускать её руками.
**Бизнес-правила.**
1. Ярлык: `Exec=/usr/bin/astra-voice --hidden`, `X-KDE-autostart-after=panel`, **без `OnlyShowIn`**; путь фиксирован пакетом, **никогда не переписывается** при запуске.
2. Состояние тумблера = файл существует и не содержит `Hidden=true`; чужие/старые ярлыки (`handy.desktop`) не трогаем.
3. Автозапуск стартует свёрнутым в трей, с видимой иконкой; **никакой записи без хоткея**.
4. Single-instance: повторный запуск показывает окно уже работающего экземпляра.
5. При автозапуске аудио не открывается, модель грузится в фоне, трей — с ретраем.
**Исключения.** `~/.config/autostart` недоступен для записи → ошибка с путём; `systemctl --user` отсутствует — не влияет (не источник правды).

### F11. Настройки, тема, язык — P0 (минимум) / P1 (полный экран)
**Описание.** Окно с левым сайдбаром из 6 разделов (Общие · Модели · Вывод · Сеть и обновления · Продвинутые · О программе; «Отладка» внизу — по флагу; направление A «Панель», G2 2026-09-08), строки «заголовок · «?» · контрол справа», тёмная/светлая тема из KDE.
Размер окна настроек: по умолчанию и минимальный — **900×620** (`minimumWidth/Height`; ниже ломается карточка модели — решение полировки G2, 2026-09-08).
**Story.** Как П1, я хочу минимум настроек на виду и всё остальное — под рукой, но не перед глазами.
**Бизнес-правила.**
1. Разделы и строки: **Общие** — хоткей(и), режим, микрофон, индикатор (снизу/сверху/выкл), звук, автозапуск; **Модели** — F7; **Вывод** — комбинация вставки, задержка, восстановление буфера, защита истории Klipper, пробел в конце, терминалы; **Продвинутые** — потоки ORT, выгрузка модели, лимит записи, порог тишины, «надёжный режим evdev», экспериментальные; **Сеть и обновления** (отдельный раздел сайдбара — G2 2026-09-08) — тумблеры «проверять обновления утилиты» / «…моделей», «офлайн-режим», URL каталога/обновлений, «проверить сейчас», «обновить из файла»; **О программе** — F13.
2. Тема — в KDE из `kdeglobals`, во Fly из настроек темы Fly (без `kdeglobals`, S17-A7), до первой отрисовки; пересчёт при смене темы в KDE (сигнал D-Bus/файл) без перезапуска, во Fly допускается после перезапуска с явной подсказкой; настройка «Тема: авто / светлая / тёмная».
3. Язык UI — русский исходный, английский второй; все строки во внешних файлах перевода; смена языка — без перезапуска (или с явным «перезапустить»).
4. Настройки — один JSON в `~/.config/astra-voice/settings.json`, версионированный (`schema_version`), миграции вперёд; «Сбросить настройки» с подтверждением.
5. Строки, зафиксированные `policy.conf`, показываются заблокированными с подписью «Задано администратором».
6. Импорт настроек Handy (хоткей, микрофон, модель) при первом запуске, если найден `~/.config/com.pais.handy/` — P2, только с согласия.
**Исключения.** Битый settings.json → резервная копия `.bak`, настройки по умолчанию, уведомление.

### F12. Трей и меню — P0
**Story.** Как П1, я хочу управлять утилитой из трея, не открывая окно.
**Бизнес-правила.** Меню: «Слушаю…/Готов» (статус, неактивный), «Отмена» (только во время записи/распознавания), «Модель ▸» (список установленных с галочкой), «Скопировать последний текст» (неактивно, если нет), «Настройки…», «Проверить обновления» (всегда активно — ручная проверка есть явное действие пользователя; неактивно только в офлайн-режиме или по политике администратора — решение G2-полировки 2026-09-08), «О программе», «Выход». Левый клик — окно настроек; средний — старт/стоп записи (toggle) — P2. Иконка — 4 состояния (F3).

### F13. «О программе», NOTICE, PRIVACY — P1
**Story.** Как П3, я хочу видеть версии, лицензии и список сетевых хостов, чтобы согласовать установку с ИБ.
**Бизнес-правила.** Экран показывает: версию утилиты (`font-mono`) и дату сборки, версии Python/PyQt5/onnxruntime/onnx-asr, активную модель с ревизией, лицензию утилиты (GPL-3.0-or-later), кнопку «Лицензии компонентов и моделей» (содержимое `NOTICE`: имя, правообладатель, лицензия, ссылка — sherpa-onnx Apache-2.0 с NOTICE по §4(d), Whisper — по моделям: large-v3-turbo MIT, small и base Apache-2.0, CC-BY-модели с атрибуцией), «Приватность» (`PRIVACY.md`: аудио не покидает устройство, не сохраняется; точный список хостов; как выключить сеть), дисклеймер о товарных знаках («Astra Linux — товарный знак ПАО «Группа Астра»; GigaAM — Сбер; Whisper — OpenAI; продукт не аффилирован…»), пути данных/логов с кнопками «Открыть», «Статистика» (§10), «Пройти настройку заново», «Обновить из файла…». Те же `NOTICE`/`PRIVACY.md`/`LICENSE` — внутри deb (`/usr/share/doc/astra-voice/`).
**Acceptance.** Given установленная утилита, When открываю «О программе», Then вижу версию, ≥ 1 запись NOTICE на каждый вендоренный компонент и каждую установленную модель, список хостов, совпадающий с `PRIVACY.md`, и дисклеймер о знаках.

### F14. Приватность, сеть, политика администратора — P1
**Story.** Как П3, я хочу одним файлом зафиксировать офлайн-режим и корпоративный URL на всех машинах.
**Бизнес-правила.**
1. Сетевые действия — только четыре: проверка обновлений утилиты, проверка обновлений моделей/манифеста, скачивание модели по кнопке, скачивание deb по кнопке. Всё остальное — запрещено (в т. ч. телеметрия, crash-dumps, «отправить отчёт» — §9.5).
2. Три тумблера: «Проверять обновления утилиты», «Проверять обновления моделей» (**оба выключены по умолчанию во всех профилях**, включает пользователь), «Офлайн-режим» (перекрывает оба и скрывает кнопки «Скачать» с сети; также включается переменной `HF_HUB_OFFLINE=1`). Состояние показывается в «О программе».
3. `/etc/astra-voice/policy.conf` (читается при старте, только чтение): `profile=personal|secure`, `offline=true|false`, `catalog_url=`, `updates_url=`, `updates=gui|admin`, `zps=auto|on|off`, `domestic_only=true|false`, `autostart_default=`. `profile=secure` ⇒ `offline=true`, `updates=admin`, `domestic_only=true`, если не переопределено. Значения из policy блокируют соответствующие строки настроек.
4. Кэш проверок, backoff, уважение `RateLimit`/`Retry-After`, корректный User-Agent — обязательны (F8, F9); прокси — из окружения (`HTTPS_PROXY`/`NO_PROXY`), корпоративный CA — из системного хранилища или `SSL_CERT_FILE`; проверка TLS **никогда** не отключается.
5. В текстах для пользователя **не пишем «включите VPN»**; при недоступности хоста — «Установить из файла» / «Обратитесь к администратору за корпоративным зеркалом».
6. Логи — локальные, ротация 5 × 1 МБ, без аудио и без распознанного текста (только длины/тайминги/коды ошибок); уровень — настройка.
**Acceptance.** Given `policy.conf` с `profile=secure`, When запускаю утилиту, Then строка обновлений показывает «Проверка обновлений отключена (задано администратором)», в «Моделях» нет кнопок «Скачать» с сети, показаны только отечественные модели, а `tcpdump` за сессию не фиксирует исходящих соединений процесса.

### F15. Манифест каталога и корпоративный URL — P1
**Story.** Как П3, я хочу отдать сотрудникам внутренний каталог моделей с внутреннего сервера, чтобы им не нужен был интернет.
**Бизнес-правила.** Формат — §7.3; встроенный манифест в пакете — источник по умолчанию; `catalog_url` (настройка или policy) — тот же JSON-формат + отсоединённая подпись (ключ — наш вшитый либо корпоративный из `policy.conf: catalog_pubkey=`); `sources[].type=corp` разрешаются относительно `corp_base`; манифест без валидной подписи не применяется (предыдущий остаётся). Инструмент `astra-voice-catalog` (CLI, P2) для генерации корпоративного манифеста из папки с весами (считает sha256, размеры).
**Acceptance.** Given `catalog_url` указывает на внутренний сервер с подписанным манифестом и весами, When открываю «Модели» без доступа в интернет, Then вижу модели из корпоративного манифеста, скачиваю GigaAM v3 с внутреннего сервера, sha256 совпадает, модель становится активной.

### F16. Диагностика — P2
**Story.** Как П2, я хочу быстро собрать сведения для баг-репорта без утечки текста.
**Бизнес-правила.** «Отладка» (по `Ctrl+Shift+D`): уровень логов, «Открыть лог», «Собрать диагностику» — zip с логами, `settings.json` (без путей домашнего каталога), версиями, выводом `pactl info`/`wpctl status`, **без** аудио и текстов; отправка — только вручную пользователем. Тестовый экран микрофона (уровень + распознать без вставки).

---

## 6. Сценарии end-to-end и acceptance (Given-When-Then)

### S1. Первый запуск / онбординг (F1, F7, F2, F6, F10)
- **A1.** Given чистая ALSE 1.8 с установленным пакетом и без моделей, When запускаю Astra Voice, Then открывается экран 1 «Приветствие и сеть», язык — русский (по `LANG=ru_RU`), оба тумблера проверок пусты и, пока я их не включил, не выполнено ни одного сетевого запроса (проверка `tcpdump`); When включаю оба и нажимаю «Продолжить», Then настройки сохранены, а запросы идут только по правилам F8/F9.
- **A2.** Given экран 2 «Модель» и доступный huggingface.co, When нажимаю «Скачать» у GigaAM v3 RNN-T, Then вижу хост и объём (222 МБ), прогресс с процентами и скоростью, затем «Проверяю…» и статус «Установлена · Активна»; sha256 всех файлов совпадает с манифестом.
- **A3.** Given экран 2 и нет сети, When открываю экран, Then кнопки «Скачать» неактивны с текстом «Нет доступа к huggingface.co», активна «Установить из файла/папки…», и после выбора папки с корректными файлами модель устанавливается и становится активной.
- **A4.** Given экран 3 «Горячая клавиша», When нажимаю комбинацию, занятую в KDE (например, `Alt+F2`), Then вижу предупреждение о конфликте с названием действия KDE и могу выбрать другую или оставить.
- **A5.** Given экран 4 «Микрофон», When говорю тестовую фразу, Then индикатор показывает уровень, а через ≤ 1 с после отпускания вижу распознанный текст на экране, и текст **не** вставлен ни в какое окно.
- **A6.** Given экран 5 «Автозапуск» с тумблером «вкл», When нажимаю «Готово», Then появляется `~/.config/autostart/<app-id>.desktop` с `Exec=/usr/bin/astra-voice --hidden` без `OnlyShowIn`, окно сворачивается в трей, показано уведомление с хоткеем.
- **A7.** Given завершённый онбординг, When запускаю утилиту повторно, Then онбординг не показывается, окно не открывается (только трей).

### S2. Диктовка push-to-talk (F2, F3, F4, F5)
- **A1.** Given утилита в трее, модель в памяти, курсор в текстовом поле Kate, When зажимаю хоткей, говорю 6 с «Проверка связи, запятая, всё работает точка» и отпускаю, Then пилюля появилась ≤ 100 мс после нажатия в состоянии «слушаю» (бирюза, столбики двигаются), после отпускания — «распознаю», и текст с пунктуацией появился в Kate в позиции курсора не позднее 0,5 с (p95 по 50 прогонам) после отпускания.
- **A2.** Given то же, When нажимаю и отпускаю хоткей за 0,2 с, Then ничего не вставлено, ошибок нет, пилюля не задержалась дольше 150 мс.
- **A3.** Given идёт запись, When нажимаю `Esc` или «×» в пилюле, Then запись отброшена, вставки нет, пилюля скрыта, трей — «готов».
- **A4.** Given запись длится 120 с (лимит), When лимит достигнут, Then запись остановлена автоматически, текст распознан и вставлен, пилюля показала «Достигнут лимит записи».
- **A5.** Given включена выгрузка по простою и модель выгружена из памяти, When зажимаю хоткей, Then пилюля показывает «Загружаю модель…», запись при этом идёт, и текст вставляется после загрузки (≤ 5 с + время распознавания).

### S3. Диктовка toggle (F2)
- **A1.** Given режим «нажать — говорить — нажать», When нажимаю хоткей один раз, говорю 10 с, нажимаю снова, Then запись идёт между нажатиями (пилюля «слушаю» всё время), после второго нажатия текст вставлен.
- **A2.** Given запись в режиме toggle, When 120 с прошли без второго нажатия, Then автостоп по S2-A4.
- **A3.** Given push-to-talk и toggle на разных комбинациях, When использую любую, Then работает соответствующий режим, конфликтов нет.

### S4. Вставка в активное окно (F5)
- **A1.** Given буфер обмена содержит «АБВ», курсор в Firefox (поле адреса), When диктую «привет», Then в поле появилось «привет», а через ≤ 200 мс буфер снова содержит «АБВ».
- **A2.** Given курсор в Konsole, комбинация по умолчанию `Ctrl+V`, автоопределение терминалов включено, When диктую, Then текст вставлен через `Ctrl+Shift+V` и **не** появилось `^V`.
- **A3.** Given курсор в LibreOffice Writer, When диктую, Then текст вставлен без диалога «Специальная вставка» и без чужого форматирования.
- **A4.** Given фокус на рабочем столе (нет окна), When диктую, Then текст в буфере, пилюля «Скопировано в буфер», ошибок нет.
- **A5.** Given включена «Не оставлять текст в истории буфера», When диктую, Then в истории Klipper распознанного текста нет (P1).

### S5. Микрофон молчит / отсутствует (F6)
- **A1.** Given после входа в систему профиль звуковой карты слетел (гонка WirePlumber) и микрофон даёт тишину, When зажимаю хоткей и говорю 3 с, Then через 2 с пилюля показывает «Микрофон молчит», уведомление содержит кнопку «Перезапустить звуковую службу»; When нажимаю её, Then выполняется `systemctl --user restart wireplumber` без запроса пароля, через ≤ 3 с повторная проверка показывает уровень, трей «готов».
- **A2.** Given микрофон физически отключён, When зажимаю хоткей, Then «Микрофон не найден. Подключите микрофон или гарнитуру и попробуйте снова», трей — красный, кнопка «Обновить список» после подключения возвращает «готов».
- **A3.** Given устройство занято другим приложением (эксклюзивно), When зажимаю хоткей, Then после 3 автоповторов — «Микрофон недоступен» с кодом, без краха.
- **A4.** Given любая ошибка микрофона, When она произошла, Then на диске не появилось ни одного аудиофайла.

### S6. Нет сети (F8, F9, F7)
- **A1.** Given тумблеры проверок включены, сети нет, When запускаю утилиту, Then GUI готов ≤ 1,5 с, диктовка работает, модальных окон нет, строка внизу — «Источник обновлений недоступен» (или кэш «Доступна версия X», если был), «Модели → Доступные» показывает «Нет доступа к каталогу — Установить из файла».
- **A2.** Given нет сети, When нажимаю «Проверить сейчас», Then ответ ≤ 3 с: «Источник обновлений недоступен», подсказка «Обновить из файла…».
- **A3.** Given прокси 407 в окружении, When идёт проверка, Then результат «недоступен», без зависаний и без повторов чаще 1 раза.

### S7. Смена модели (F7, F4)
- **A1.** Given установлены GigaAM v3 RNN-T (активна) и e2e_ctc, When нажимаю «Выбрать» у e2e_ctc, Then карточка «Переключаю…», через ≤ 5 с — «Активна», трей «Модель ▸» показывает галочку на e2e_ctc, а строка внизу слева — новое имя.
- **A2.** Given переключение идёт, When зажимаю хоткей, Then диктовка работает на прежней модели, текст вставлен.
- **A3.** Given у e2e_ctc «ОЗУ: оценка ~400 МБ» серым, When делаю первую диктовку на ней, Then карточка показывает «ОЗУ: N МБ (замерено на этом компьютере)» жирным, а после ≥ 5 диктовок полоска скорости помечена «замерено».
- **A4.** Given модель активна, When нажимаю «Удалить», Then кнопка неактивна с подсказкой «Сначала выберите другую модель».

### S8. Обновление модели при старте (F8)
- **A1.** Given `pinned_revision` активной модели = `7655ad71…` (ветка `e2e_rnnt`), тумблер включён, HF доступен и вернул другой `targetCommit` для этой ветки, When утилита запущена и прошла случайная задержка, Then на карточке — «Обновление доступно», внизу — «Обновление модели: GigaAM v3 RNN-T», одно уведомление; ничего не скачано (проверка размера каталога моделей).
- **A2.** Given то же, When нажимаю «Обновить», Then скачивание в staging, sha256, смоук-распознавание тестового wav; при успехе — активна новая ревизия, при провале — «Новая ревизия не прошла проверку, оставлена текущая», текущая работает.
- **A3.** Given HF вернул изменение только ветки `main`, а не `e2e_rnnt`, When проверка прошла, Then обновление **не** предлагается.
- **A4.** Given манифест с `catalog_url` содержит новую модель, When проверка прошла, Then в «Доступных» карточка с бейджем «Новое».
- **A5 (P2).** Given у автора `ai-sage` появился репозиторий семейства GigaAM, которого нет в манифесте, When проверка прошла, Then в «Моделях» информационный баннер с именем, датой и ссылкой, без кнопки «Скачать».
- **A6.** Given проверка была < 24 ч назад, When запускаю утилиту снова, Then новой проверки нет (по логу и `last_checked_at`).
- **A7.** Given HF вернул 429, When проверка, Then пропуск, повтор не раньше окна из `RateLimit`, в UI — тишина.

### S9. Обновление утилиты — трек без ЗПС (F9)
- **A1.** Given версия 0.2.0, GitHub `releases/latest` = 0.2.1, ЗПС выкл, пользователь в `astra-admin`, When утилита запущена, Then строка внизу «Доступна версия 0.2.1» цветом акцента; клик открывает панель с «Что нового», размером и кнопкой «Скачать и установить».
- **A2.** Given панель, When нажимаю «Скачать и установить», Then «Загрузка… N %» с прогрессом, затем проверка sha256 и подписи, затем **одно** окно polkit KDE (помощник обновления); после ввода пароля — «Установка…», затем «Установлено. Перезапустить сейчас / позже»; `dpkg-query -W astra-voice` = 0.2.1.
- **A3.** Given подпись не совпала (подменённый deb), When проверка, Then файл удалён, строка «Пакет не прошёл проверку подписи», установки не было.
- **A4.** Given polkit отменён, When отмена, Then «Установка отменена», deb в `~/.cache/astra-voice/updates/`, кнопка «Открыть папку».
- **A5.** Given тумблер «Проверять обновления утилиты» выключен, When запускаю, Then строка «Проверка обновлений отключена», сетевых запросов нет; «Проверить сейчас» в трее и в разделе «Сеть» **активны** (ручная проверка — явное действие пользователя; неактивны только в офлайн-режиме или по политике администратора — решение 2026-09-08, F12).
- **A6.** Given нажато «Пропустить эту версию», When перезапускаю, Then строка не показывает 0.2.1; при выходе 0.2.2 — показывает.
- **A7.** Given версия актуальна, When нажимаю «Проверить сейчас», Then «Установлена последняя версия» на 3 с, потом строка возвращается к обычному виду.

### S10. Обновление утилиты — трек с ЗПС / без прав (F9)
- **A1.** Given `DIGSIG_ELF_MODE=1` (или `policy.conf: zps=on`), When есть новая версия, Then панель показывает «Скачать пакет для администратора», кнопки «Установить» нет, текст «На этом компьютере включена замкнутая программная среда — установку выполняет администратор».
- **A2.** Given то же, When нажимаю «Скачать пакет для администратора», Then в папке — `.deb`, `SHA256SUMS`, подпись, `INSTALL-ADMIN.md`; подпись проверена; кнопка «Открыть папку»; утилита **не** запускала `pkexec`/`apt`.
- **A3.** Given ЗПС выкл, но пользователь не в `astra-admin`/`sudo`, When есть новая версия, Then тот же трек B.
- **A4.** Given невозможно определить ЗПС (конфиг нечитаем, policy `auto`), When открываю панель, Then вопрос «Включена ли ЗПС?» с вариантами Да / Нет / Не знаю; «Не знаю» → трек B.
- **A5 (v1.1, после v1.0).** Given ВМ с включённой ЗПС и зарегистрированным ключом организации, When администратор ставит подписанный deb по `INSTALL-ADMIN.md`, Then утилита запускается, воркер работает, диктовка вставляет текст (Ц5).

### S11. Автозапуск вкл/выкл (F10)
- **A1.** Given тумблер выключен, When включаю, Then файл `.desktop` создан с каноническим `Exec`, без `OnlyShowIn`, с `X-KDE-autostart-after=panel`; после перелогина утилита в трее, окно не открыто, аудио не открыто (нет потока в `wpctl status` до первого хоткея).
- **A2.** Given включён, When выключаю, Then файл удалён (или `Hidden=true`), после перелогина утилиты нет.
- **A3.** Given старые `handy.desktop`/`Handy.desktop` в `~/.config/autostart`, When управляю тумблером, Then эти файлы не изменены.
- **A4.** Given утилита уже запущена, When запускаю второй экземпляр, Then открывается окно первого, второй процесс завершился.

### S12. Отключение пилюли (F3)
- **A1.** Given «Индикатор: снизу», When выбираю «выключен», Then появляется предложение включить звуковой сигнал (один раз); при следующей диктовке пилюли нет, иконка трея переходит в «слушаю» (бирюза) и обратно.
- **A2.** Given пилюля выключена и звук включён, When диктую, Then слышны сигналы начала и конца, текст вставлен.
- **A3.** Given пилюля включена, When диктую в Kate, Then фокус остаётся в Kate (пилюля не забирает фокус) и текст вставляется в Kate.

### S13. Тема тёмная/светлая из KDE (F11, F3)
- **A1.** Given в KDE выбрана тёмная схема (`kdeglobals`), When открываю окно, Then первая отрисовка уже тёмная (без вспышки светлой), пилюля — в тёмном варианте, иконка трея — под тёмную панель.
- **A2.** Given окно открыто, When меняю схему KDE на светлую, Then окно и пилюля перекрашиваются ≤ 2 с без перезапуска.

### S14. Отмена во время распознавания и очередь (F2)
- **A1.** Given идёт распознавание 20-с фразы, When нажимаю «Отмена» в трее, Then вставки нет, воркер готов к следующей записи ≤ 1 с.
- **A2.** Given идёт распознавание, When начинаю новую запись, Then она принимается, обе вставки приходят по порядку.

### S15. Диск и удаление модели (F7)
- **A1.** Given свободно 100 МБ, When нажимаю «Скачать» у модели 222 МБ, Then ошибка «Недостаточно места: нужно ~270 МБ, свободно 100 МБ», загрузка не начата.
- **A2.** Given модель установлена и не активна, When «Удалить» → подтверждение, Then папка удалена, статус «Не скачана», размер каталога уменьшился на размер модели.
- **A3.** Given загрузка оборвалась на 60 %, When сеть вернулась и нажато «Повторить», Then продолжается с 60 % (запрос `Range`), итоговый sha256 верен.

### S16. Удаление утилиты
- **A1.** Given утилита установлена с моделями и автозапуском, When `apt remove astra-voice`, Then пакет удалён, пользовательские данные (`~/.config`, `~/.local/share/astra-voice`) **не** тронуты, ярлык автозапуска остаётся, но не ломает вход (Exec отсутствует → KDE/Fly молча пропускают); `apt purge` не трогает домашние каталоги; в «О программе» есть подсказка «Удалить модели и настройки» с путями.

### S17. Fly-сессия ALSE (F2–F6, F9–F12; требование заказчика 2026-09-09)
Родная сессия ALSE 1.8 — Fly (fly-dm, оконный менеджер fly-wm, панель Fly, fly-notifications) — **равноправная целевая среда v1.0** наряду с KDE Plasma 5.27. Прогон — на той же машине заказчика переключением сессии в fly-dm (допущение A12); архитектурные отличия Fly (EWMH-флаги пилюли, SNI/XEmbed, уведомления, polkit-агент, автозапуск, детект темы, буфер без Klipper) разбирают архитекторы, PRD задаёт ожидаемое поведение.
- **A1 (регрессия).** Given вход во Fly-сессию ALSE 1.8 на машине заказчика, When прогоняю S2–S5 и S8–S13, Then все их acceptance зелёные с теми же ожиданиями (тайминги, вставка, ошибки микрофона, обновления моделей и утилиты, автозапуск, пилюля, тема).
- **A2 (пилюля под fly-wm).** Given курсор в текстовом поле (редактор Fly / браузер) во Fly, When зажимаю хоткей, Then пилюля показана поверх всех окон (EWMH `_NET_WM_STATE_ABOVE`, skip-taskbar, skip-pager) под fly-wm, фокус остаётся в поле, текст после отпускания вставлен в него; пилюли нет в панели задач Fly и в Alt+Tab.
- **A3 (трей панели Fly).** Given панель Fly, When утилита стартует и меняет состояния, Then значок присутствует в трее; если панель даёт SNI — 4 состояния как в KDE; если только XEmbed — перекраска под тему не требуется, состояния «готов / слушаю / распознаю / ошибка» различимы формой значка и подсказкой; меню трея открывается и содержит те же пункты и правила активности, что в F12.
- **A4 (уведомления).** Given Fly-сессия с работающей службой уведомлений, When срабатывает «Микрофон молчит» / «Доступна версия X» / «Astra Voice готов», Then уведомление показано штатным механизмом Fly (`org.freedesktop.Notifications`). Given служба уведомлений недоступна (владелец шины завершён модулем fly-notifications), When то же событие, Then утилита не падает и не блокируется на D-Bus дольше 1 с, а показывает запасной баннер в своём окне/пилюле с тем же текстом и кнопкой действия.
- **A5 (polkit во Fly).** Given трек A обновления во Fly (пользователь в `astra-admin`, ЗПС выкл), When «Скачать и установить», Then одно окно пароля агента polkit Fly и установка по S9-A2. Given агент polkit Fly не запущен, Then «Не найден агент авторизации» + подсказка `sudo apt install ./…`, deb сохранён.
- **A6 (автозапуск во Fly).** Given тумблер автозапуска включён, When выхожу и вхожу во Fly-сессию, Then утилита стартует в трей (XDG autostart без `OnlyShowIn`), аудио не открыто до первого хоткея, модель загружена в фоне; поздний старт панели Fly не приводит к потере значка (ретрай ≤ 30 с).
- **A7 (тема без kdeglobals).** Given во Fly выбрана тёмная тема оформления, When открываю окно и пилюлю, Then они тёмные с первой отрисовки без чтения `kdeglobals` (детект темы Fly); Given тему определить не удалось, Then светлая тема и настройка «Тема: авто / светлая / тёмная»; When меняю тему Fly, Then перекраска ≤ 2 с либо после перезапуска с явной подсказкой (допуск для Fly).
- **A8 (буфер без Klipper).** Given буфер «АБВ», курсор в поле во Fly, When диктую «привет», Then текст вставлен, буфер восстановлен ≤ 200 мс; опция «Не оставлять текст в истории буфера (Klipper)» неактивна с подписью «нет менеджера истории буфера»; обращений к Klipper по D-Bus нет.
- **A9 (хоткей и вставка).** Given Fly-сессия, When зажимаю `Ctrl+Space` при активном `fly-term` / браузере / LibreOffice, Then захват работает (XGrabKey), вставка — по S4; `fly-term` входит в список автоопределения терминалов (`Ctrl+Shift+V`).

---

## 7. Каталог моделей

### 7.1 Требования к карточке — см. F7 (таблица полей). Протокол честности:
- Одна единица размера во всём UI — **МБ (десятичные)**.
- Полоска качества строится **только** по одному набору (Russian LibriSpeech *test*, бенчмарк onnx-asr, `comparison.md`); пунктуация — отдельный значок, не участвует в полоске; по клику — «Как мы считаем» с источником и датой. **Оговорка в подсказке: «WER бенчмарка измерен на fp32-весах той же модели»** — публичного WER для int8 нет ни у кого; расхождение int8/fp32 считаем малым, пока собственный замер (US-5.4) не покажет иное.
- Полоска скорости — из RTFx **x64 int8** (`benchmarks.md` onnx-asr, столбец `x64 RTFx (int8)`, Ryzen 7 9800X3D); «замерено» перекрывает «бенчмарк»; холодный первый прогон исключается. Три состояния достоверности бенчмарка: `benchmark` (int8 по протоколу) · `benchmark_fp32` (int8-замера нет — T-one, GigaAM Multilingual) · `no_data` (Whisper small: ни WER, ни RTFx по протоколу; FLEURS 11,4 % — другой протокол, только в подсказке; замер в v0.2 по US-5.4). Цифры fp32 из `comparison.md` в полоску **не** подставляются как int8.
- ОЗУ — три уровня достоверности; при смене ревизии модели или числа потоков замер сбрасывается.
- Whisper base показывается с почти пустой полоской качества (WER 38,33 %) и подписью «для русского не рекомендуется — ошибка в каждом третьем слове» — честно, чтобы пользователь не решил, что «утилита плохая».
- Единственный источник цифр §7.2 — `research/catalog-numbers.md` (HF API `?blobs=true`, байты; ревизии `main` на 2026-09-08). Правка цифр — только через него.

### 7.2 Состав каталога v1 (Must = 9, Should = 1, Could = 2)
Состав — по своду `research/summary.md` §6 и решениям 2026-09-07/08 (Whisper — три позиции; T-one — Should). **Цифры — из `research/catalog-numbers.md`** (источник истины, 2026-09-08): диск — сумма точных байт файлов, нужных рантайму, по `GET /api/models/{repo}?blobs=true`, **десятичные МБ** (в ревизии 2 девять позиций были в МиБ); WER — Russian LibriSpeech test, onnx-asr `comparison.md` (**fp32-веса**); RTFx — x64 **int8**, `benchmarks.md` (Ryzen 7 9800X3D). Где у модели два экспорта (onnx-asr / sherpa-onnx), указаны оба размера; **рантайм v1 — `onnx-asr`, единый** (синтез архитектуры 2026-09-09), в манифест попадают onnx-asr-экспорты, sherpa — справочно. ОЗУ — оценка ×1,85 от int8-весов (разброс 1,5–3×), к fp32 (T-one) неприменима; заменяется замером (US-5.4).
| # | Модель · вариант | Назначение (текст карточки) | Диск, МБ | WER ru (fp32) | RTFx x64 int8 | ОЗУ (уровень) | Пункт. | Язык | Лицензия | Происх. (по юрлицу) | Приор. |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | **GigaAM v3 RNN-T** (e2e_rnnt int8) — по умолчанию | Русская диктовка с пунктуацией. Рекомендуем | **226,4** (onnx-asr: istupakov `gigaam-v3-onnx`) / 231,9 (sherpa: csukuangfj — справочно) | 7,60 % | 42,5 | 415 МБ (замерено, эталон) | да | ru | MIT · GigaChat Team, 2024 | Сбер — отеч. | Must |
| 2 | GigaAM v3 CTC (e2e_ctc int8) | То же, быстрее на ~25 %, точность чуть ниже | 224,9 | 7,80 % | 52,2 | ~415 МБ (оценка) | да | ru | MIT | Сбер — отеч. | Must |
| 3 | GigaAM v3 RNN-T без пунктуации (rnnt int8) | Максимальная точность слов, без знаков препинания | 229,3 | **4,39 %** | 42,8 | ~425 МБ (оценка) | нет | ru | MIT | Сбер — отеч. | Must |
| 4 | GigaAM Multilingual CTC 220M (int8) | Русский + казахский/киргизский/узбекский/английский; без пунктуации | 224,8 (istupakov) | 8,43 % | **по бенчмарку fp32: 58,5** (int8-замера нет) | ~415 МБ (оценка) | нет | ru, kk, ky, uz, en | MIT | Сбер — отеч. | Must |
| 5 | T-one (Т-Банк) | Русская, лёгкая, без пунктуации; хорошая точность | 144,2 (**fp32 — int8-весов не существует**) | 6,57 % | **по бенчмарку fp32: 26,3** (int8 N/A) | нет оценки (fp32; замер в v0.2) | нет | ru | Apache-2.0 | T-Tech / АО «ТБанк» — отеч. | **Should** |
| 6 | Vosk ru 0.54 (int8) | Лёгкая полная Vosk; точность ниже GigaAM | 72,5 | 9,89 % | 70,5 | ~135 МБ (оценка) | нет | ru | Apache-2.0 · Alpha Cephei Inc. | **зарубежная · команда из России** (Alpha Cephei Inc., Делавэр, США); **вне фильтра «только отечественные»** | Must |
| 7 | Vosk small ru 0.52 (int8) | Для слабых машин и малого диска | 26,7 | 14,53 % | 83,5 | ~50 МБ (оценка) | нет | ru | Apache-2.0 · Alpha Cephei Inc. | зарубежная · команда из России; вне фильтра | Must |
| 8 | Whisper large-v3-turbo (int8) | Многоязычная, качественно, **медленно на CPU** | 1 089,1 (onnx-community, бенчмарчен) / 1 036,6 (sherpa) | 10,10 % | **3,9** | ~2 ГБ (оценка) | да | 99 языков | **MIT** (`openai/whisper-large-v3-turbo`) | OpenAI (США) — зарубеж. | Must |
| 9 | Whisper small (int8) | Многоязычная, средняя; по-русски слабее GigaAM | 253,5 (onnx-community — единственный экспорт под onnx-asr) / 375,5 (sherpa) | **нет данных по протоколу** (FLEURS 11,4 % — другой протокол, только в подсказке; оценка ≈20 % — не показывать) | **нет данных** (оценка ≈13–16 — не показывать); замер в v0.2 (US-5.4) | ~470 МБ (оценка) | да | 99 языков | **Apache-2.0** (`openai/whisper-small`) | OpenAI — зарубеж. | Must |
| 10 | Whisper base (int8) | Быстрая и лёгкая; **для русского не рекомендуется — ошибка в каждом третьем слове** | 109,1 (istupakov `whisper-base-onnx`, бенчмарчен) / 160,6 (sherpa) | **38,33 %** | 51,6 | ~200 МБ (оценка) | да | 99 языков | **Apache-2.0** (`openai/whisper-base`) | OpenAI — зарубеж. | Must |
| 11 | GigaAM Multilingual Large CTC (int8) | «Качество любой ценой»: точнее v3, тяжёлая, без пунктуации | 591,6 (istupakov) | **5,55 %** | по бенчмарку fp32: 30,4 (int8-замера нет) | ~1,1 ГБ (оценка) | нет | ru, kk, ky, uz, en | MIT | Сбер — отеч. | Could |
| 12 | NeMo FastConformer ru pc (CTC) | Самая быстрая, с пунктуацией, точность ниже | 131,6 (CTC) / 136,4 (RNN-T-декодер: WER 11,57 %, RTFx 54,2) | 13,10 % | 70,6 | ~245 МБ (оценка) | да | ru | CC-BY-4.0 (атрибуция обязательна) | NVIDIA (США) — зарубеж. | Could |
Ревизии `main` всех репозиториев на 2026-09-08 и точные байты — в `research/catalog-numbers.md` §1; манифест закрепляет их как `sources[].revision`. Whisper-репозитории `csukuangfj/sherpa-onnx-whisper-*` **без файла лицензии** — юридически опираемся на карточки `openai/whisper-*` (NOTICE по моделям раздельно, §9.6). Исключены: Silero (CC-NC), Canary/Parakeet (слабы по-русски, CC-BY), GGUF/transcribe.cpp (нужен C++-движок), Whisper medium/large-v3 (непригодны по скорости на CPU — решение 2026-09-07), fp32-варианты GigaAM (885 МБ, без выигрыша). **Рантайм v1 — `onnx-asr`, единый** (решение архитекторов: `arch/plan-claude.md` §4.1, синтез §6, 2026-09-09); источник весов — onnx-asr-экспорты (istupakov / onnx-community / alphacep / t-tech), sherpa-размеры в таблице — справочно; пресеты держим у себя с закреплённым коммитом и sha256.

### 7.3 Формат манифеста каталога (`catalog.json`, версия 1)
```json
{
  "manifest_version": 1,
  "generated_at": "2026-09-07T12:00:00Z",
  "publisher": "astra-voice",
  "models": [
    {
      "id": "gigaam-v3-e2e-rnnt-int8",
      "name": "GigaAM v3 RNN-T",
      "vendor": "Сбер (GigaChat Team)",
      "family": "gigaam-v3", "variant": "e2e_rnnt",
      "engine": "onnx-asr", "layout": "onnx-asr-gigaam-v3",
      "purpose": {"ru": "Русская диктовка с пунктуацией — по умолчанию", "en": "…"},
      "languages": ["ru"], "punctuation": true,
      "origin": {"kind": "ru", "holder_entity": "ПАО Сбербанк (РФ)", "note_ru": null},
      "license": {"spdx": "MIT", "holder": "GigaChat Team, 2024", "url": "https://…/LICENSE", "attribution_required": false},
      "recommended": true, "min_ram_mb": 1024,
      "disk_bytes": 226431968,
      "ram_peak_mb": {"value": 415, "kind": "measured", "cpu": "Intel Core Ultra 7 255U", "threads": 2, "at": "2026-09-04"},
      "quality": {"kind": "benchmark", "wer_pct": 7.60, "dataset": "Russian LibriSpeech test", "weights": "fp32", "runtime": "onnx-asr", "source_url": "https://github.com/istupakov/onnx-asr/blob/main/docs/comparison.md"},
      "speed": {"kind": "benchmark", "rtfx": 42.5, "weights": "int8", "cpu": "Ryzen 7 9800X3D", "source_url": "https://istupakov.github.io/onnx-asr/benchmarks/"},
      "files": [
        {"path": "v3_e2e_rnnt_encoder.int8.onnx", "size": 0, "sha256": "…"},
        {"path": "v3_e2e_rnnt_decoder.int8.onnx", "size": 0, "sha256": "…"},
        {"path": "v3_e2e_rnnt_joint.int8.onnx", "size": 0, "sha256": "…"},
        {"path": "v3_vocab.txt", "size": 0, "sha256": "…"},
        {"path": "v3_e2e_rnnt.yaml", "size": 0, "sha256": "…"}
      ],
      "sources": [
        {"type": "hf", "repo": "istupakov/gigaam-v3-onnx", "revision": "322c3b29492673eb7d0b434bfa9dfb8653e34d02"},
        {"type": "github_release", "base": "https://github.com/ArrivaRUS/astra-voice/releases/download/models-2026.09/"},
        {"type": "corp", "base": "{corp_base}/models/gigaam-v3-e2e-rnnt-int8/"}
      ],
      "update_watch": {"hf_repo": "ai-sage/GigaAM-v3", "branch": "e2e_rnnt", "authors": ["ai-sage", "istupakov", "csukuangfj"], "family_keywords": ["giga-am", "gigaam"]},
      "related": ["gigaam-v3-e2e-ctc-int8", "gigaam-v3-rnnt-int8"],
      "smoke_test": {"wav": "builtin:test-ru-6s.wav", "expect_any": ["проверка", "связи"]}
    },
    {
      "id": "whisper-small-int8",
      "name": "Whisper small", "vendor": "OpenAI",
      "engine": "onnx-asr", "layout": "onnx-community-whisper",
      "languages": ["*"], "punctuation": true,
      "origin": {"kind": "foreign", "holder_entity": "OpenAI (США)", "note_ru": null},
      "license": {"spdx": "Apache-2.0", "holder": "OpenAI", "url": "https://huggingface.co/openai/whisper-small", "attribution_required": false},
      "disk_bytes": 253466497,
      "ram_peak_mb": {"value": 470, "kind": "estimated"},
      "quality": {"kind": "no_data", "note_ru": "По протоколу данных нет; FLEURS 11,4 % — другой протокол"},
      "speed": {"kind": "no_data"},
      "files": [{"path": "encoder_model_int8.onnx", "size": 92326127, "sha256": "…"}, {"path": "decoder_model_merged_int8.onnx", "size": 156750845, "sha256": "…"}],
      "sources": [{"type": "hf", "repo": "onnx-community/whisper-small", "revision": "36050c46d777d46dc4b5f43f6d90574fc38f8732"}]
    }
  ]
}
```
Правила: **`engine`** — в v1 единственное значение `onnx-asr` (единый рантайм, `arch/plan-claude.md` §4.1, синтез §6); поле остаётся в схеме для будущего `sherpa-onnx` — записи с иным `engine` утилита v1 пропускает с отметкой в логе. **`layout`** — одна из **семи раскладок плана §4.1**: `onnx-asr-gigaam-v3` (#1–#3, вариант в `variant`, один репо `istupakov/gigaam-v3-onnx`), `onnx-asr-gigaam-multilingual` (#4, #11), `onnx-asr-t-one` (#5, fp32), `onnx-asr-vosk` (#6, #7; подкаталоги `am-onnx/` / `am/`, `lang/` — явно в `files[].path`), `onnx-community-whisper` (#8, #9), `onnx-asr-whisper-ort` (#10), `onnx-asr-nemo` (#12). Репозиторий, ревизия и файлы — из `research/catalog-numbers.md` §1 (onnx-asr-экспорты); `files[]` с `size` и `sha256` генерирует `scripts/build_manifest.py` по HF API на закреплённой ревизии (в примере первой записи имена — по шаблону плана `v3_<variant>_*.int8.onnx` + vocab + yaml, `size: 0` и `sha256: …` — заполняет спайк S3; `size` обязателен в боевом манифесте). `quality.kind`/`speed.kind` ∈ {`measured`, `benchmark`, `benchmark_fp32`, `no_data`}; `quality.weights` фиксирует, на каких весах измерен WER (в бенчмарке onnx-asr — `fp32`); при `no_data` полоска не рисуется. `origin.kind` ∈ {`ru`, `foreign`} — по юрлицу правообладателя, `note_ru` — уточнение для карточки («команда из России»). `sha256` обязателен для каждого файла (для LFS — из HF API: `lfs.sha256` в `GET /api/models/{repo}?blobs=true` или `lfs.oid` в `/api/models/{repo}/tree/{rev}` — одно и то же значение; для не-LFS файлов вроде `tokens.txt` API даёт git-SHA-1, поэтому sha256 считается при публикации манифеста); `sources[].revision` обязателен, загрузка идёт по `/resolve/{revision}/…`, не по `main`; `update_watch.branch` — ветка-вариант (для `ai-sage/GigaAM-v3` следим за `targetCommit` ветки, не `main`); манифест сопровождается отсоединённой подписью `catalog.json.sig` (тот же инструмент и ключ, что у релизов утилиты — minisign или GPG; все зеркала проверяются одним закреплённым ключом); корпоративный манифест — тот же формат, `sources` может содержать только `corp`; поля `ram_peak_mb.kind` ∈ {`measured`, `claimed`, `estimated`}; локальные замеры хранятся отдельно (`~/.local/state/astra-voice/measurements.json`) и перекрывают манифест в UI; неизвестные поля игнорируются (forward-compat), `manifest_version` > поддерживаемой → отказ с сообщением.

---

## 8. Нефункциональные требования
| Область | Требование |
|---|---|
| Офлайн-первый | Все функции, кроме четырёх сетевых действий (F14), работают без сети; ни одна сетевая ошибка не блокирует UI и не показывает модалку |
| Старт | До готовности трея ≤ 1,5 с; до готовности к диктовке (модель в памяти) ≤ 5 с на эталонной машине; при автозапуске — без `sleep`-костылей (прогноз Researcher #2 для PyQt5 из apt: холодный старт GUI 0,15–0,6 с, low) |
| Задержка | «Отпустил → текст» p95 ≤ 0,5 с (6-с фраза, GigaAM v3 e2e_rnnt, модель в памяти, эталон); ориентир для 4 ядер/8 ГБ — ≤ 1,5 с (оценка, проверить) |
| Память | GUI + трей в покое без модели ≤ 120 МБ RSS (допущение для PyQt5/QML — проверить спайком); воркер с GigaAM v3 ≤ 450 МБ (замер 415; для незамеренных моделей оценка 1,5–3× от int8-весов); суммарно **≤ 600 МБ — установившийся режим**. **При смене модели пик ограничен `MemAvailable`, а не константой**: кандидат грузится во временный второй воркер (старая модель продолжает обслуживать записи) только при `MemAvailable ≥ 1,5 × веса кандидата + 200 МБ`; иначе — выгрузка текущей → загрузка кандидата с состоянием «Переключаю…» (синтез архитектуры, Р5). Замер — VmHWM воркера, дополнительно PSS из `smaps_rollup` |
| CPU в покое | ≤ 1 % CPU в простое (нет опроса микрофона, нет таймеров чаще 1 Гц) |
| Сеть | Только хосты из `PRIVACY.md`; проверки выключены, пока пользователь их не включил; ≤ 1 проверка/24 ч на каждый источник; таймаут 3 с; уважение `RateLimit`/`Retry-After`, `ETag`; прокси из окружения; TLS-проверка не отключается, корпоративный CA — из системного хранилища; User-Agent проекта |
| Приватность | Аудио — только в ОЗУ, никогда на диск; распознанный текст — не логируется и не сохраняется (кроме «последний текст» в памяти); опция защиты истории Klipper |
| Надёжность | Крах воркера не роняет GUI; single-instance; ретрай трея; корректное завершение по `Выход`/SIGTERM (восстановление буфера, освобождение микрофона) |
| Совместимость | Astra Linux SE 1.8.x, X11; **две равноправные сессии: KDE Plasma 5.27 (машина заказчика) и родной Fly (fly-dm / fly-wm / панель Fly — по умолчанию у корпоративных пользователей)** — все сценарии §6 и E2E-прогон P21½ в обеих, S17 для Fly; Python 3.11; PyQt5 5.15; glibc 2.36 (шим `__isoc23_*` для onnxruntime — по синтезу не нужен, контроль `objdump` в CI) |
| Упаковка | Один deb, зависимости из репозиториев Astra + вендоренные python-пакеты; `postinst` без сети; воспроизводимая сборка одной командой; SBOM в релизе |
| i18n | Русский — исходный язык всех строк; английский — полный второй; плюрализация; даты/числа по локали; без английских «хвостов» |
| Доступность | Все настройки доступны с клавиатуры; контраст текста ≥ 4,5:1 в обеих темах; состояние записи никогда не передаётся только цветом (форма/текст/трей/звук); accessible-имена у контролов |
| Локализация ошибок | Каждое сообщение об ошибке = что случилось + что сделать + кнопка действия; пути — реальные для ALSE/KDE |
| Логи | Локально, ротация, без аудио/текста; уровень настраивается |

---

## 9. Безопасность и комплаенс

### 9.1 Целостность моделей
Каждый файл — sha256 из манифеста (для HF — `lfs.oid`), проверка до `rename`; закреплённые ревизии (`pinned_revision`); смоук-тест после обновления ревизии с автоматическим откатом; манифест подписан (наш ключ вшит; корпоративный — из `policy.conf`). Runtime-загрузка модели трактуется как supply-chain-изменение, а не «данные».

### 9.2 Подпись релизов и проверка перед установкой
`SHA256SUMS` + отсоединённая подпись релиза (**один инструмент — minisign или GPG — для релизов и манифеста каталога**, выбор за архитекторами; открытый ключ закреплён в пакете; все зеркала проверяются тем же ключом); проверка обязательна и для «обновить из файла»; **ровно один привилегированный шаг**: собственное polkit-действие (`auth_admin`, без `_keep`) привязано к небольшому root-помощнику `/usr/libexec/astra-voice-update-helper`, а не к `/usr/bin/apt`; помощник повторно проверяет подпись/sha256/имя/архитектуру/версию в root-staging и выполняет `apt-get install --no-remove`; политика ставится из deb в `/usr/share/polkit-1/actions/`, JS-правила в `rules.d` не поставляются; никакого `dpkg -i` и никакой установки без проверки; первая установка — штатным apt (помощник появляется с первым пакетом).

### 9.3 Трек ЗПС
**В v1.0 (заказчик 2026-09-07):** детект `DIGSIG_ELF_MODE`/`policy.conf: zps`, трек B «скачать пакет и передать администратору» (S10), документ для администратора; собственные ELF/`.so` (onnxruntime/движок, помощник обновления — ориентировочно 2–5 объектов, A11) в v1 **не подписаны** ГОСТ-ключом на сборке — на машине с ЗПС их подписывает интегратор/администратор заказчика по своей процедуре (`INSTALL-ADMIN.md`). **После v1.0 (v1.1, Ц5):** подпись всех собственных ELF/`.so` на сборке (`bsign-integrator`, ключ организации), открытый ключ и регистрация админом, `postinst` ничего не подписывает на месте, прогон на ВМ с ЗПС (P21½). Минимизировать число собственных ELF (Python поверх системного Qt) — действует с v0.1.

### 9.4 Профиль «защищённый контур» (приказы ФСТЭК № 117/239, Указ № 166)
`profile=secure`: тумблеры проверок заблокированы в выключенном состоянии (во всех профилях они выключены, пока пользователь не включил), установка — только администратором, офлайн-путь для моделей и утилиты, `domestic_only=true` (зарубежные по юрлицу правообладателя модели скрыты из каталога: Whisper, FastConformer и обе версии Vosk — Alpha Cephei Inc., США), закреплённые ревизии, документ «Влияние на среду функционирования ALSE» для администратора ИБ (tech-writer, до пилота). Установка обновления **всегда** по явному действию пользователя/администратора — тихой автоустановки не существует ни в одном профиле.

### 9.5 Запреты функциональности (явно, L8/L9 из `research/legal.md`)
Утилита **не содержит и не будет содержать в v1**: диаризацию и распознавание говорящего; «профиль голоса», голосовую аутентификацию/разблокировку; wake-word, привязанный к сотруднику; хранение аудио, аудио-сниппетов и эмбеддингов голоса; телеметрию, crash-dumps, «отправить отчёт» на любой сервер, собственный сервер обновлений с логами; скрытую запись микрофона (запись только по хоткею, всегда с индикацией в трее); автозапуск без видимого UI/иконки; эксфильтрацию текста/аудио; обход СЗИ. Появление любого из этого = новое правовое заключение (разработчик становится оператором ПДн).

### 9.6 Документы и атрибуции
`LICENSE` (GPL-3.0-or-later), `NOTICE` (каждый компонент и каждая модель: имя, правообладатель, лицензия, ссылка; sherpa-onnx Apache-2.0 §4(d); Whisper — **по моделям раздельно**: large-v3-turbo — MIT, small — Apache-2.0, base — Apache-2.0 (по карточкам `openai/whisper-*`; репозитории `csukuangfj/sherpa-onnx-whisper-*` файла лицензии не содержат — источник права первоисточник OpenAI); Vosk — Apache-2.0, Alpha Cephei Inc.; CC-BY — с атрибуцией; лицензия фиксируется в манифесте для каждого HF-репо), `PRIVACY.md` (хосты, что и когда уходит, как выключить), дисклеймер о товарных знаках в README и «О программе». Задача комплаенса вне PRD, но до публичного релиза: **письменное согласие ПАО «Группа Астра» на имя Astra Voice** (L1) — риск принят заказчиком; без согласия релиз помечается как гейт G5 с открытым вопросом.

### 9.7 Модель угроз
T1 (проектирование) — security-analyst по `security-gate`: поверхность = загрузка моделей, обновлятор (polkit), глобальный хоткей и вставка в чужие окна, `policy.conf`, парсинг манифеста. Ревью T2 на каждом изменении обновлятора; T3 перед публичным релизом.

---

## 10. Аналитика (локальная, без телеметрии)
События пишутся **только** в `~/.local/state/astra-voice/stats.json` и показываются в «О программе → Статистика» (кнопка «Очистить»); никуда не отправляются.
| Событие | Поля | Под метрику |
|---|---|---|
| `dictation` | model_id, audio_ms, t_ms (отпустил → текст готов), paste_ms, cold(bool), result(ok/empty/cancelled) | Ц2 (p95 задержки), полоска скорости |
| `model_measure` | model_id, revision, peak_rss_mb, threads, cpu, ts | Ц6, карточка ОЗУ |
| `mic_error` | kind (none/busy/silent/other), recovered_by (restart_wireplumber/retry/none) | качество F6 |
| `update_check` | source (models/app/catalog), result (ok/none/unavailable/ratelimit), ts | Ц4, F8/F9 |
| `update_apply` | kind (model/app), track (A/B/file), result | Ц5 (v1.1), F9 |
| `onboarding` | step_reached, duration_s | Ц3 |
Отдельно ручные замеры: Ц3 (секундомер на ВМ), Ц4 (`nethogs`/`tcpdump`), Ц5 (прогон на ВМ с ЗПС), Ц7 (время сборки).

---

## 11. Не-цели / вне скоупа v1
- Постобработка текста LLM (локальная или облачная), «умные» замены, словари терминов (кастомные слова — после v1).
- Wayland; macOS/Windows; другие дистрибутивы (Debian-совместимость — бонус, не цель).
- Облачные движки распознавания и любые сетевые ASR.
- Диаризация, идентификация говорящего, «профиль голоса», wake-word (запрет §9.5).
- Стриминговое распознавание «по словам» во время речи (только если архитекторы покажут, что это дёшево на T-one; по умолчанию — нет).
- История распознаваний на диске (P2 после v1, по умолчанию выкл).
- GPU/NPU-ускорение; GGUF/transcribe.cpp.
- Свой apt-репозиторий (стратегия для корпоративов — после v1; в v1 — deb в GitHub Releases + корпоративный URL).
- ГОСТ-подпись собственных ELF/`.so` для ЗПС, ключ организации, прогон на ВМ с ЗПС (Ц5) — v1.1, после v1.0 (заказчик 2026-09-07).
- Плазмоид/виджет панели вместо пилюли.
- Реестр российского ПО, сертификация ФСТЭК утилиты.

---

## 12. Открытые вопросы и допущения

### 12.1 Открытые вопросы к заказчику (меняют продукт)
**Нет.** Шесть вопросов ревизии 1 закрыты заказчиком и Юркой 2026-09-07 (`decisions/log.md`, «Ответы заказчика на открытые вопросы PRD»): 1 — сетевые тумблеры пусты во всех профилях; 2 — `Ctrl+Space` push-to-talk, toggle второй; 3 — модель в памяти постоянно, выгрузка — опция; 4 — ЗПС-трек (подпись, ВМ, Ц5) после v1.0; 5 — Whisper ×3; 6 — автозапуск включён по умолчанию, в v0.2.

### 12.2 Допущения (помечены, не выдуманы как факты)
- A1. Размер deb без моделей ≤ 80 МБ и память GUI в покое ≤ 120 МБ — до спайка PyQt5/QML не подтверждены.
- A2. ОЗУ моделей 2–11 — оценка ×1,85 от int8-весов; заменяется замером после первого прогона.
- A3. Восстановление буфера — только для текстового содержимого; изображения/файлы в буфере не восстанавливаются.
- A4. `DIGSIG_ELF_MODE` читается обычным пользователем (на машине заказчика — да); иначе — вопрос пользователю/policy.
- A5. Whisper в `onnx-asr` покрывает нужные варианты; иначе — `sherpa-onnx` для Whisper (архитекторы).
- A6 (закрыто 2026-09-08). Происхождение Vosk: по юрлицу — Alpha Cephei Inc. (Делавэр, США), команда из России → «зарубежная · команда из России», вне фильтра «только отечественные». Не проверялось: наличие Vosk/GigaAM в реестре отечественного ПО.
- A7. Пункты 39 приказа № 117 и 29.3.3 приказа № 239 цитируются по вторичным источникам (L14) — требования §9.4 сформулированы консервативно.
- A8. Английский UI — полный перевод, но без отдельного тестирования сценариев на английском.
- A9. GitHub Releases как зеркало весов допустимо по MIT/Apache; лимит размера ассета GitHub (2 ГБ) достаточен для всех моделей каталога.
- A10. Срок Ц5 (ЗПС) — ориентир 2026-12-31 (v1.1); дата назначается при планировании v1.1.
- A11. Число собственных неподписанных `.so`/ELF в deb — 2–5 (onnxruntime, движок, помощник обновления) — подтвердить на закреплённых wheel'ах (`find … -exec file`); по синтезу Р2 — ровно 3 `.so`, счётчик в CI.
- A12. Fly-сессия доступна для E2E на машине заказчика переключением сессии в fly-dm (без отдельной ВМ); панель Fly и её трей (SNI или XEmbed), служба уведомлений и polkit-агент Fly — как в штатной поставке ALSE 1.8.

---

## 13. QA-чеклист PRD (10 пунктов + обязательный блок)
- [x] Каждая цель SMART (число + срок) — §1.3, Ц1–Ц7.
- [x] Целевые пользователи конкретны — §1.2, П1–П3, с не-целевыми.
- [x] У каждой фичи user story As a / I want / so that — F1–F16.
- [x] Бизнес-правила исчерпывающие, без «и т. д.» — проверено по F1–F16 (перечни закрыты: состояния, комбинации, хосты, поля карточки, ключи policy).
- [x] Покрыты исключения: таймауты (F8/F9, S6), нет прав (F9 трек B, S10), битые данные (sha256, манифест, settings.json), границы (0,3 с / 120 с / диск / ОЗУ / 429).
- [x] Acceptance Given-When-Then у каждой фичи — §6 S1–S16 + inline у F13, F14, F15.
- [x] Приоритеты P0/P1/P2 проставлены — у каждого модуля.
- [x] Не-цели указаны — §11.
- [x] События аналитики под метрики — §10 (локально, без телеметрии).
- [x] Открытые вопросы/допущения вынесены — §12.
- [x] Блок «Проверка гипотезы»: эффект · метрика · дата (2026-11-15) · решение при недоборе — §1.4.

Далее: `stories.md` (INVEST + story map) → `backlog.md` (RICE + MoSCoW + релизы) → ⛔ G1.

---

## 14. Что изменилось в ревизии 2 (2026-09-07)
| # | Где | Было (0.1) | Стало (0.2) | Источник |
|---|---|---|---|---|
| 1 | F1, F8, F9, F14, S1-A1, §3, §8, §9.4 | Предвыбор «проверять обновления» для личного профиля | Оба тумблера **пусты во всех профилях**, включает пользователь; №1/№4 — после включения | заказчик, вопрос 1 |
| 2 | F2, F1 (экран 3), §3 | Хоткей «`Ctrl+Space`-подобный, уточнить» | **`Ctrl+Space`** push-to-talk; toggle — второй режим | Юрка, вопрос 2 |
| 3 | F4, S2-A5, §3 | «Никогда не выгружать» как допущение | Модель в памяти постоянно — решение; выгрузка по простою — опция | заказчик, вопрос 3 |
| 4 | Ц5, §3, §4, F9 (трек B), §9.3, §11, S10-A5, A10 | ГОСТ-подпись ELF/.so + ВМ с ЗПС в v1.0 | **После v1.0 (v1.1)**; в v1.0 — детект `DIGSIG_ELF_MODE`, трек «передать администратору», документ для админа | заказчик, вопрос 4 |
| 5 | F10, §3 | Автозапуск P2, v1.0 | **P1, v0.2**, включён по умолчанию на последнем экране онбординга | заказчик, вопрос 4/6 |
| 6 | §7.2 | 8 Must + 1 Should + 2 Could; e2e_rnnt «222 МБ» | **10 Must + 2 Could** (по своду summary §6: Multilingual 220M, T-one, Vosk ru добавлены в Must; Large — Could); размеры — десятичные МБ по HF API (**232 МБ**) | summary §6, tech-codex (сверка csukuangfj) |
| 7 | F7, §7.3 | sha256 «из `lfs.oid`», загрузка с `main`, `rename` | `lfs.sha256`/`lfs.oid` — одно значение; не-LFS — считать при публикации; загрузка по `/resolve/{revision}/`; каталог `<ревизия>/` + `current.json`; запись каталога привязана к рантайму | tech-codex блок 2 |
| 8 | F7 | `quality = clamp(1 − WER/25 %)` | `clamp((30 % − WER)/25 %)` — WER ≤ 5 % = полная полоска; пороги видны пользователю; ОЗУ-оценка с разбросом 1,5–3× | tech-codex «как честно рисовать полоски» |
| 9 | F8 | Поиск близкой модели по белому списку авторов | Уточнено: `author=salute-developers` не находит `ai-sage`; список авторов — в манифесте; GitHub без Releases/тегов — не источник; `HF_HUB_OFFLINE` уважается | tech-codex блок 2, summary §1 |
| 10 | F9, §9.2, S9-A2 | `pkexec apt-get install` из GUI; fallback `pkcon` | **Один** привилегированный шаг через root-помощник с собственным polkit-действием (`auth_admin`), повторная проверка в root-staging, `apt-get install --no-remove`; запрет привязки к `/usr/bin/apt`, `_keep`, `rules.d`; один инструмент подписи (minisign/GPG) для релизов и манифеста | tech-codex блок 3, summary §1 |
| 11 | F5 | — | Вставка в окно, активное **до** показа пилюли (`_NET_ACTIVE_WINDOW`) | tech-codex 5.4 |
| 12 | F14, §8 | Прокси из окружения | + корпоративный CA из системного хранилища, TLS не отключать; `ETag`, лимит GitHub 60/ч; прогноз старта PyQt5 0,15–0,6 с | tech-codex блоки 2–3, 5.1 |
| 13 | §12.1 | 6 открытых вопросов | Открытых вопросов **нет**; добавлены допущения A10, A11 | decisions/log.md |

## 15. Что изменилось в ревизии 3 (2026-09-08, только данные каталога)
| # | Где | Было (0.2) | Стало (0.3) | Источник |
|---|---|---|---|---|
| 1 | §7.2 | Размеры — 9 из 12 в МиБ, записанных как МБ (214 / 138 / ~70 / ~26 / 987 / 357 / 153 / 564 / 125) | Десятичные МБ по байтам HF API: 224,8 / 144,2 / 72,5 / 26,7 / 1 089,1 (1 036,6 sherpa) / 253,5 (375,5) / 109,1 (160,6) / 591,6 / 131,6; GigaAM v3 ×3 — 231,9 / 224,9 / 229,3 | catalog-numbers §1, §4.1 |
| 2 | §7.1, §7.2, F7 | WER/RTFx частично из fp32-столбца; без колонок в таблице | Колонки WER (fp32, Russian LibriSpeech) и RTFx (x64 int8) по протоколу; пометки «по бенчмарку fp32» (T-one 26,3; Multilingual 58,5 / 30,4) и «нет данных по протоколу» (Whisper small — замер в v0.2, US-5.4); оговорка «WER бенчмарка измерен на fp32-весах» | catalog-numbers §0, §2, §4.2–4.3; решение Юрки 2026-09-08 |
| 3 | §7.1, §7.2 | Whisper base «для русского не рекомендуется» | WER 38,33 %, RTFx 51,6; подпись «ошибка в каждом третьем слове» | catalog-numbers §4.2 |
| 4 | §7.2, §9.6, F13 | Whisper «MIT / Apache-2.0 (обе в NOTICE)» на всех трёх | По моделям: large-v3-turbo — MIT; small и base — Apache-2.0; NOTICE раздельно; csukuangfj-репо без лицензии — опора на `openai/*` | catalog-numbers §4.4 |
| 5 | §7.2, F7, §9.4, A6 | Vosk «отеч. (уточнить)» | «Зарубежная · команда из России» (Alpha Cephei Inc., Делавэр, США) — по юрлицу; обе версии вне фильтра «только отечественные»; A6 закрыто | catalog-numbers §3; решение Юрки 2026-09-08 |
| 6 | §7.2, Ц6 | T-one — Must (10 Must) | T-one — **Should** (fp32 144,2 МБ, int8 нет; ОЗУ без оценки); каталог 9 Must + 1 Should + 2 Could (10 позиций в v1 — US-5.8 без изменений) | решение Юрки 2026-09-08 |
| 7 | §7.3 | `engine: onnx-asr` при csukuangfj-раскладке (несовместимо) | На модель `engine` ∈ {onnx-asr, sherpa-onnx} + `layout`; пример согласован (sherpa-onnx ↔ csukuangfj), второй пример — onnx-asr ↔ onnx-community; `quality/speed.kind` ∈ {measured, benchmark, benchmark_fp32, no_data}, `quality.weights`; `origin` — объект по юрлицу; CPU бенчмарка — Ryzen 7 9800X3D. Выбор рантайма v1 — за архитекторами | catalog-numbers §4.4; решение Юрки 2026-09-08 |
| 8 | F11, F12, F3.2 | — | Сверено с правками Юрки после G2: «Проверить обновления» в трее всегда активно (кроме офлайн/policy); окно 900×620 минимум; сайдбар из 6 разделов («Сеть и обновления» — отдельный); «Готово» в пилюле 500 мс | decisions/log.md 2026-09-08 (G2, полировка) |

## 16. Что изменилось в ревизии 4 (2026-09-09)
| # | Где | Было (0.3) | Стало (0.4) | Источник |
|---|---|---|---|---|
| 1 | §6 S17 (новый), Ц1, Ц5, §8 «Совместимость», A12, F3.4, F3.7, F5.6, F9, F11.2, S16 | Fly упомянут одной строкой «KDE Plasma 5.27 и Fly» | **Fly-сессия ALSE — равноправная целевая среда v1.0**: сценарий S17 (A1–A9: регрессия S2–S5/S8–S13, пилюля под fly-wm без кражи фокуса, трей SNI/XEmbed с состоянием по форме и подсказке, уведомления Fly + запасной баннер, polkit-агент Fly, автозапуск, тема без kdeglobals, буфер без Klipper, хоткей/вставка); E2E в обеих сессиях; допущение A12 | decisions/log.md 2026-09-09 «Требование заказчика: KDE и родной Fly» |
| 2 | §8 «Память» | «суммарно ≤ 600 МБ» | ≤ 600 МБ — установившийся режим; при смене модели пик ограничен `MemAvailable`: временный воркер только при `MemAvailable ≥ 1,5 × веса кандидата + 200 МБ`, иначе выгрузка → загрузка со состоянием «Переключаю…» | arch/plan-synth.md §3 Р5, §6 |
| 3 | §7.2, §7.3 | Рантайм не выбран; пример манифеста — sherpa-onnx (csukuangfj) + onnx-asr | **`engine: onnx-asr` для всех записей** (единый рантайм v1; поле остаётся для будущего sherpa-onnx); пример — `istupakov/gigaam-v3-onnx` @ `322c3b29…`, 226,4 МБ, `layout: onnx-asr-gigaam-v3`; список из семи `layout` по плану §4.1; sherpa-размеры — справочно | arch/plan-claude.md §4.1; синтез §6 |
| 4 | S9-A5, F12 | — | Сверено: ручная проверка обновлений («Проверить сейчас» в трее и в «Сети») активна всегда, кроме офлайн-режима/policy — S9-A5 (правка Юрки) и F12 согласованы, правок не потребовалось | синтез §3 Р9 |
| 5 | §8 «Совместимость» | шим `__isoc23_*` — «архитекторам» | по синтезу Р2 шим не нужен (ORT 1.29.0, max GLIBC_2.28), контроль `objdump` и счётчик ELF = 3 в CI | arch/plan-synth.md §3 Р2 |
