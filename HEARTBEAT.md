# HEARTBEAT — где мы сейчас

> «Пульс» проекта astra-voice: подхватывается Юркой при старте сессии. **Обновлено:** 2026-09-14 закрытие дня

## Фаза
**Разработка (Фаза 4)** — G0–G4 пройдены; M0 (KDE) и M1 приняты 2026-09-09; далее M2.

## Сделано
- 2026-09-07: интейк (`PROJECT.md`), публичный репо `ArrivaRUS/astra-voice`, память в git, риск-теги.
- 2026-09-07: Discovery закрыт — 5 отчётов в `research/`, свод `research/summary.md`; право 🟡
  (имя 🔴 — согласие Астры; LICENSE → GPL-3.0-or-later).
- 2026-09-07: G2a закрыт — имя **Astra Voice**, «слушаю» бирюза, знак 06 «Слоги», лого-пак (`design/brand/logo-pack.html`).
- 2026-09-07: **G1 принят** — PRD 0.2 / stories 0.2 / backlog 0.2 (три релиза, ЗПС-подпись в v1.1).
- 2026-09-08: **G2** — направление A «Панель» (из A/B/C); flows.md + 30 экранов направлений в репо.
- 2026-09-08: **G3 принят** — полный макет A (48 экранов), spec.md 0.2, tokens 2.1.1, refs; PRD 0.3 (цифры каталога по ресёрчу).
- 2026-09-09: **архитектура** — планы #1 (Fable) и #2 (GPT-6 Astra), синтез, челлендж Astra (22 находки закрыты), T1 (40 угроз), G4 (v1.0 = .deb, user-bundle v1.1), Fly-адаптеры; PRD 0.4 (S17 Fly); урок `.patches/001`; codex 0.153.4.
- 2026-09-07: рамки до PRD — оба режима обновления, PyQt5 из apt, пилюля по умолчанию с выключателем.
- Codex CLI поднят до 0.153.4 — канал `gpt-6-astra` открыт для архитектора #2.

## В работе (2026-09-14, вечер — сессия после компактификации)
- **M0 (KDE) ✅ · M1 ✅ · M2 ✅ · M3 ✅ · M4 — код завершён**: CI зелёный на `514956a` (пакеты Qt в job `unit`, гейт Qt-окружения,
  диагностика `engine`; урок 005 доп., T-61…T-63); пилюля `6f335f9` — DesignReviewer PASS 0 расхождений, снимки детерминированы;
  **мостик загрузки модели** (пробел M4: GUI не слал `model.load`) — `core/model_request.py`, загрузка по `hello` воркера
  (старт и самоперезапуск), пилюля `loading-model` ≤ 10 с, «Модель не загружена», повтор по нажатию в простое, «подробности» →
  окно; три цикла ревью Claude (B1/M1, M-A/M-B, minor) — PASS; 2217 unit + 56 xvfb; changelog `0.1.0~m4.1`. Коммит C — см. git log.
- **Дефект движка на Intel Xeon с AMX** (пустой текст; `research/tech-amx-int8.md`, уроки 006/007): причина — MLAS onnxruntime
  ≤ 1.24.4; апгрейд ≥ 1.25 даёт ×2 замедление на машине заказчика (быстрый u8u8-путь не вернули до 1.30), onnx-asr исключает
  1.25/1.26 из-за чужого дефекта. Ветка `wip/ort-1.25.1` в worktree `~/.cache/astra-voice-dev/wt-ort125` (не влито).
  **Решение — спайками S6-a (переквантовка в u8s8) и S6-b (seccomp-запрет AMX на 1.24.4), выбор двумя архитекторами по цифрам, до R2.**
  CI `engine` красный на Intel-раннерах (≈½ прогонов) — известно, не маскируем.
- **Проба заказчика m4.1 (17:32) — диктовка не поднялась**: `bootstrap.py` в пакете лежит рядом, а не внутри (`supervisor.py:17`, с M2); урок 008 (+ правило 5: ресурсы — трей Fly читал SVG из удалённого `data/icons`). Починка: `supervisor.launcher_path()`, `find_tray_icon_path()` (тема → dev), traceback в журнале, `packaging/smoke-installed.sh` (41 модуль + 15 ресурсов + `--version`) в `build-deb.sh` и job'ах `deb`/`release`, AST-гейт `test_deb_layout_imports`, `timeout-minutes` ×7, два флака `test_worker_main` → `wait_until` (20/20); 2234 unit. **Ревью PASS, коммит `ae3e021`, `0.1.0~m4.2` на `~/Desktop` (m4.1 снят); заказчику отправлены команды install + запуск.**
- **Проба m4.2 (18:38)**: старт ок, трей, модель за 767 мс; хоткей (18:45) → `FrameError «Неверный тип поля device»` (`record.start` с `device: None`; `_command` глотал без traceback; тесты оркестратора подменяли `_send` — урок 007 доп., правило 6). Починка → `0.1.0~m4.3` (сообщение по списку полей схемы, `exc_info`, `test_ipc_contract` через настоящий `ipc.encode`); ревью Claude → коммит → `~/Desktop`; заказчику: install, «Выход» из трея, запуск заново.
- **Живая приёмка M4 не проведена.** Пакет `0.1.0~m4.1` собран из финального дерева → `~/Desktop`; `model_dir` прописан в
  `~/.config/astra-voice/settings.json` (резервная копия `settings.json.bak-20260914`). Заказчику отправлены три команды
  (install, stop Handy, запуск) и просьба «ок» на автопрогон 50 диктовок.
- Отложено: minor 3 ревью CI (`qml-module-qtquick-window2`/`qtgraphicaleffects` не подтверждены гейтом); `python3-sympy` в
  `Depends` избыточен с ORT 1.25; `design/refs/09-tray-dark.png` устарел (M11); `ui/pill.py` `_visibility_changed` при
  разрушенном `QQuickView`; вопрос developer: ставить ли `xvfb` на машину; повтор загрузки по хоткею читает настройки старта.
  Nit'ы ревью №4 (пакет): `paths.icon_theme_dir()` в dev указывает над репо (пробовать тему только вне dev); AST-гейт `test_deb_layout_imports` видит только `mv` (добавить `rm`, каталоги) и регэксп рецепта прихватывает соседние цели; смоук не проверяет иконку приложения из `.desktop`.

## Следующий шаг (первое действие следующей сессии)
1. Если заказчик установил m4.1 и дал «ок»: `scripts/e2e/virtual_mic.sh up && scripts/e2e/dictate50.sh --session kde --yes`
   → `astra-voice --stats` (p95 ≤ 500 мс); ручные T-50/T-56/T-58; пробник `Ctrl+Alt+Space` после остановки Handy (S18-A9) →
   **M4 ✅** в `docs/plans.md` (+ факты живого прогона в `status.md`). Если нет — напомнить команды (ниже) и ждать.
2. Спайки S6-a/S6-b (developer-codex, по одному брифу на каждый; замеры на свободной машине) → `dual-architect` → решение по
   движку → ИБ T2 на выбранный путь. До этого `engine` в CI красный на Intel — не «чинить» ослаблением.
3. M5 «Онбординг и первая модель» → R1 v0.1 (30.09). Мастер-ключ подписи — до 25.09. M0 Fly — при перелогине.

Команды заказчику (KDE, 2026-09-14): `sudo apt install ~/Desktop/astra-voice_0.1.0~m4.1_amd64.deb` ·
`systemctl --user stop app-Handy@autostart.service` (Handy держит `Ctrl+Space`; вернуть: `start`) · `astra-voice` → курсор в поле
ввода → удерживать `Ctrl+Space`, говорить, отпустить.

## Открытые СТОП-точки
- нет (следующая — G5 на первый релиз v0.1).
- Открытый вопрос заказчику: письменное согласие ГК «Астра» на имя (L1 в `research/legal.md`).

## Блокеры
- нет.

## Anti-drift (конец сессии 2026-09-09)
- delegations: 37 (за 09.09 — 15)
- yurka_direct_actions: 0 (микроправки PRD/спеки ≤3 строк, синтез и свод — зона Юрки)
